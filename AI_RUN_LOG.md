# Bitacora de turnos de trabajo IA

El archivo de entrada unico para iniciar trabajo en este proyecto es `AGENTS.md`.

Si una IA abre primero esta bitacora, debe volver a `AGENTS.md`, seguir el protocolo de arranque, leer `CONTEXT.md`, auditar la ultima entrada y recien despues trabajar.

Agregar cada nueva entrada arriba de todo, debajo de esta introduccion.

## 2026-07-09 14:45 -03:00 - Claude Sonnet 5

- Objetivo: El usuario pidió poder elegir a mano qué PDF corresponde a un diseño puntual (cuando el emparejamiento automático por nombre falla o elige mal), aclarando que debía vivir en Diseños y que Producción debía heredarlo sin cambios propios.
- Cambios: Investigado antes de tocar código: un explorador de archivos nativo del navegador no sirve para esto (no da una referencia persistente a un archivo, solo su contenido una vez). En cambio, se encontró que el Bridge ya expone `/pdf-catalog` (lista completa de PDFs ya escaneados) con su función cliente lista (`getBridgePdfCatalog`), pero sin ningún consumidor — era la pieza que faltaba conectar.
  **SQL** (`sql/design_manual_pdf_link.sql`, ya aplicada): 3 columnas nuevas en `designs` (`manual_pdf_root_name`, `manual_pdf_relative_path`, `manual_pdf_file_name`), globales por diseño (no por PC).
  **Cliente** (`app/admin/page.js`): nuevo botón "🎯" al lado del ícono de vincular existente en cada fila de Diseños, abre un modal con buscador sobre el catálogo completo del Bridge. Nueva función `manualPdfMatchFor(design)` arma un objeto con la misma forma que devuelve el Bridge al matchear automático (mismo `found`/`fileName`/`rootName`/`relativePath`, con `matchType:'manual'` para distinguirlo) — clave para que nada más en el código note la diferencia. `refreshDesignPdfLinks` (la función existente que llama al Bridge) ahora pisa el resultado automático con el vínculo manual de cada diseño que lo tenga, antes de guardar el estado compartido `designPdfMatches`. Como ese estado es el mismo que ya se pasaba a `ProductionTab` sin cambios, Producción hereda el vínculo manual automáticamente — no se tocó `ProductionTab.js` para nada, tal como pidió el usuario.
- Verificacion: Migración probada dentro de una transacción (columnas + un UPDATE de prueba, con rollback antes de aplicarla para real). `CI=true npm run build` OK sin errores nuevos. Se releyó el diff completo.
- Auditoria: N/A, feature nueva y aislada.
- Pendiente/Riesgos: No se pudo probar en un navegador real con el Bridge conectado de verdad (mismo límite de siempre). Documentado en `PDF_MANUAL_LINK_FEATURE.md` y enlazado desde `CONTEXT.md`.

## 2026-07-09 14:05 -03:00 - Claude Sonnet 5

- Objetivo: El usuario confirmó que Ctrl+N le sigue abriendo pestaña nueva del navegador y pidió mantener Alt+N funcionando mientras tanto.
- Cambios: `app/admin/page.js` — se restauró el handler de Alt+N (que el turno anterior había reemplazado en vez de sumar) y se dejó el de Ctrl+N igual al lado, comentado explicando por qué no puede funcionar. Título del botón flotante vuelto a "Crear pedido (Alt+N)".
- Verificacion: `CI=true npm run build` OK.
- Auditoria: N/A, cambio puntual sobre el turno anterior.
- Pendiente/Riesgos: Ctrl+N no se puede interceptar desde JS de pagina en navegadores normales (Chrome/Firefox/Edge lo reservan a nivel de navegador para "ventana nueva", ni preventDefault ni stopPropagation lo evitan) — se le explicó al usuario en el chat, no es arreglable desde este código. Si en el futuro quiere un atajo con Ctrl que sí funcione, Ctrl+Alt+N es una alternativa viable (no reservada por los navegadores).

## 2026-07-09 13:40 -03:00 - Claude Sonnet 5

- Objetivo: Lista grande de 9 pedidos del usuario (mas uno de "pie de pagina web" explicitamente pausado, "aun no hagas cambios") repartidos en dos bloques: Producción y Carga de Pedido Rápido (CreateOrderModal).
- Cambios:
  **Carga Pedido Rápido** (`components/CreateOrderModal.js`, `app/admin/page.js`):
  1. Atajo cambiado de Alt+N a Ctrl+N (`e.ctrlKey || e.metaKey`, ambos casos de `n`/`N`), con `stopPropagation()` ademas de `preventDefault()` para intentar ganarle al atajo nativo del navegador (no se puede garantizar al 100% en todos los navegadores/SOs, pero es lo maximo que se puede hacer desde código de página).
  2. El botón flotante "+" ahora solo se renderiza si `visibleTabIds.includes('orders')` — deja de ser un botón aparte y pasa a depender de la visibilidad real de la pestaña Pedidos.
  3. Cantidad: el `<input type="number">` controlado directo por `row.qty` se reemplazó por un buffer de texto propio (`qtyVal`, mismo patrón que ya usaba el input de diseño) con `type="text" inputMode="numeric"` y limpieza manual de no-dígitos — se sospecha que el input numérico nativo se re-normalizaba en cada tecla en algunos navegadores, haciendo que el segundo dígito reemplazara al primero en vez de sumarse; no se pudo reproducir en vivo en este entorno (sin navegador real), se aplicó el patrón robusto ya usado en el mismo archivo para el otro campo.
  4. Fila de diseño vacía que no vuelve a ser editable: causa raíz confirmada — el `<span onClick>` que reabre edición queda con contenido vacío cuando `inputVal===''`, colapsando a ~0px de ancho dentro de su `div` padre (que no tenía `onClick` propio), dejando esa celda sin superficie clickeable. Se agregó el mismo `onClick` al `div` contenedor (`flex:1`, llena toda la celda).
  **Producción** (`components/ProductionTab.js`):
  5. Buscador de diseño: nuevo input de texto arriba de la tabla de detalle del pedido, filtra `selectedOrderTasks` antes del `.map()` (el resumen de arriba sigue usando la lista completa sin filtrar, no se ve afectado).
  6. Botón de imprimir: las 2 fórmulas de "cantidad restante a imprimir" (`printSingleTask` y el input inline de hojas por fila) usaban `task.produced_qty` (troquelado); se cambiaron a `task.printed_qty` (impreso), que es lo que el usuario pidió.
  7. Vendedor ve su pedido aunque no sea el operario asignado: nuevo memo `currentViewerSellerId` en `app/admin/page.js` (matchea `sellers.email` contra `currentUser`), pasado como prop `currentSellerId` a `ProductionTab`; el filtro de `produceOrderRows` ahora también deja pasar `order.seller_id === currentSellerId`.
  8. Info no editable de quién/cuándo cargó un pedido: nuevas columnas `orders.created_by_email`/`created_by_name` (`sql/order_creator_audit.sql`, ya aplicado), completadas en `createAdminOrder` (solo pedidos cargados a mano desde el admin — los de checkout de cliente no tienen "creador admin" que mostrar, ya se ve la fecha y el cliente). Se muestra con el mismo componente `InfoTooltip` de la feature de diseños extra, en el modal "Ver pedido" y en el encabezado de Producción.
  9. Movimientos de stock: nuevas columnas `production_stock_log.order_id`/`order_code`/`actor_email`/`actor_name` (`sql/stock_log_audit.sql`, ya aplicado), completadas en `update_production_task_progress()` e `increment_production_task_counter()` (las 2 únicas funciones que insertan ahí; se investigó "impresión rápida" — `QuickPrintOverlay.js` no hace ninguna escritura a Supabase directamente, así que hoy no existe un tercer camino de escritura sin pedido asociado más allá de la edición manual inline, que ya no llevaba pedido). Tabla del log: 2 columnas nuevas (Pedido, Usuario) y el contenedor pasa a tener `overflowY:auto, maxHeight:480` para poder scrollear.
- Verificacion: Las 2 migraciones SQL se probaron dentro de una transacción de prueba (mismo patrón de hoy: pedidos/tareas descartables, JWT simulado de admin y de operario, verificación de que `order_id`/`order_code`/`actor_email`/`actor_name` quedan bien poblados en ambas funciones, limpieza completa antes del COMMIT — incluyendo restaurar `production_stock.qty_produced` al valor previo para no ensuciar el stock real). `CI=true npm run build` OK sin errores nuevos. Se releyó el diff completo de los 3 archivos de código modificados.
- Auditoria: Se encontró de paso (no se tocó, fuera de alcance) una superposición de 2 versiones de `update_production_task_progress` en la base (una vieja de 4 parámetros sin `printed_qty`, inalcanzable desde el cliente actual porque siempre manda `p_printed_qty` con nombre) — queda anotado en el comentario del SQL para quien lo encuentre después.
- Pendiente/Riesgos: El punto de "pie de página web" quedó explícitamente sin implementar (el usuario pidió solo lluvia de ideas, se le devolvieron sugerencias en el chat). No se pudo probar nada de esto en un navegador real (mismo límite de siempre). El fix del input de cantidad (punto 3) se aplicó por patrón robusto sin poder confirmar la causa exacta del bug original — si el problema persistiera, revisar si hay algo más específico del navegador del usuario.

## 2026-07-09 12:15 -03:00 - Claude Sonnet 5

- Objetivo: Investigacion sobre Cached Egress de Supabase (sin cambios de codigo de producto). El usuario pidio: (1) revisar si ya existia algo para que las imagenes del catalogo cacheen del lado del navegador del visitante, (2) una tercera revision a fondo para confirmar que lo encontrado realmente funciona, (3) documentarlo si correspondia.
- Cambios: Se configuro acceso a la Management API de Supabase (token del usuario como variable de entorno `SUPABASE_ACCESS_TOKEN`, nunca pegado en el chat ni guardado en el repo/memoria). Se encontro que `app/api/asset/[...path]/route.js` + `components/SafeImage.js` (agregados 2026-07-04 por una IA anterior, no documentados hasta ahora) ya resuelven exactamente el problema: Supabase Free no manda el `Cache-Control` real configurado al subir un archivo (esa parte requiere "Smart CDN", exclusivo de plan Pro+, confirmado por la Management API que este proyecto esta en plan Free) — el proxy propio re-sirve el archivo con `Cache-Control: public, max-age=31536000, immutable` real, y `SafeImage` reescribe automaticamente cualquier URL cruda de Supabase Storage para pasar por ese proxy. Se verifico en profundidad (no solo lectura de codigo): 100% de las URLs de `designs.image_url/optimized_image_url/model_url` matchean el patron que reescribe `SafeImage` (sin hosts distintos ni query strings que fragmenten el cache), grep exhaustivo confirma que TODO punto de render de imagen de cara al cliente usa `SafeImage` (catalogo, carrito, landing, chat), se confirmo por que es seguro cachear "para siempre" (cada version de imagen optimizada se sube con un nombre de archivo con timestamp fresco, nunca se pisa el mismo path), y se probo en vivo contra produccion: primer pedido a `/api/asset/...` da `X-Vercel-Cache: MISS` con el header correcto, pedidos siguientes dan `HIT` (confirma cache de navegador Y de borde de Vercel funcionando, sin depender de Supabase Pro). Se documento todo en `IMAGE_ASSET_CACHING.md` (nuevo, mismo patron que `EXTRA_DESIGNS_FEATURE.md`/`VOICE_ORDER_FEATURE.md`) y se enlazo desde `CONTEXT.md`.
- Verificacion: Pruebas en vivo contra `https://www.inkora.com.ar/api/asset/...` (curl -I, 3 pedidos consecutivos) y contra la Management API de Supabase (listar proyectos, organizacion, plan). No se toco codigo de producto en este turno, solo se agrego documentacion.
- Auditoria: N/A para el codigo (solo lectura/investigacion); se reviso el historial git de los 2 archivos del proxy para confirmar que no son codigo abandonado (agregados 2026-07-04, con comentarios explicando el mismo diagnostico).
- Pendiente/Riesgos: Hueco conocido y de bajo impacto: `components/DesignThumb.js` (solo panel de Admin, staff) sigue usando `<img>` directo con URL cruda, sin pasar por el proxy — no se toco por ser trafico marginal, queda anotado en `IMAGE_ASSET_CACHING.md` por si se quiere prolijidad total a futuro. La confirmacion de "Vercel-Cache: HIT" fue desde una sola ubicacion/PoP de testing — el cache de Vercel es por region, asi que el primer visitante de cada region geografica igual paga el costo una vez (comportamiento normal de cualquier CDN, no una falla).

## 2026-07-09 11:30 -03:00 - Claude Sonnet 5

- Objetivo: El usuario mandó una captura de la pestaña Diseños mostrando la pastilla que indica el peso original/optimizado de la miniatura ("159 KB->...") cortada, sin llegar a mostrar el peso optimizado.
- Cambios: `app/admin/page.js`, `pillStyle` dentro del render de esa pastilla (fila de diseños): `width:76` → `width:112`. Con `whiteSpace:'nowrap'` + `textOverflow:'ellipsis'`, el texto completo (`"<original> KB-><optimizado> KB"`, ej. "159 KB->109 KB") no entraba en 76px y se cortaba con "…"; 112px cubre con margen los casos de 4 cifras (ej. "1.234 KB->987 KB").
- Verificacion: `CI=true npm run build` OK sin errores nuevos. Cambio de una sola línea (un valor de ancho), sin tocar la lógica de cálculo de tamaños.
- Auditoria: N/A, cambio aislado de estilo.
- Pendiente/Riesgos: No se pudo confirmar visualmente en un navegador real (mismo límite del entorno); a confirmar por el usuario que ahora se ve completo.

## 2026-07-09 11:20 -03:00 - Claude Sonnet 5

- Objetivo: El usuario reportó que el fix de refresh forzado de este mismo día (agregar `self:true` al canal que escucha) NO alcanzó: "cualquiera de los botones... no pasa nada, tampoco una animación del botón ni nada" — es decir, ni siquiera aparecía el "✓ Enviado"/"Error al enviar" que ya existía en el código.
- Cambios: Causa raíz real encontrada leyendo el código fuente de `@supabase/realtime-js` (no por prueba y error): `RealtimeClient.channel(topic)` **reusa el canal existente si ya hay uno con el mismo topic** en vez de crear uno nuevo (confirmado en `node_modules/@supabase/realtime-js/dist/main/RealtimeClient.js`). En `/admin` ya existe un canal escuchando en ese topic (el listener agregado en un turno anterior) — así que `broadcastForceRefresh` (`lib/force-refresh.js`), al llamar `supabase.channel(FORCE_REFRESH_CHANNEL)`, recibía ESE MISMO canal (no uno propio), y después llamaba `.subscribe(callback)` sobre él. `RealtimeChannel.subscribe()` (`RealtimeChannel.js`) envuelve TODA su lógica en `if (this.channelAdapter.isClosed())` — si el canal ya está "joined" (que es el caso, porque el listener ya lo suscribió antes), ese bloque completo se salta y el callback **nunca se invoca, ni con éxito ni con error** — la Promise quedaba colgada para siempre, por eso "no pasaba nada" (ni el ✓ ni el error). Además, el código viejo hacía `supabase.removeChannel(channel)` después de mandar, que — al ser el MISMO canal que el listener — lo hubiera desconectado del broadcast para el resto de la sesión en el primer envío exitoso (un segundo bug encima del primero). Se reescribió `broadcastForceRefresh` para NO volver a suscribirse: solo llama `channel.send(...)` directo sobre el canal (ya sea el existente y ya conectado, o uno nuevo que Supabase manda automáticamente por REST si todavía no está conectado por websocket — comportamiento propio de `RealtimeChannel.send()` para mensajes tipo broadcast), y ya no hace `removeChannel`.
- Verificacion: `CI=true npm run build` OK sin errores nuevos. El diagnóstico es por lectura directa del código fuente de la librería (no hay forma de levantar dos sesiones de navegador con login de Google real en este entorno para probarlo en vivo de punta a punta), pero la causa está confirmada línea por línea, no es una hipótesis.
- Auditoria: Se releyó la entrada de las 09:05 del mismo día (el fix anterior de `self:true`) — ese fix sigue siendo necesario y correcto (sin él, aunque el envío ahora funcione, la propia pestaña del admin no reaccionaría a su propio broadcast), simplemente no era suficiente por este segundo bug independiente.
- Pendiente/Riesgos: Sigue sin poder probarse en vivo con dos sesiones reales en este entorno. Falta confirmación del usuario en producción.

## 2026-07-09 10:55 -03:00 - Claude Sonnet 5

- Objetivo: Ajuste de UX sobre la feature recien implementada (diseños extra en un pedido): el usuario pidió que el botón "+ Agregar diseño", tanto en "Ver pedido" como en Producción, no sea un botón aparte arriba de la tabla, sino la ÚLTIMA FILA de la tabla de items/tareas.
- Cambios: En `app/admin/page.js` (modal "Ver pedido") y `components/ProductionTab.js` (tabla de tareas de Producción), se sacó el botón + panel que estaban por fuera de la tabla, y se agregó una `<tr>` final dentro del `<tbody>` (con `colSpan` a todas las columnas) que muestra el botón "+ Agregar diseño" (estilo borde punteado, como una fila de "agregar") o, al hacer click, el mismo `AddExtraDesignForm` de siempre pero ocupando esa fila. No se tocó la lógica de datos ni el resto del diseño (resaltado por vista, tooltip, desglose del resumen) — solo dónde vive el disparador.
- Verificacion: `CI=true npm run build` OK sin errores nuevos. Diff revisado, acotado a mover el botón/panel de lugar en ambos archivos.
- Auditoria: Se releyó la entrada anterior (mismo día, implementación completa de la feature) para tocar solo la UI del disparador sin afectar la RPC ni el modelo de datos ya aplicado.
- Pendiente/Riesgos: Mismo límite de siempre — no se pudo ver en un navegador real. Falta confirmación visual del usuario en producción.

## 2026-07-09 10:40 -03:00 - Claude Sonnet 5

- Objetivo: Implementar la feature discutida (y explicitamente pausada) en el turno anterior: permitir agregar un diseño extra a un pedido ya existente, desde "Ver pedido" (admin) y desde Producción (admin y operario), con el desglose "100 + 10" y un tooltip de auditoría (quién y cuándo). El usuario confirmó el diseño completo en 4 puntos de esta conversación (resaltado solo en la vista donde se agregó — fusionado en la otra; permisos via vistas existentes, sin flag nuevo; el desglose "+" solo en el resumen de arriba, la columna por fila queda normal; agregó de paso el pedido del tooltip "i" hover) y dio el visto bueno explícito a implementar todo.
- Cambios:
  **SQL** (`sql/order_extra_designs.sql`, aplicado en producción): 5 columnas nuevas en `production_order_tasks` (`added_qty`, `added_via`, `added_by_email`, `added_by_name`, `added_at`) — se decidió NO usar una tabla de historial separada, ver `EXTRA_DESIGNS_FEATURE.md` para el razonamiento. Nueva función `add_order_extra_design(p_order_id, p_design_id, p_qty, p_added_via)`, SECURITY DEFINER, autorizada para `is_admin()` o cualquier operario activo (sin exigir que el operario ya esté asignado a ese pedido puntual — la restricción real es de vistas, no de RPC, tal como pidió el usuario). Agrega el item a `orders.items` (con los metadatos embebidos, para que "Ver pedido" los lea sin joins) y hace upsert en `production_order_tasks` (suma a la fila existente si el diseño ya estaba en el pedido, crea una nueva si no). `get_operator_production_tasks()` extendida para exponer las columnas nuevas.
  **Bug preexistente encontrado y corregido de paso**: al comparar la función live contra el historial de archivos SQL, se encontró que `sql/production_order_tasks_position.sql` (turno anterior, fix de orden del carrito) había pisado sin querer la protección de `is_manual_link` (agregada por `sql/fix_manual_link_persistence.sql`, anterior a esa migración) en `admin_sync_order_production_tasks()` (el DELETE ya no excluía filas `is_manual_link`) y en `get_operator_production_tasks()` (columna ya no expuesta). Verificado con `SELECT count(*) WHERE is_manual_link=true` → 0 filas afectadas hoy, pero se restauró la protección ya que se estaban reescribiendo esas mismas funciones.
  **Cliente**: `components/InfoTooltip.js` (nuevo, ícono "i" hover genérico) y `components/AddExtraDesignForm.js` (nuevo, mini-formulario compartido: fuzzy search sobre `designs` vía `useDesigns()` + stepper de cantidad, mismo criterio que el picker de "Nuevo pedido"). En `app/admin/page.js`: botón "+ Agregar diseño" en el modal "Ver pedido", fila con fondo verde + tooltip cuando `item.added_via === 'pedido'`. En `components/ProductionTab.js`: botón "+ Agregar diseño" en el encabezado del pedido (visible para admin y operario, sin gate de rol), tooltip por fila cuando `task.added_via === 'produccion'`, y el resumen "A producir" ahora muestra `base + agregado` cuando hay adiciones vía Producción (el resto de las tarjetas del resumen y la columna "a producir" de cada fila quedan exactamente igual que antes).
- Verificacion: Antes de aplicar nada, se corrió la migración completa dentro de una transacción de prueba (`node` + `pg` directo contra `SUPABASE_DB_URL`) que crea 2 pedidos descartables, ejercita la RPC real (simulando JWT de admin y de operario reales vía `SET LOCAL request.jwt.claims`) con 7 escenarios: alta de diseño nuevo, merge de una segunda adición sobre el mismo diseño (confirma que suma a la MISMA fila, no duplica), alta por operario no-admin, rechazo de usuario ajeno ("No autorizado"), rechazo de cantidad/origen inválido, `get_operator_production_tasks()` devolviendo las columnas nuevas, y sobrevivencia de una fila `is_manual_link` a un resync (el bug corregido). Los 2 pedidos de prueba se borraron (cascada a sus tareas) DENTRO de la misma transacción antes del `COMMIT`, así que no quedó ningún dato de prueba en producción. `CI=true npm run build` OK sin errores nuevos (el único warning de build, en `QuickPrintOverlay.js`, es preexistente y no tocado este turno). Se releyó el diff completo de los 2 archivos modificados.
- Auditoria: Se releyeron las últimas entradas del mismo día (sticky header + fix de refresh, y el fix de subpestañas) para confirmar que no se pisaba nada — ninguna toca las mismas funciones SQL ni los mismos componentes.
- Pendiente/Riesgos: No se pudo probar en un navegador real con login de Google (mismo límite de siempre en este entorno) — la verificación fue a nivel SQL directo (incluyendo el auth branch, simulado) más lectura cuidadosa del JSX. Falta que el usuario confirme en producción: (1) que el botón aparece donde espera en ambas vistas, (2) que el desglose "100 + 10" se ve correcto con datos reales, (3) que el tooltip se ve bien posicionado dentro de la tabla angosta de Producción (no se probó en pantalla chica/mobile). Si en el futuro se necesita un historial completo de múltiples adiciones (no solo "la última"), hay que migrar a una tabla aparte, ver nota en `EXTRA_DESIGNS_FEATURE.md`.

## 2026-07-09 09:05 -03:00 - Claude Sonnet 5

- Objetivo: Dos reportes del usuario sobre el panel de admin: (1) pidio que la barra superior (header con logo/"Panel de Administración"/version/logout) quede siempre fija al hacer scroll; (2) reporto que el boton de forzar refresh a usuarios (Configuración > Actualizar usuarios) "no hace nada" al apretarlo.
- Cambios: **(1) Header fijo**: `styles.header` pasa a `position:'sticky', top:0, zIndex:300` (antes no tenia `position`, se iba con el scroll). La barra de pestañas (`styles.tabBar`) ya era `sticky` pero en el JSX real se pisaba con un override inline a `position:'relative'` (probablemente agregado sin querer al sumar los botones de scroll `‹›`, que en realidad no necesitaban ese override porque `sticky` tambien crea contexto de posicionamiento para hijos `absolute`) — se saco el override, y su `top` paso de `0` a `46` (alto del header) para que quede pegada justo debajo en vez de tapada por el header. Se ajusto tambien el banner de "Simulando como" (impersonación), que era `sticky top:0`, a `top:46` por la misma razon. Los estilos dark-mode heredan todo esto via spread (`...styles.header`), no hizo falta tocarlos aparte.
  **(2) Boton de refresh roto**: causa raiz encontrada en el listener de `app/admin/page.js` (el que reacciona al broadcast con `audience==='admin'`): se suscribe al mismo canal Realtime (`inkora-force-refresh`) que usa `broadcastForceRefresh` para enviar, y ambos comparten la MISMA conexion (un solo cliente `supabase` por pestaña, ver `lib/supabase.js`) — por default, Supabase Realtime Broadcast NO entrega a un socket los mensajes que el mismo socket envio, salvo que el canal que escucha se cree con `config:{broadcast:{self:true}}`. Como el usuario probo el boton con una sola pestaña propia de `/admin` abierta, nunca podia ver el efecto en si mismo aunque el broadcast si habia salido bien (por eso "aprieto y no pasa nada"). Se agrego `self:true` a la creacion del canal que escucha en `page.js`. El listener de `MaintenanceGate.js` (audience `site`) no tiene este problema: nunca puede ser la misma pestaña que envia (el que envia esta en `/admin`, que esta en `EXEMPT_PREFIXES` y jamas monta ese listener), asi que no se toco.
- Verificacion: `CI=true npm run build` OK sin errores nuevos. Se releyo el diff completo (`git diff -- app/admin/page.js`), acotado a los 4 puntos de arriba. Se revisaron a mano todos los `position:'sticky'`/`fixed'` restantes del archivo para descartar que el nuevo `zIndex:300` del header tapara algo que antes quedaba por encima (los unicos candidatos, el FAB de chat y su lista de mensajes, usan `bottom:` no `top:`, sin conflicto).
- Auditoria: Se releyo la entrada anterior (08:18, fix de subpestañas) para confirmar que no se pisaba nada de ese cambio; ninguno de los dos fixes de este turno toca `resolveTabVisible`/`resolveSubtabVisible`.
- Pendiente/Riesgos: No se pudo probar en vivo con dos pestañas/sesiones reales (una que envia, otra que recibe) por falta de login Google en este entorno — el diagnostico del bug de `self:true` es por lectura de codigo y es un comportamiento documentado de Supabase Realtime, no una prueba end-to-end propia. Falta confirmacion real del usuario en produccion para ambos puntos.

## 2026-07-09 08:18 -03:00 - Claude Sonnet 5

- Objetivo: El usuario probo asignarle a un operario real solo la subpestaña "Producir" dentro de Producción, pero seguia viendo TODAS las subpestañas (Pedidos, Stock, Historial, Operarios). Pidio primero solo investigar (sin tocar codigo), despues confirmo implementar el fix.
- Cambios: Causa raiz confirmada en `resolveSubtabVisible` (`app/admin/page.js`): una subpestaña sin configuracion propia siempre resolvia a `true` (visible) como default, pensado originalmente como "hereda el acceso de la pestaña padre si no la tocaste". El problema real: en cuanto se configura UNA subpestaña puntual (ej. "Producir"), las demas (que nunca se tocaron) seguian con ese mismo default visible, quedando abiertas por accidente. Se cambio el default para que dependa de si ESA pestaña ya tiene alguna subpestaña configurada (para cualquier usuario, no solo el actual): si ninguna subpestaña de "Producción" fue configurada nunca, todas siguen heredando el acceso de la pestaña (modo simple, sin cambios de comportamiento); pero en cuanto se configura una sola, el resto pasa a default oculto hasta configurarse explicitamente ("modo granular"). Se actualizo tambien `tabVisibilitySummary()` para que el resumen en la UI diga "Nadie (sin configurar)" en vez del enganoso "Sin restriccion propia" una vez que la pestaña entro en modo granular.
- Verificacion: `CI=true npm run build` OK. Se trazo a mano el escenario real reportado (operario con `production.users` configurado + `production.subtabs.produce.users` configurado, sin tocar las demas subpestañas) contra la nueva logica: "Producir" resuelve visible (tiene su propia entrada), "Pedidos"/"Stock"/"Historial"/"Operarios" resuelven ocultas (sin entrada propia, pero la pestaña ya esta en modo granular por culpa de "Producir") — coincide con el comportamiento esperado. Se releyo el diff completo, acotado y limpio.
- Auditoria: Se releyo la entrada anterior del mismo dia (21:42, la unificacion de Usuarios) para confirmar que este bug era independiente de esos cambios (el resolver de subpestañas no se habia tocado en ese turno, solo el de pestañas de nivel superior).
- Pendiente/Riesgos: Falta la confirmacion real del usuario con el operario ya asignado (deberia dejar de ver Pedidos/Stock/Historial/Operarios sin tener que re-configurar nada, ya que el fix es sobre la resolucion, no sobre los datos guardados). Si en el futuro se quiere que una pestaña con subpestañas nunca entre en "modo granular" por accidente (ej. un admin tildando y destildando sin querer), no hay proteccion extra mas alla de la propia UI.

## 2026-07-08 21:42 -03:00 - Claude Sonnet 5

- Objetivo: El usuario probo con un operario real recien migrado a `/admin` (turno de hoy) y reporto 4 cosas: (1) al entrar como operario aparecia el error "no se pudo preparar este pedido para producir" (con admin no pasaba); (2) la columna de pedidos en "Producir" deberia ir de mas nuevo a mas viejo; (3) el operario veia TODOS los pedidos aunque no estuvieran asignados a el, sospechando que la asignacion no aplicaba en `/admin`; (4) unificar las subpestañas Admins+Operarios de Usuarios en una sola, con selector de rol (si es admin, ve todo siempre; si es operario, aplican las restricciones).
- Cambios: **(1) Causa raiz del error**: `admin_sync_order_production_tasks` (la RPC que "prepara" un pedido materializando sus items como tareas) exige `is_admin()` server-side; `ProductionTab.js` la disparaba automaticamente al seleccionar cualquier pedido, sin mirar el rol de quien la llamaba — un operario la disparaba igual, el servidor la rechazaba, y el mensaje generico confundia. Se agrego prop `viewerRole` a `ProductionTab` (pasado desde `page.js`) y `syncOrderTasks()` ahora no hace nada si `viewerRole !== 'admin'` (no hay nada que preparar para un operario, sus pedidos ya deberian llegar preparados/asignados). Se ocultó ademas, solo para operarios, el selector "asignar operario" del encabezado del pedido (esa RPC tambien es admin-only).
  **(2) Orden de pedidos**: `produceOrderRows` ahora se ordena por `created_at` descendente antes de renderizarse (antes quedaba en el orden que traia `orders`, sin criterio explicito).
  **(3) Causa raiz de "ve todos los pedidos"**: confirmado el diagnostico del usuario, aunque no era un problema de seguridad real (`update_production_task_progress`/`increment_production_task_counter` YA verificaban server-side que el operario fuera el asignado a esa tarea especifica, rechazando con "No autorizado" si no) — era un bug de UI: `produceOrderRows` armaba la lista a partir de `orders` completo (todos los pedidos cargados, sin filtrar), y para cualquier pedido sin tareas asignadas a ese operario caia al fallback `getOrderProductionItems(order)`, que arma filas directo desde `order.items` sin mirar asignacion. Ahora esa lista se filtra primero: si `viewerRole !== 'admin'`, solo entran los pedidos donde `tasksByOrder[order.id]` (ya filtrado server-side por `get_operator_production_tasks`) tiene al menos una tarea.
  **(4) Unificacion Admins+Operarios**: se evaluo fusionar directamente las tablas `admins`/`production_operators` en una sola, pero se descarto por el riesgo: `is_admin()` (la funcion que sostiene practicamente todas las policies RLS del proyecto) esta atada a la tabla `admins` tal cual existe hoy, y tocar eso es un cambio de altisimo riesgo para algo que se puede resolver mejor en la capa de UI. En cambio: la subpestaña "Operarios" de Usuarios desaparece, la subpestaña "Admins" pasa a llamarse "Equipo" y ahora muestra una lista unica combinando `admins`+`production_operators`, cada fila con un `<select>` para cambiar el rol (Admin/Operario). Cambiar el rol mueve a la persona entre las dos tablas reales (nueva funcion `setStaffRole`): a Admin → insert en `admins` + marca el registro de operario `active:false` (no se borra, para no perder el historial de tareas ya asignadas); a Operario → reactiva el registro existente o crea uno nuevo via el mismo endpoint `/api/admin/operators` ya existente, y borra de `admins`. Nuevo formulario unico "Agregar admin u operario" (`addStaffMember`) con selector de rol, reemplaza los dos formularios separados que existian. **Cambio de semantica ligado a esto**: ahora los admins SIEMPRE ven todas las pestañas/subpestañas sin excepcion (`resolveTabVisible`/`resolveSubtabVisible` retornan `true` de una si `viewerRole==='admin'`, sin mirar la config de "Pestañas y permisos") — las restricciones de esa pantalla (armada mas temprano hoy) pasan a aplicar solo a operarios. Esto tambien vuelve innecesario el aviso de auto-bloqueo que se habia agregado hoy mismo para Configuración (un admin ya no puede excluirse a si mismo de nada), asi que se saco ese `window.confirm` y el picker de esa pantalla ahora solo lista operarios (elegir un admin ahi ya no tenia efecto real).
- Verificacion: `CI=true npm run build` OK sin errores ni warnings nuevos. Se releyo el diff completo de este turno; se encontro y corrigio un detalle antes de darlo por terminado: el mapeo de slugs de URL vieja (`SLUG_USERS_SUBTABS`) todavia apuntaba `?vista=operarios` a la subpestaña `operators` ya eliminada, lo que hubiera dejado un bookmark viejo mostrando Usuarios en blanco; se corrigio para que apunte a `admins` (la subpestaña unificada). Se confirmo con grep que `renderOperatorsPanel`/`createOperator`/`operatorForm`/`toggleOperator` siguen intactos y en uso real (los sigue usando la subpestaña "Operarios" DENTRO de Producción, que es una pantalla distinta y no se toco). Se levanto el servidor de desarrollo y se confirmo `/admin` compilando y respondiendo `200`.
- Auditoria: Se releyo la entrada anterior del mismo dia (20:21) para confirmar que el pedido de hoy sobre operarios en `/admin` seguia fresco en memoria antes de diagnosticar estos bugs nuevos, todos relacionados a esa misma migracion.
- Pendiente/Riesgos: Sigue pendiente la prueba real con el operario en un navegador (no se pudo repetir aca por falta de login Google real) — recomendado que el usuario vuelva a probar con esa misma cuenta ahora que estan los 3 fixes de Producción. El cambio de rol (`setStaffRole`) no se probo en vivo con datos reales por el mismo motivo, aunque reusa exactamente los mismos mecanismos (`supabase.from('admins')...`, `/api/admin/operators`, `production_operators.active`) que ya estaban probados por separado en sus flujos originales. Si en el futuro se quiere permitir que un operario tenga tareas de DOS "identidades" (ej. paso de operario a admin y volvio a operario mas de una vez), el registro de `production_operators` que se reactiva es siempre el mismo (por email), asi que el historial de tareas viejas asignadas a ese `operator_id` se mantiene correctamente enlazado.

## 2026-07-08 20:21 -03:00 - Claude Sonnet 5

- Objetivo: Dos pedidos nuevos del usuario: (1) en Configuración, un boton para forzar un refresh instantaneo (no modo mantenimiento) a los usuarios del sitio normal, y por separado a los del panel de admin (a todos o a un usuario puntual); (2) cambiar el indicador de version del header de admin (mostraba un hash de git tipo "vcd9db2e") a un formato tipo "1.10.0", que muestre ademas la hora de esa version, y que si el navegador tiene cargada una version vieja lo indique de forma sutil con un boton "Actualizar" (solo en admin, al lado de "Panel de Administración", que simplemente hace refresh).
- Cambios: **Refresh forzado en vivo** (nuevo `lib/force-refresh.js`): usa Supabase Realtime Broadcast (no una tabla ni polling) en un canal fijo `inkora-force-refresh`; el emisor (`broadcastForceRefresh`) se suscribe, manda `{audience, target}` y se desuscribe. Se agregaron listeners: en `components/MaintenanceGate.js` (para pestañas NO admin/operarios/producción, ya que ese es justo el mismo criterio "exempt" que ese componente ya usaba) que recarga si `audience === 'site'`; y en `app/admin/page.js` que recarga si `audience === 'admin'` y (`target === 'all'` o `target` coincide con el email del usuario logueado). Nueva tarjeta "Actualizar usuarios" en Configuración con 3 acciones: **Sitio** (a todos), **Panel de admin** (a todos), y **Usuario puntual** (selector reutilizando la misma lista de admins+operarios ya armada para permisos de pestañas). No pone en mantenimiento ni bloquea nada, solo dispara el broadcast; si nadie tiene la pagina abierta en ese momento, no le llega a nadie (no hay cola/reintento, es deliberado: para eso ya esta el aviso pasivo de version nueva).
  **Version del header**: `next.config.mjs` ahora genera `NEXT_PUBLIC_APP_VERSION` como `1.0.<cantidad de commits>` (en vez del hash corto de git) para que se lea como una version real sin que nadie tenga que bumpearla a mano; el hash real se guarda aparte en `NEXT_PUBLIC_APP_COMMIT` (visible en el tooltip) y se agrego `NEXT_PUBLIC_APP_BUILD_TIME` (hora del build). Nuevo endpoint `app/api/version/route.js` (`dynamic:'force-dynamic'`, `Cache-Control:no-store`) que siempre refleja lo que esta realmente desplegado en ese momento en el servidor. Nuevo componente `AdminVersionBadge` en el header: muestra "v1.0.<n> · dd/mm hh:mm", y cada 60s consulta `/api/version`; si la version del servidor no coincide con la que el navegador ya tiene cargada, aparece un boton chico "Nueva versión · Actualizar" (estilo sutil, translucido) que solo hace `window.location.reload()`. Se movio de antes de "Panel de Administración" a despues, como se pidio.
- Verificacion: `CI=true npm run build` OK sin errores ni warnings nuevos (el nuevo `/api/version` aparece como ruta dinamica `ƒ`, confirmando que no quedo cacheada estaticamente). Se probo en el servidor de desarrollo real: `GET /api/version` devolvio `{"version":"1.0.706","commit":"aa71b92","buildTime":"...`, confirmando que la cadena completa funciona de punta a punta *localmente*. **Bug real encontrado al verificar en produccion**: `/api/version` en `inkora.com.ar` devolvio `"1.0.10"` en vez de ~707 — Vercel clona el repo en modo shallow para el build, asi que `git rev-list --count HEAD` contaba solo los commits disponibles ahi. Primer intento de arreglo: detectar shallow (`git rev-parse --is-shallow-repository`) y pedir el historial completo (`git fetch --unshallow`) antes de contar; se reprodujo el bug a proposito clonando shallow en este entorno (`git clone --depth=10 file://...`) y se confirmo que el fetch SI arregla el conteo localmente (10 -> 707) — pero al pushear y probar en el `/api/version` real de produccion, **siguio devolviendo "1.0.10"**: el `fetch --unshallow` debe fallar silenciosamente en el sandbox de build real de Vercel (probablemente sin credenciales del remoto disponibles para un comando git arbitrario fuera del clon inicial que hace la propia integracion de Vercel), algo que no se pudo reproducir ni depurar mas sin acceso a los logs de build de Vercel. Se abandono depender de git para el numero de version: ahora `1.<dias>.<minutos del dia>` se deriva solo de la hora de build (`Date.now()` en el momento del build, contra un epoch fijo 2026-01-01), sin ningun comando git de por medio — ya no puede fallar por shallow clone. El hash de commit (`NEXT_PUBLIC_APP_COMMIT`, solo visible en el tooltip) sigue usando `git rev-parse --short HEAD`, que a diferencia del conteo si funciono bien en produccion las dos veces (un shallow clone siempre incluye el commit punta, aunque no sus ancestros).
  **Confirmado en produccion real tras el segundo fix**: `GET https://www.inkora.com.ar/api/version` devolvio `{"version":"1.188.1413","commit":"f3d3498",...}` — numero correcto y coincidiendo con el commit recien pusheado, en el deploy real (no local). **Lo que no se pudo probar en este entorno**: el broadcast en si (hace falta dos sesiones de navegador reales con login de Google, que no estan disponibles aca) — se revisó el codigo con cuidado en su lugar (mismo patron ya usado en otras partes del proyecto para Realtime).
- Auditoria: Se releyo la entrada anterior del mismo dia (18:32) para no pisar el trabajo de orden de carrito recien cerrado.
- Pendiente/Riesgos: **Seguridad**: el canal de broadcast es publico (cualquiera con la anon key podria en teoria mandarle un mensaje y forzar un reload a otros visitantes) — el boton para dispararlo desde la UI ya esta protegido porque vive detras del login de admin, pero alguien tecnico podria mandarlo igual desde afuera de la app. El impacto es bajo (fuerza un reload, no expone ni corrompe datos), pero si se quiere blindar del todo habria que pasar a "Realtime Authorization" (canales privados con policy RLS), que no se armo en este turno por alcance. El alcance de "admin" quedo limitado a `/admin` exclusivamente (no incluye `/operarios` ni `/produccion`, que el usuario no menciono); si tambien se quiere ahi, es un agregado chico. Falta la prueba real con dos sesiones (una disparando el refresh, otra recibiendolo) para confirmar el comportamiento en vivo.

## 2026-07-08 18:32 -03:00 - Claude Sonnet 5

- Objetivo: Dos pedidos del usuario mostrando capturas de la tabla de detalle de pedido en Producción: (1) el orden de los diseños en esa tabla (y en "el pedido") aparecía alfabético en vez de respetar el orden real en que se seleccionaron en el carrito — aclaró explícitamente que "impresión rápida" ya está bien (numérico) y no tocarlo; (2) que el número de "Impreso" y de "Troquelado" cambie a texto verde cuando ya alcanzó o superó la cantidad requerida.
- Cambios: **Punto 2 (color verde)** en `components/ProductionTab.js`: `StockCell` ahora recibe una prop `requiredQty` opcional; cuando el valor actual (el que se está mostrando, no solo el ya guardado, para que cambie de color al instante mientras se edita) es `>= requiredQty`, el texto del input pasa a verde (`#18a36a`, el mismo verde ya usado en el resto del panel). Se pasó `requiredQty={task.required_qty}` únicamente en las dos celdas que correspondían (Impreso/`printed_qty` y Troquelado/`produced_qty`); Desperdicio no lo recibe, sigue con su color de siempre.
  **Punto 1 (orden alfabético), causa raíz real encontrada**: no era un bug de `ProductionTab.js` sino de la funcion SQL `get_operator_production_tasks()` en Supabase, que devuelve las tareas de producción con `ORDER BY ... product_name, design_name` (alfabético) porque `production_order_tasks` nunca guardó la posición original del item dentro del carrito. Se creó `sql/production_order_tasks_position.sql`: agrega la columna `sort_order` a `production_order_tasks` (se descartó llamarla "position" porque es palabra reservada de SQL y rompía el parser), recalcula `admin_sync_order_production_tasks()` y `admin_assign_order_operator()` para que la completen a partir de `jsonb_array_elements(...) WITH ORDINALITY` sobre `orders.items` (o sea, el índice real del item tal como se agregó al carrito), cambia el `ORDER BY` final de las 3 funciones para que usen `sort_order` en vez de nombre, y hace un backfill para los pedidos ya existentes re-derivando `sort_order` desde el `items` guardado en cada pedido. **Esta migración ya se aplicó directamente a la base de producción** (se tenía acceso directo via `SUPABASE_DB_URL` + el modulo `pg` ya vendorizado en `tools/backup-app/node_modules`, corrida dentro de una transacción con rollback automático si algo fallaba). No hizo falta ningún cambio de código en `ProductionTab.js` para este punto: el cliente ya preserva fielmente el orden que le llega de la RPC (`tasksByOrder` solo hace `push`, sin re-ordenar), asi que arreglar el `ORDER BY` en el servidor alcanzó. Se confirmó explícitamente que no se tocó `QuickPrintOverlay.js` (impresión rápida), que ya ordena numéricamente por el prefijo del nombre de archivo y el usuario pidió dejar intacto.
- Verificacion: Antes de tocar nada real, se corrió una consulta de solo lectura contra la base de producción reconstruyendo la lógica de backfill como `SELECT` (sin `UPDATE`) para confirmar en datos reales que (a) el pedido más grande (102 diseños distintos) queda en un orden genuinamente no-alfabético coincidente con lo esperado, y (b) 0 filas quedarían sin poder emparejarse a un `sort_order`. Recién con eso confirmado se aplicó la migración real dentro de una transacción (`BEGIN`/`COMMIT`, con `ROLLBACK` automático ante cualquier error — de hecho el primer intento falló por el conflicto de la palabra reservada "position" y se revirtió solo, sin dejar nada a medio aplicar). Se volvió a verificar después: `0` filas con `sort_order` nulo, y el mismo pedido de prueba devuelve el orden esperado. `CI=true npm run build` OK sin errores ni warnings nuevos para el cambio de color.
- Auditoria: Se releyeron las dos entradas anteriores del mismo día (18:00 y 18:16) para confirmar que no dejaban nada a medias antes de empezar con este pedido nuevo.
- Pendiente/Riesgos: **No se encontró una segunda ubicación separada** mostrando los items de "el pedido" en orden alfabético (se buscó en toda la vista de Pedidos/Estadísticas de `page.js` sin encontrar un `.sort()` alfabético aplicado a `order.items` fuera de la ya corregida); es posible que "el pedido" y "producción" en el pedido del usuario se refirieran a esta misma tabla vista de dos formas, pero si el usuario todavía ve alfabético en algún otro lugar puntual después de este fix, hay que pedirle que indique exactamente cuál pantalla es. El backfill fue determinístico y verificado (0 filas huérfanas), pero si en el futuro un pedido tiene un `items` malformado o vacío que no matchea ningún `production_order_tasks` existente, esa fila quedaría con `sort_order` NULL y caería al fallback alfabético de siempre (`NULLS LAST, product_name, design_name`) — no rompe nada, solo no se beneficia del fix hasta que se re-sincronice.

## 2026-07-08 18:16 -03:00 - Claude Sonnet 5

- Objetivo: Implementar el punto 5 pendiente del turno anterior: columnas redimensionables (arrastrar el borde, como en Excel) en la tabla de detalle de pedido de Produccion ("Producir" > pedido seleccionado), guardando el ancho como preferencia del admin/operario logueado (persiste entre sesiones y dispositivos). El usuario pidio ademas, si correspondia, crear un archivo aparte para preferencias de usuario pensando en futuras tablas con esta misma necesidad.
- Cambios: **Nuevo archivo `lib/admin-preferences.js`**: centraliza `adminPreferenceKey(baseKey, email)` (arma una key `settings` sufijada por email, misma logica que ya existia inline en `page.js` para el filtro individual de vendedor) mas `parseColumnWidths(value, defaults)` y `clampColumnWidth(value, min, max)`, para que cualquier tabla nueva que sume anchos de columna (u otra preferencia por-usuario) reutilice esto en vez de repetir la logica. Se migro `app/admin/page.js` para importar `adminPreferenceKey` de ahi y se borro la funcion local duplicada (el unico uso existente, el filtro de vendedor en Escalas, ahora pasa `currentUser` explicito en vez de leerlo de un closure).
  **Columnas redimensionables** en `components/ProductionTab.js`: la tabla de tareas del pedido seleccionado (Producto/Diseño/A producir/Impreso/Troquelado/Desperdicio/Observaciones/Imprimir) paso de anchos fijos hardcodeados en el `<colgroup>` (mas una columna "Diseño" sin ancho propio que absorbia el espacio sobrante hasta completar 100%) a un estado `columnWidths` con anchos individuales para las 8 columnas (incluida Diseño, que ahora tiene un ancho de base propio de 280px en vez de "lo que sobre"). Cada `<th>` tiene un agarre invisible de 7px en el borde derecho (mouse y touch) que al arrastrar cambia el ancho en vivo (clamp 50-480px) y al soltar dispara el guardado. El ancho total de la tabla ahora es la suma real de las columnas (con un piso de 860/880px como antes) en vez de forzarse a 100% del contenedor.
  **Persistencia**: nueva prop `orderDetailColumnWidths` (valor crudo ya guardado, si existe) y `onSaveOrderDetailColumnWidths` (callback) en `ProductionTab`; `app/admin/page.js` arma la key real con `adminPreferenceKey(PRODUCTION_ORDER_DETAIL_WIDTHS_PREF_KEY, currentUser)` (constante exportada desde `ProductionTab.js`) y reusa el `saveSetting()`/`settings` ya existentes — mismo mecanismo `settings` con key por-email que ya usaba el filtro de vendedor, sin tabla nueva ni mecanismo nuevo de guardado.
- Verificacion: `CI=true npm run build` OK, sin errores ni warnings nuevos en los 3 archivos tocados (`lib/admin-preferences.js`, `components/ProductionTab.js`, `app/admin/page.js`) — solo los 2 warnings preexistentes de `<img>`. Se releyo el diff completo linea por linea; se detecto y corrigio en el camino un default demasiado angosto para la columna "Diseño" (200px, se subio a 280px para no achicarla de golpe respecto a como se veia antes en pantallas anchas) y se agrego `preventDefault()` en el `touchmove` para que no scrollee la pagina mientras se arrastra un borde en mobile/tablet. Se levanto el servidor de desarrollo y se confirmo `/admin` compilando y respondiendo `200` sin errores.
- Auditoria: Se releyo la entrada anterior (mismo dia, 18:00) para retomar exactamente donde quedo (esa entrada ya dejaba investigado que `adminPreferenceKey` existia inline en `page.js`, este turno lo extrajo a un archivo compartido tal como se le pidio).
- Pendiente/Riesgos: **Misma limitacion de siempre**: no se pudo arrastrar un borde de columna en un navegador real en este entorno (sin cuenta Google real ni Playwright/chromium-cli instalado) — recomendado que el usuario lo prueble en persona, especialmente en mobile/touch. El ancho de columna es una preferencia pura de UI (no afecta datos ni permisos); si algun dia se quiere el mismo resize en la OTRA tabla de Produccion (la de Stock/reporte, con su propio `<colgroup>` en la linea ~2654, no tocada en este turno) se puede reusar `parseColumnWidths`/`clampColumnWidth` del nuevo `lib/admin-preferences.js` sin duplicar logica.

## 2026-07-08 18:00 -03:00 - Claude Sonnet 5

- Objetivo: Ajustes pedidos por el usuario sobre la funcion de permisos de pestanas del turno anterior: (1) fusionar "Orden de pestanas" y "Permisos de pestanas" en una sola seccion/fila por pestana; (2) permitir reordenar tambien las subpestanas (drag), que el orden personalizado se refleje al entrar a la pestana real, y confirmar que esto (igual que el orden de pestanas) es global para todos los usuarios, no una preferencia individual; (3) permitir restringir tambien la visibilidad de la pestana "Configuracion" (antes quedaba hardcodeada siempre-visible); (4) permitir seleccionar varias pestanas para reordenar en bulk tambien (no solo asignar usuarios), y sacar el texto "N seleccionadas" del bulk bar; (5) pregunta aparte (sin implementar todavia): si ya existe un mecanismo de preferencias visuales por usuario individual (para poder guardar ahi, mas adelante, el ancho de columnas de la tabla de detalle de pedido en Produccion).
- Cambios: **Fusion de tarjetas**: "Orden de pestanas" + "Permisos de pestanas" pasaron a ser una sola tarjeta "Pestanas y permisos" en `app/admin/page.js`; cada fila de pestana ahora tiene, en un mismo renglon: checkbox de seleccion bulk, agarradera de arrastre (mismo mecanismo de drag nativo que ya existia), icono+nombre, numero de orden, resumen de visibilidad y boton "Editar".
  **Orden de subpestanas**: nuevo setting global `admin_subtab_order` (mismo mecanismo `settings` key/value que `admin_tab_order`), forma `{ [tabId]: [subtabId,...] }`, con normalizacion tolerante a datos viejos/incompletos (`getOrderedSubtabs`). Cada pestana con subpestanas (Usuarios/Produccion/Seguimiento) las lista debajo, indentadas, cada una con su propia agarradera de arrastre (drag scopeado a esa pestana especifica via un solo estado `draggingConfigSubtab`), checkbox y "Editar". El orden elegido ahora es lo que efectivamente ordena la navegacion real: `renderUsersSubtabs()`, `TrackingTab` y `ProductionTab.js` ya no usan una lista de subpestanas hardcodeada, arman los botones a partir del arreglo ya ordenado+filtrado (`visibleUsersSubtabs`/`visibleProductionSubtabs`/`visibleTrackingSubtabs`).
  **"Configuracion" ahora configurable**: se saco el caso especial hardcodeado (`resolveTabVisible` ya no excluye `'config'`) y la constante `ADMIN_NON_CONFIGURABLE_TABS` se elimino. Como salvavidas minimo (no pedido explicitamente, pero para evitar un accidente serio): si al guardar una restriccion de "Configuracion" el propio admin que esta editando quedaria excluido, aparece un `window.confirm` avisando que la unica forma de revertirlo despues seria editando la tabla `settings` directo en Supabase.
  **Bulk reorder de pestanas**: nueva funcion `moveSelectedTabsBy('up'|'down')` que mueve, como bloque, las filas de pestanas (no subpestanas) tildadas para bulk, preservando su orden relativo entre si (algoritmo de intercambio de a un paso, recorriendo el arreglo una vez). Aparecen botones "↑ Mover arriba"/"↓ Mover abajo" en la barra bulk (deshabilitados si la seleccion actual son solo subpestanas). Se saco el texto "N seleccionada(s)" de esa barra, tal como pidio el usuario; solo quedan los botones de accion.
  **Confirmado explicitamente en la respuesta al usuario (sin cambios de codigo porque ya era asi)**: el orden de pestanas/subpestanas y los permisos de visibilidad son configuracion GLOBAL (misma fila `settings`, sin sufijo de usuario) — aplica igual para todos los que entran a `/admin`, a diferencia de otras preferencias que si son por-admin (ver punto siguiente).
  **Investigacion para el punto 5 (sin implementar nada todavia)**: se encontro que ya existe un mecanismo real de preferencia por-admin-individual en este mismo archivo: `adminPreferenceKey(baseKey)` arma una key tipo `${baseKey}_<email_normalizado>` y se guarda con el mismo `saveSetting()` en la tabla `settings` generica (lo usa hoy `admin_scale_seller_filter_individual` para el filtro de vendedor en Escalas, con un toggle para volverlo global si se prefiere). Es decir, no hay una tabla nueva de "preferencias de usuario": el mismo patron de `settings` + key sufijada por email ya sirve para esto, incluso ahora que los operarios tambien tienen `currentUser` (email) seteado al entrar a `/admin`.
- Verificacion: `CI=true npm run build` OK, sin errores ni warnings nuevos (solo los 2 warnings preexistentes de `<img>`). Se releyo el diff completo de este turno linea por linea (igual que el turno anterior) buscando bugs de logica antes de commitear. Se levanto el servidor de desarrollo una vez mas y se confirmo `/admin` compilando y respondiendo `200` sin errores. **Misma limitacion que el turno anterior**: no se pudo ejercitar el drag-and-drop real ni el flujo logueado en un navegador real (Google OAuth, sin cuenta de prueba ni Playwright/chromium-cli instalado en este entorno) — se compenso con relectura cuidadosa del diff y con el hecho de que el mecanismo de drag de subpestanas es una copia deliberada, casi 1 a 1, del mecanismo de drag de pestanas que ya esta en produccion desde el turno anterior (mismo patron probado, aplicado a un arreglo anidado en vez del arreglo principal).
- Auditoria: Se releyo la entrada anterior (mismo dia, 17:33) para partir del diseno ya explicado ahi (modelo de datos, resolucion de visibilidad, gate de acceso a operarios) y modificarlo en vez de rehacerlo.
- Pendiente/Riesgos: Sigue pendiente la prueba real en navegador con login real (admin y operario) — recomendado antes de asignarle produccion a operarios reales, ahora con mas motivo porque el drag de subpestanas y el mover-en-bulk son interacciones nuevas sin probar en vivo. El punto 5 (ancho de columnas de la tabla de detalle de pedido en Produccion, guardado como preferencia por-admin) queda pendiente de implementar, a la espera de que el usuario confirme el entendimiento.

## 2026-07-08 17:33 -03:00 - Claude Sonnet 5

- Objetivo: En Admin > Configuracion, en la misma tarjeta donde ya se puede reordenar las pestanas de arriba, agregar la posibilidad de asignar QUIEN ve cada pestana y cada subpestana (multiselect de admins + operarios), con seleccion multiple de filas para carga masiva (bulk). Si no se selecciona un usuario para una pestana/subpestana, no se le muestra; si se oculta una pestana entera, sus subpestanas quedan ocultas automaticamente.
- Decision previa consultada con el usuario: hoy `/admin` solo dejaba entrar a emails de la tabla `admins` (pantalla "Acceso denegado" para cualquier otro); para que asignarle pestanas a un operario tuviera efecto real, hacia falta que `/admin` tambien aceptara operarios. Se pregunto explicitamente y el usuario eligio permitir el ingreso de operarios a `/admin` (viendo solo lo que se les asigne), sin tocar `/operarios` que sigue igual.
- Cambios: **Modelo de datos** nuevo en `app/admin/page.js`: setting `admin_tab_visibility` (mismo mecanismo `settings` key/value que ya usa `admin_tab_order`), forma `{ [tabId]: { users: [...] , subtabs: { [subtabId]: { users: [...] } } } }`. Identidad de usuario = `admin:<email>` u `operator:<email>` (email en minuscula). Default si una pestana/subpestana nunca se toco: pestanas visibles para todos los admins (no para operarios, igual que hoy); subpestanas sin restriccion propia heredan lo que ya resolvio la pestana padre. La pestana `config` queda hardcodeada como siempre-visible-solo-para-admins y fuera de la lista configurable, a proposito: si se pudiera restringir, un admin podria auto-excluirse de Configuracion y perder la unica forma de revertirlo desde la UI.
  **Acceso**: `checkAdmin()` ahora, si el email no esta en `admins`, llama la RPC ya existente `claim_production_operator()` (la misma que usa `/operarios`) para reconocer operarios activos; si tampoco es operario, sigue mostrando "Acceso denegado" igual que antes. Antes de pasar a `screen:'panel'`, se traen los `settings` (incluye el nuevo permiso) y se corrige de una vez `activeTab`/`usersSubtab`/`productionSubtab`/`trackingSubtab` a la primera opcion permitida para ese viewer especifico -para que un operario nunca vea, ni por un instante, una pestana que no le corresponde, ni siquiera si entra con `?tab=database` a mano en la URL-. Se agrego ademas un `useEffect` que repite esa correccion en vivo si el panel ya esta abierto y la configuracion de permisos cambia mientras tanto (ej. dos admins, uno editando permisos y el otro navegando).
  **UI nueva**: tarjeta "Permisos de pestanas" en Configuracion, debajo de "Orden de pestanas". Lista cada pestana (en el mismo orden personalizable) con su resumen ("Todos los admins" / "N seleccionados" / "Nadie") y boton "Editar"; las que tienen subpestanas (`users`, `production`, `tracking`) las listan debajo, indentadas, cada una con su propio resumen/editar. Cada fila tiene un checkbox de seleccion para bulk; con 1+ marcadas aparece una barra "Asignar usuarios..." que abre el mismo modal pero aplicando el resultado a todas las filas marcadas de una. El modal lista admins y operarios (de los arrays `admins`/`operators` ya cargados en la pagina) con checkboxes, atajos "Seleccionar todos"/"Ninguno", boton "Guardar" y un boton separado "Quitar restriccion" que borra la config explicita de esa fila (vuelve al default) en vez de guardar una lista vacia -para no confundir "nadie" (deliberado) con "todavia no configurado".
  **Filtrado real**: la barra de pestanas superior, `renderUsersSubtabs()`, el nav interno de `TrackingTab` y el de `ProductionTab.js` ahora filtran por lo que el viewer tiene permitido (props nuevas `allowedSubtabs` en ambos componentes). Pantalla nueva "Todavia no tenes pestanas asignadas" para el caso limite de un operario sin ninguna pestana habilitada todavia.
- Verificacion: `dotnet`... no aplica (sin cambios .NET esta vez). `CI=true npm run build` OK, sin errores nuevos (solo quedaron los warnings preexistentes de `<img>`; se detecto y corrigio en el camino un error real propio -comillas rectas sin escapar en un parrafo nuevo- y un warning nuevo de `exhaustive-deps` en un `useMemo`, ambos ya resueltos antes de commitear). Se levanto el servidor de desarrollo (`npm run dev`) y se confirmo por log que `/admin` compila sin errores (6014 modulos) y responde `200`; se confirmo con `curl` que la pantalla inicial "checking" (blanco, previo a resolver sesion) se sirve como se espera. **Limitacion real de esta verificacion**: no se pudo probar el flujo logueado (ver la tarjeta nueva, tildar usuarios, guardar, y sobre todo el login de un operario real a `/admin`) porque el acceso es exclusivamente Google OAuth y este entorno no tiene una cuenta de Google real disponible ni un navegador con el que completar ese login; tampoco hay `chromium-cli`/Playwright instalado en el proyecto para automatizarlo sin agregar una dependencia nueva pesada solo para esta prueba. Se compenso con una relectura completa y deliberada del diff (linea por linea) buscando bugs de logica, y se corrigieron dos cosas menores encontradas asi (ver Pendiente).
- Auditoria: Se audito la entrada anterior (Bridge v1.6.6, este mismo turno mas temprano) y se confirmo que no tocaba nada de `app/admin/page.js`. Se investigo primero con un subagente Explore la estructura real de pestanas/subpestanas, el mecanismo existente de `admin_tab_order`, y el esquema real de `admins`/`production_operators`/RLS antes de tocar nada, para no inventar una forma de guardado distinta a la ya establecida en el proyecto (se reuso `settings` + `saveSetting`, igual que `admin_tab_order` y que `visibility-rules.js` para clientes).
- Pendiente/Riesgos: **Falta la prueba real con un operario real entrando a `/admin`** (login Google, ver que efectivamente solo aparecen sus pestanas asignadas) — recomendado antes de asignarle produccion a operarios reales. La seguridad de "ocultar una pestana" es real para casi todos los datos (Supabase RLS ya exige `is_admin()` en la gran mayoria de tablas, confirmado leyendo las policies, asi que un operario sin acceso a una pestana tampoco puede leer esos datos aunque manipule la URL) pero no se audito RLS tabla por tabla exhaustivamente; si en el futuro se agrega una tabla nueva sin RLS propio, ocultar su pestana en la UI seria solo cosmetico. La pestana "Base de datos" ya tenia su propio candado hardcodeado (`DATABASE_EDITOR_EMAIL`, un solo email con permiso de edicion) que sigue intacto y sin relacion con este sistema nuevo -este sistema nuevo solo controla quien ve la pestana, no quien puede editar adentro-. Las subpestanas internas de "Base de datos" (Productos/Disenos/Escalas/etc., un spreadsheet distinto por tabla dentro de `AdminDatabaseSheet.js`) quedaron deliberadamente fuera de este sistema -son un componente aparte con su propia logica, no comparten el patron `xSubtab` de React que usan Usuarios/Produccion/Seguimiento-; si se quiere permisos ahi tambien seria un trabajo aparte. Dar acceso a un operario a Usuarios > Admins le permitiria gestionar (agregar/quitar) admins -es una decision deliberada del admin que lo asigne, no un bug, pero vale tenerlo presente-.

## 2026-07-08 16:17 -03:00 - Claude Sonnet 5

- Objetivo: El usuario actualizo el Bridge a 1.6.5 en una PC y al imprimir aparecio "SumatraPDF no esta disponible" (un error que ya habia visto antes), pese a que en una prueba anterior creia que ya andaba bien; en otra PC con una version distinta, la impresion si funciona. Pidio analizar la causa y evaluar si convenia pasarme esa "version vieja" para revisarla. Sobre la marcha aclaro que el Bridge corriendo en esta PC (la de este chat, no la de las dos que menciono) tambien imprime bien (1 trabajo x10 copias), y pregunto si podia revisar esa instalacion.
- Cambios: **Diagnostico** (sin tocar codigo todavia): se descargo el ZIP real publicado de `bridge-v1.6.5` desde GitHub y se confirmo que si incluye `SumatraPDF.exe` (74.5MB, contenido correcto) — descartado bug de empaquetado. Se comparo el commit del tag contra `HEAD` (sin diffs) — descartado fix sin publicar. Se reviso el historial de `PrintJobService.cs` y se encontro la causa real: desde el commit `51fbf61` (v1.6.3), si SumatraPDF no se detecta Y se piden >1 copias, el Bridge tira una excepcion y **no imprime nada**; antes de ese commit, en el mismo caso igual imprimia (con el bug original de N trabajos de 1 copia). Es decir la "version vieja que anda bien" del usuario probablemente nunca curo el bug original, solo no tiene el bloqueo duro nuevo. Se encontro ademas, en esta PC, un proceso real corriendo (`bin/Published`, build suelto sin versionar) y una copia extraida en Descargas del ZIP real de 1.6.5 — ambos con `SumatraPDF.exe` como sibling directo del exe, por eso imprimen bien: la deteccion por "archivo al lado del exe" nunca fallo aca. Lo unico que cambia en el caso roto es el paso de migracion a la carpeta estable (`%LOCALAPPDATA%\Inkora\PrintBridge\app`) que hace el boton "Actualizar"; el sospechoso mas probable es que el antivirus ponga en cuarentena el `SumatraPDF.exe` recien copiado ahi (archivo no firmado, invocado en modo silencioso).
  **Endurecimiento del Bridge (v1.6.6)**, para que esta clase de falla no vuelva a bloquear la impresion sin importar la causa exacta: en `PrintJobService.cs`, `EnsureSumatraDetected()` ahora intenta una auto-reparacion (una vez por proceso) descargando `SumatraPDF.exe` desde la URL portable oficial (la misma que ya usaba `build-release.ps1` como fallback) a la carpeta estable, antes de resignarse. Se elimino el `throw` que bloqueaba impresiones de >1 copia sin Sumatra: ahora cae al metodo viejo (N trabajos separados) pero deja un aviso claro en el nuevo campo `PrintJob.Warning` ("se imprimieron N copias como trabajos separados..., revisa si el antivirus puso en cuarentena SumatraPDF"), visible en las respuestas de `/print` y `/print-direct` y en `/print/queue`. Bump de version a `1.6.6` (`.csproj` + `LATEST_BRIDGE_VERSION` en `components/ProductionTab.js` y `app/operarios/page.js` + default de `build-release.ps1`).
- Verificacion: `dotnet build` OK (0 errores). `node --check` OK en `ProductionTab.js` y `operarios/page.js`. `CI=true npm run build` OK con warnings preexistentes. **Prueba real de punta a punta** (no solo teoria): se publico el exe localmente sin `SumatraPDF.exe` cerca, se armo una carpeta+PDF de prueba y se disparo `/print-direct` real contra una impresora inexistente (para no gastar papel/tinta de las Epson reales conectadas a esta PC) con 3 copias. Caso A (URL de descarga real): la auto-reparacion bajo el .exe real (16.065.496 bytes, mismo hash que el vendorizado) en ~9s, quedo detectado, `printMethod` paso a `SumatraPDF` y el trabajo termino `done` sin error. Caso B (URL de descarga forzada a un dominio invalido, para simular que la reparacion tambien falla): el trabajo igual termino `ok:true`/`done`, con `printMethod: shell-printto-single-copy` y el nuevo `warning` explicando la degradacion — ya no se bloquea la impresion. Se corrio tambien `build-release.ps1 -Version 1.6.6` localmente: genero el ZIP con `SumatraPDF.exe` incluido (70.7MB).
- Auditoria: Se reviso `git log`/`git tag` de `bridge/` completo (no solo la ultima entrada) para entender el historial real del bug de copias, ya que el pedido del usuario requeria contexto de varios turnos anteriores (ChatGPT Codex, v1.6.3-1.6.5). **Riesgo/incidente propio de este turno**: para poder probar `/print-direct` de punta a punta hizo falta una carpeta PDF autorizada; se sobreescribio sin querer `pdf-roots.json` real de esta PC (en `%LOCALAPPDATA%\INKORA\PrintBridge\`) con la carpeta de prueba. Se detecto y se reconstruyo el valor original a partir de las lineas "Carpeta PDF autorizada: ..." del propio log de diagnostico (`Z:\Mi unidad\1 INKORA\Planchas de calcos` y su subcarpeta `...\PDFs con nombre`), y se confirmo la restauracion re-escaneando: 654 PDFs reales encontrados de nuevo (Paw Patrol, Stitch, etc.), coincide con lo que ya se veia en el log antes de tocar nada.
- Pendiente/Riesgos: Falta la confirmacion real en la PC que reporto el error: pedirle al usuario, en esa PC, (1) abrir `http://127.0.0.1:17389/health` y mirar `sumatraPdfProbePaths`, (2) revisar Windows Security > Historial de proteccion por una cuarentena de "SumatraPDF" cerca de la hora de la actualizacion, (3) confirmar en el Explorador si `%LOCALAPPDATA%\Inkora\PrintBridge\app\SumatraPDF.exe` existe. Falta publicar el release real: crear tag `bridge-v1.6.6`, pushear y confirmar que GitHub Actions genera el asset y que la PC afectada pueda actualizar y probar de nuevo. La auto-reparacion depende de que esa PC tenga salida a internet hacia `sumatrapdfreader.org` al momento de imprimir (razonable, ya necesita salir a `inkora.com.ar`); si esa URL puntual dejara de existir en el futuro, la auto-reparacion fallaria en silencio (logueado) y el Bridge seguiria imprimiendo en modo degradado con warning, no bloqueado. No se pudo probar en una Epson fisica real (se evito a proposito para no gastar consumibles); la prueba fue contra una impresora inexistente para validar solo la logica de deteccion/fallback.

## 2026-07-08 10:28 -03:00 - Claude Sonnet 5

- Objetivo: Ampliacion grande del panel de backups (`tools/backup-app/`) pedida por el usuario en una lista de 10 puntos: (1) integrar la copia a Google Drive dentro del mismo programita (el usuario paso la ruta real: `Z:\Mi unidad\1 INKORA\Code\inkora-erp\Copia de seguridad`, que resulto ser una carpeta sincronizada por Google Drive de escritorio, no algo que necesite API/OAuth); (2)(3) icono de Inkora para el programa y dentro de el, y nombre de archivo mas distintivo; (4) backup manual del CODIGO del proyecto (no solo la base), con la misma info que el de DB; (5) carpeta configurable para ambos con selector nativo de Windows; (6) frecuencia semanal por defecto para el codigo; (7) frecuencia configurable para ambos; (8) barra de progreso real (no solo texto) durante la copia; (9) indicador de proxima corrida automatica; (10) ventana a 1/4 de pantalla. Antes de este pedido tambien se le explico al usuario, con analisis honesto, que backupear el codigo a Drive es una buena idea pero menos critico que la base (el codigo ya vive duplicado en esta PC + GitHub).
- Cambios: Reescritura casi completa de `tools/backup-app/`. **Config persistente**: nuevo `config.js`/`config.json` (excluido de git) con rutas locales+Drive, frecuencia en dias y ultima corrida por tipo (`db`/`code`), mas helpers `isDue`/`nextRunInfo` para calcular si corresponde correr y cuanto falta. **Backup de codigo nuevo**: `handleRunCodeBackup` arma un zip (libreria `archiver`, fijada en `7.0.1` porque la `8.0.0` que instalo npm por defecto cambio su forma de exportarse — `archiver(...)` dejo de ser una funcion invocable y tiro `archiver is not a function`, se detecto en la primera prueba y se bajo de version) del codigo fuente actual excluyendo dependencias/build/binarios grandes (lista en el propio archivo: `node_modules`, `.next`, `.git`, `.vercel`, `coverage`, `bin`/`obj`/`build`/`.gradle`/`.vs` de los proyectos .NET/Android, `tools/pg-bin`, `public/models`, el ejecutable vendorizado `bridge/Inkora.PrintBridge/tools`, y el zip `Inkora.PrintBridge.zip` de la raiz) — se ajusto esta lista en varias vueltas despues de detectar con pruebas reales que el primer intento generaba un zip de 867MB (incluia sin querer una carpeta `bridge/.../bin` con DLLs de .NET duplicadas y builds de Android) hasta llegar a 232 archivos / 3.3MB, que es codigo fuente real. **Progreso real** (no una animacion generica): para la base de datos se cuenta cuantas lineas `dumping contents of table` salieron de `pg_dump --verbose` contra el total de tablas; para el codigo, archivos ya comprimidos contra el total detectado. **Copia a Drive**: como la carpeta que paso el usuario ya es una carpeta local sincronizada por Google Drive de escritorio, la "subida" es un `fs.copyFileSync` comun a esa ruta — sin rclone, sin OAuth, sin tocar nada mas de esa carpeta (nunca se borra nada ahi, solo se agregan archivos, tal como pidio el usuario explicitamente por tratarse de un Drive "delicado"). **Endpoints nuevos**: `/api/config` (GET/POST), `/api/browse-folder` (spawnea un `FolderBrowserDialog` de `System.Windows.Forms` via PowerShell y devuelve la ruta elegida), `/api/autorun-plan` (que corresponde correr hoy segun frecuencia). **UI rehecha**: `index.html`/`style.css`/`app.js` pasaron de una columna angosta a un layout de dos secciones lado a lado (DB y Codigo) generadas desde un `<template>` compartido, con logo de Inkora (`public/logo.png`, copiado de `public/icons/icon-192.png` del sitio) en el encabezado, selector de frecuencia, rutas local/Drive con boton "Cambiar", barra de proxima automatica, y barra de progreso real ademas del log de texto que ya existia. **Icono**: se genero `tools/backup-app/icon.ico` a mano (sin libreria: un `.ico` valido puede envolver un PNG directo desde Windows Vista en adelante, asi que se armo el header/directory-entry de 22 bytes y se le pegaron los bytes del PNG original) a partir del mismo logo del sitio; se renombro `launch.vbs` a `Inkora-Backups.vbs`; se actualizo el acceso directo del Escritorio (ahora "Inkora Backups.lnk") para usar el nuevo nombre/icono; se actualizo la tarea programada de Windows para apuntar al script renombrado. **Ventana a 1/4 de pantalla**: el primer intento calculaba la resolucion real via WMI (`Win32_VideoController`) desde el propio `.vbs`, pero eso resulto ser la causa de que el lanzador quedara colgado en las pruebas (ver Verificacion) — se saco esa consulta WMI del `.vbs` (que ahora solo abre con un tamano de arranque fijo, 960x540) y en cambio la propia pagina se redimensiona sola al cargar con `window.resizeTo`/`moveTo` usando `screen.availWidth/Height`, mucho mas rapido y sin depender de WMI.
- Verificacion: Se probo cada pieza por separado con `curl`/Node antes de dar nada por bueno. `archiver@8.0.0` fallo en la primera prueba real (no en teoria) con `archiver is not a function`; se aislo el problema con un script standalone, se confirmo que la 7.0.1 si exporta la funcion clasica, y se fijo esa version. El primer zip de codigo (867MB) se diagnostico listando el contenido real del zip (`unzip -l | sort -rn`) en vez de asumir cuales carpetas excluir; se corrigio y volvio a probar hasta bajar a 3.3MB reales. Se probo `/api/run-code-backup` completo por SSE: progreso real, `done`, y evento `drive` confirmando copia exitosa a `Z:\Mi unidad\1 INKORA\Code\inkora-erp\Copia de seguridad\` (verificado ademas listando esa carpeta real, el archivo esta ahi). Se probo `/api/run-backup` (DB) confirmando que el porcentaje sube tabla por tabla (40%→99%) igual que antes pero ahora con numero real en vez de animacion. Se disparo la tarea programada real (`Start-ScheduledTask`) dos veces: la primera vez el `.vbs` quedo colgado (proceso `wscript` nunca terminaba) — se investigo y la causa real NO fue la lentitud de WMI como se sospechaba en un primer momento, sino un bug mas tonto: quedo una variable `projectRoot` sin declarar bajo `Option Explicit` (arrastrada de un `Dim` que se edito a medias), lo que dispara un dialogo de error de Windows Script Host invisible para este entorno y deja el proceso colgado esperando un clic que nunca llega; se saco esa linea muerta y de paso se sacó la consulta WMI enteramente (ver Cambios). Con esa correccion, la segunda corrida de `Start-ScheduledTask` funciono de punta a punta: `wscript` termino solo, el servidor quedo escuchando, y `config.json` registro un `lastRun` nuevo con un archivo de backup real generado (`supabase_2026-07-08_1023.sql`). **Limitacion real detectada y no resuelta**: pasados los 10 segundos de cuenta regresiva, `window.close()` no cerro la ventana (el servidor seguia respondiendo varios segundos despues) — no se intento forzar el cierre por PID porque en las pruebas se vio que Edge puede reusar un proceso ya existente en vez de abrir uno nuevo por cada ventana `--app=`, y matar ese proceso por PID arriesgaba cerrar OTRAS ventanas de Edge del usuario que no tienen nada que ver; se opto por la alternativa segura (dejar un mensaje "ya podes cerrar esta ventana" en vez de forzar nada) y se le aviso al usuario de esta limitacion en vez de asumir que quedo resuelta.
- Auditoria: Se releyo `BACKUPS.md` completo antes de reescribirlo, para mantener el tono/estructura ya establecida y no perder ninguna seccion previa (Session Pooler, restaurar, etc.) al ampliarla.
- Pendiente/Riesgos: El auto-cierre de la ventana a los 10s no esta garantizado (ver arriba) — el backup en si termina bien de todas formas, es solo la ventana la que puede quedar abierta esperando un cierre manual. No se probo el backup de la base de datos con `driveDir` configurado (solo se probo Drive para el codigo, que es la carpeta que dio el usuario); la logica es identica (`copyToDrive`) asi que deberia funcionar igual, pero falta confirmarlo con una carpeta real de Drive para la base si el usuario la quiere. La lista de exclusiones del backup de codigo es una lista fija en `server.js` (no descubierta dinamicamente desde `.gitignore`) — si en el futuro aparece una carpeta pesada nueva no prevista, hay que agregarla a mano ahi. No se pudo confirmar visualmente el resultado final (tamano real de ventana, distribucion de las dos secciones, que se vea bien) — falta que el usuario la abra y la mire.

## 2026-07-07 22:25 -03:00 - Claude Sonnet 5

- Objetivo: Dos pedidos del usuario sobre el backup de Supabase armado en los turnos anteriores: (1) documentar el fallback a Session Pooler (para que otra IA sin contexto previo lo sepa si hay que usarlo); (2) armar un boton/programa manual para disparar un backup en el momento, con estilo visual de Inkora, moderno/minimalista, ventana chica flotante, mostrando ultimo backup + stats de la base + progreso en vivo + boton para abrir la carpeta del resultado. Se discutio primero Admin-web vs programa local (se explico por que el local es mucho mas simple dado que todo lo demas ya vive en la PC del usuario, no en Vercel) y el usuario eligio el programa local. Tambien pidio que la corrida automatica de las 3 AM abra ese mismo panel para poder verla, y que se cierre sola 10s despues de terminar con cuenta regresiva. Se le pregunto si la tarea programada ya contemplaba "correr al prender la PC si estaba apagada a las 3AM" — se confirmo que ya estaba bien configurado desde el turno anterior (`-StartWhenAvailable`), no hizo falta corregir nada ahi.
- Cambios: **Documentacion**: se agrego a `BACKUPS.md` la seccion "Si el backup empieza a fallar solo" con el string de conexion alternativo del Session Pooler y los pasos exactos para cambiar `SUPABASE_DB_URL` si la conexion directa por IPv6 deja de andar.
  **Panel visual nuevo** (`tools/backup-app/`, con su propio `package.json`/`node_modules` — dependencia unica: `pg`, para las estadisticas en vivo — no toca el `package.json` del sitio Next.js): `server.js` es un servidor HTTP minimo (modulos nativos de Node + `pg`), escucha solo en `127.0.0.1:4173`, expone `GET /api/status` (ultimo backup leido de `backups/supabase/` + tablas/filas aprox./tamano de la base via una query a `pg_stat_user_tables`/`pg_database_size`), `GET /api/run-backup` (dispara `pg_dump.exe --verbose` y transmite cada linea de progreso por Server-Sent Events, mas un evento final `done` con archivo/tamano o `error`), `GET /api/open-folder` (abre `backups/supabase` en el Explorador) y `POST /api/shutdown` (se auto-apaga, lo llama el cliente al cerrar la ventana). `public/index.html` + `style.css` + `app.js` arman la mini-UI con la misma paleta que ya usa el resto de Inkora (`#1B2F5E`, `#2D6BE4`, `#5a6380`, bordes `#dde1ef`) pero con fuente de sistema en vez de bajar Barlow de internet (para que abra al instante sin depender de conexion). La ventana se abre con `--app=` de Edge/Chrome (sin barra de direcciones ni pestanas, tal como se pidio "ventana flotante chica"), lanzada por `tools/backup-app/launch.vbs` (arranca el server oculto de fondo, espera 1s, abre la ventana; con fallback si Edge/Chrome no estan en las rutas esperadas). El modo automatico se activa con el argumento `autorun` (`launch.vbs autorun` → URL con `?autorun=1`): la pagina dispara el backup sola al cargar y, al terminar (bien o mal), muestra una cuenta regresiva de 10s y se cierra sola (`window.close()`, mas un `sendBeacon` a `/api/shutdown` en `pagehide` para no dejar el servidor colgado). Se agrego un acceso directo en el Escritorio ("Inkora - Backup Supabase.lnk") apuntando a `wscript.exe launch.vbs`. Se reconfiguro la tarea programada existente ("Inkora - Backup Supabase diario") para que su accion ahora sea `wscript.exe launch.vbs autorun` en vez de correr el `.ps1` escondido — el script `.ps1` standalone se dejo intacto como respaldo, ya no es lo que usa la tarea por defecto. Se agrego `tools/backup-app/node_modules/` a `.gitignore`.
- Verificacion: se probaron los 4 endpoints del servidor a mano con `curl` (status devolvio 30 tablas / ~51k filas aprox. / 46MB, consistente con lo ya visto en el dump; run-backup transmitio el progreso real linea por linea de `pg_dump --verbose` y termino con `done` y un archivo real de ~59MB; open-folder efectivamente abrio una ventana nueva del Explorador, confirmado viendo el proceso `explorer.exe` recien creado; shutdown apago el proceso, confirmado porque la siguiente request se rechazo). Se corrio `launch.vbs` directo y se confirmo que levanta `node.exe` y `msedge.exe` nuevos. **La prueba mas importante**: se disparo la tarea programada real con `Start-ScheduledTask` (mismo mecanismo que usa Windows a las 3 AM, no una simulacion) y se confirmo que genero un archivo nuevo real (`supabase_2026-07-07_1922.sql`) de punta a punta, incluyendo que la variable de entorno `SUPABASE_DB_URL` se leyo correctamente en ese proceso — esto tambien confirmo indirectamente que Task Scheduler si toma el valor persistido en el registro sin necesidad de reiniciar la PC. (Un intento anterior de probar esto mismo lanzando `node` manualmente desde la sesion de PowerShell de este entorno fallo con "falta la variable" — no era un bug real, sino que esa sesion de PowerShell ya estaba abierta desde antes de configurar la variable y no la iba a ver nunca via herencia normal de entorno; quedo documentado en el propio razonamiento del turno para no confundir a futuro con un caso similar). Se mataron los procesos `node`/`msedge` de prueba al terminar para no dejar basura corriendo. **No se pudo confirmar visualmente el diseño/estilo de la ventana** (sin forma de tomar captura de pantalla en este entorno) — falta que el usuario la vea en persona y confirme que el estilo/tamano/comportamiento le cierran.
- Auditoria: Se releyo `BACKUPS.md` completo antes de tocar nada para mantener consistencia de tono/formato con lo ya escrito en turnos anteriores.
- Pendiente/Riesgos: Falta la confirmacion visual del usuario (estilo, tamano de ventana, que el cierre automatico a los 10s realmente funcione — `window.close()` en una ventana `--app=` generalmente esta permitido por Chrome/Edge sin el bloqueo tipico de "el script no abrio esta ventana", pero no se pudo verificar 100% sin probarlo en persona; si no cierra sola, el usuario puede cerrarla a mano sin que eso afecte al backup en si, que ya termino antes de mostrar la cuenta regresiva). Si en el futuro se clona este repo en otra PC, hay que acordarse de correr `npm install` dentro de `tools/backup-app/` antes de poder usar el panel (ya documentado en `BACKUPS.md`). Quedaron varios archivos de prueba reales en `backups/supabase/` de este turno (backups genuinos, no basura — se van a ir podando solos por la regla de retencion de 30, no hace falta borrarlos a mano).

## 2026-07-07 21:55 -03:00 - Claude Sonnet 5

- Objetivo: Terminar de dejar operativo el backup automatico de Supabase armado en el turno anterior (21:08). Faltaba el unico paso manual: configurar `SUPABASE_DB_URL`. El usuario encontro la cadena de conexion en el Dashboard (Project Settings > Connect > URI, conexion directa por IPv6: `db.ylawwaoznxzxwetlkjel.supabase.co:5432`) y paso la contrasena real por chat, autorizando explicitamente a configurarla.
- Cambios: Se seteo `SUPABASE_DB_URL` como variable de entorno de usuario (`[System.Environment]::SetEnvironmentVariable(..., "User")`) en la PC del usuario, con la cadena completa `postgresql://postgres:***@db.ylawwaoznxzxwetlkjel.supabase.co:5432/postgres`. Se verifico que quedo persistida leyendola de vuelta desde el registro (sin imprimir la contrasena, solo largo y host). No se modifico ningun archivo del repo en este turno.
- Verificacion: Correr `scripts\backup-supabase.ps1` directo desde la misma sesion de PowerShell fallo la primera vez ("Falta la variable SUPABASE_DB_URL") — comportamiento esperado y no un bug: una sesion de PowerShell ya abierta no relee variables de usuario recien guardadas en el registro hasta que se abre una sesion nueva, pero la tarea programada de Windows si arranca con el entorno actualizado en cada corrida (proceso nuevo cada vez). Para probar el pipeline completo sin esperar al proximo reinicio de sesion, se seteo la variable en memoria solo para esa llamada (`$env:SUPABASE_DB_URL = "..."`) y se corrio el script: genero exitosamente `backups\supabase\supabase_2026-07-07_1853.sql` (57MB, 54597 lineas, 30 `CREATE TABLE`, cantidad de tablas consistente con la lista de `CONTEXT.md`). Se confirmo con `git status --short` que el archivo no aparece como para trackear (el `.gitignore` de `/backups/` funciona).
- Auditoria: N/A, continuacion directa del turno anterior (21:08), mismo objetivo.
- Pendiente/Riesgos: La corrida real de la tarea programada (3:00 AM, proceso nuevo lanzado por el servicio de Task Scheduler) todavia no se probo sola — se probo el script a mano con la variable inyectada manualmente, que ejercita la misma logica pero no exactamente el mismo mecanismo de arranque. Debido a que la conexion usada es "Direct connection" (IPv6) y no el "Session pooler", si en el futuro esta red cambia de forma que pierda salida IPv6, el backup diario empezaria a fallar en silencio (el usuario ya fue avisado de este riesgo y de la alternativa del pooler). Falta confirmar manana que la corrida automatica de las 3 AM efectivamente genero un backup nuevo.

## 2026-07-07 21:35 -03:00 - Claude Sonnet 5

- Objetivo: Charla sobre portabilidad fuera de Supabase (se le informo al usuario, con relevamiento real del codigo, que hoy NO es una migracion rapida: 39 archivos con queries `.from()` dispersas sin capa de acceso a datos, 22 llamados `.rpc()`, 59 politicas RLS en 15 archivos SQL, 34 usos de Realtime en 7 archivos, 61 usos de Auth en 23 archivos). De ahi paso a pedir algo mas concreto y accionable: un backup periodico propio de los datos de Supabase en formato generico, guardado dentro de la carpeta del proyecto, lo mas simple posible para el usuario. Se le pregunto la frecuencia y eligio diaria.
- Cambios: Se armo un backup 100% automatico y local (no en la nube) que corre solo, sin que el usuario tenga que acordarse de nada. Se investigo primero: `supabase db dump` (CLI oficial) requiere Docker corriendo, lo cual agrega friccion/dependencia pesada — se opto en cambio por `pg_dump` directo (Supabase es Postgres estandar, acepta conexion por cadena de conexion comun). El instalador oficial de Postgres para Windows requiere elevacion de administrador (UAC), que no se puede automatizar sin interaccion; en cambio se descargo el ZIP portable de binarios de EnterpriseDB (sin instalador), se extrajo `pg_dump.exe` + las DLLs que necesita (sin las de pgAdmin/GUI, no hacen falta) a `tools/pg-bin/` (~44MB, vendorizado dentro del proyecto, no requiere instalacion ni admin) y se verifico que corre solo (`pg_dump --version` OK). Se creo `scripts/backup-supabase.ps1` (lee la cadena de conexion de la variable de entorno `SUPABASE_DB_URL` — nunca queda escrita en el script ni se le pidio la contraseña al usuario por chat — corre `pg_dump --schema=public --no-owner --no-privileges`, guarda en `backups/supabase/supabase_<fecha>.sql`, y borra automaticamente los backups de mas de 30 dias de antiguedad). Se registro una tarea programada de Windows ("Inkora - Backup Supabase diario", `Register-ScheduledTask`, sin necesitar elevacion) que corre el script todos los dias a las 3:00 AM, con "ejecutar apenas se pueda" si la PC estaba apagada a esa hora. Se agrego `/backups/` y `/tools/pg-bin/` a `.gitignore` (informado explicitamente al usuario que subir dumps reales violaria la regla de seguridad ya existente en `CONTEXT.md`). Se creo `BACKUPS.md` con las instrucciones exactas de configuracion inicial (como pegar la cadena de conexion de Supabase como variable de entorno local, un paso que tiene que hacer el usuario a mano porque no correspondia pedirle la contraseña por el chat), como probarlo, como ver/desactivar la tarea, y como restaurar un dump. Se agrego puntero a `BACKUPS.md` en `CONTEXT.md`.
- Verificacion: Se corrio `pg_dump.exe --version` (OK, `pg_dump (PostgreSQL) 17.10`, confirma que el binario portable funciona sin instalar nada). Se registro la tarea programada y se confirmo con `Get-ScheduledTask` que quedo en estado "Ready". **No se pudo correr un backup real de punta a punta** porque hace falta la contraseña real de la base de Supabase, que el usuario todavia no configuro como variable de entorno (paso pendiente de su lado, documentado en `BACKUPS.md`). Se confirmo con `git status --short` que `backups/` y `tools/pg-bin/` quedan afuera de git como corresponde, y que solo `scripts/`, `BACKUPS.md`, `.gitignore` y el puntero en `CONTEXT.md` quedan para commitear.
- Auditoria: Se reviso `CONTEXT.md` (seccion Seguridad: "no subir dumps de Supabase / backups SQL con datos reales") antes de decidir donde guardar los archivos, para no violar una regla ya existente del proyecto.
- Pendiente/Riesgos: El usuario todavia tiene que hacer el paso manual de `BACKUPS.md` (pegar su cadena de conexion de Supabase como variable de entorno `SUPABASE_DB_URL`) para que el primer backup real corra; hasta entonces la tarea programada va a fallar silenciosamente cada noche (el script corta con error controlado, no rompe nada, pero no genera backup). El dump esta acotado a `--schema=public` a proposito (evita fallos de permisos contra esquemas internos de Supabase); si el usuario quiere que tambien incluya `auth.users` (emails/usuarios), hay que agregar `--schema=auth` al script, no se hizo porque no se pidio explicitamente. No cubre los archivos del bucket de Storage (imagenes/GLB) — eso quedaria como una automatizacion aparte si se pide.

## 2026-07-07 21:08 -03:00 - Claude Sonnet 5

- Objetivo: Despues de charlar sobre por que el mic de Android suena repetido (limitacion de plataforma de la Web Speech API, no corregible desde el codigo) y sobre alternativas (grabar audio crudo + transcribir con un motor que corra en el propio dispositivo), el usuario pidio armar un segundo boton de carga por voz en el modal de "Nuevo pedido" que use Vosk (motor open source, corre en WebAssembly en el navegador), sin borrar el boton existente de Web Speech API, para poder comparar los dos metodos lado a lado.
- Cambios: Se investigo la API real de `vosk-browser` (README, `.d.ts`, ejemplos del repo en GitHub) antes de escribir nada. Se descargo `vosk-model-small-es-0.42.zip` (modelo liviano en espanol de alphacephei.com/vosk/models, ~38MB, Apache 2.0), se convirtio al formato que exige la libreria (`vosk-browser` requiere un `.tar.gz` cuya carpeta de nivel superior se llame literalmente `model/`, no el nombre original del modelo) y se lo dejo en `public/models/vosk-model-small-es-0.42.tar.gz` (servido como estatico de Next.js). Se instalo la dependencia `vosk-browser` (`package.json`/`package-lock.json`). En `components/CreateOrderModal.js`: nuevo boton "V" a la derecha de la barra de voz (el existente de Web Speech API queda igual, a la izquierda); nuevas funciones `loadVoskModel` (carga/cachea el modelo una vez), `startVosk`/`stopVosk`/`stopVoskInternal` (arman el pipeline `getUserMedia` → `AudioContext` → `ScriptProcessorNode` → `recognizer.acceptWaveform()`, con el processor ruteado a traves de un `GainNode` en volumen 0 antes de `destination` para que dispare `onaudioprocess` de forma confiable sin generar eco del microfono). Los resultados finales de Vosk se mandan al mismo `processVoiceFinal(text)` que ya usaba la Web Speech API, para que ambos motores compartan el mismo parser/matching/comandos de voz y la comparacion sea solo sobre la transcripcion en si. Se agrego `activeEngineRef` ('webspeech'|'vosk') y un helper `stopActiveEngine()` para que los comandos de voz ("pedido guardar", etc.) detengan el motor que realmente esta activo — antes esas ramas de `processVoiceFinal` asumian siempre Web Speech API. Los dos botones se deshabilitan mutuamente mientras el otro esta grabando. Se agrego tambien la seccion "Segundo motor: Vosk" en `VOICE_ORDER_FEATURE.md` con el detalle tecnico completo (incluyendo como reconvertir el modelo si hace falta actualizarlo).
- Verificacion: `npx eslint components/CreateOrderModal.js` sin errores nuevos (mismo warning preexistente de siempre). `CI=true npm run build` completo OK. Se verifico especificamente que `vosk-browser` (~5.7MB de bundle) NO se suma al chunk principal del admin: el `import('vosk-browser')` es dinamico dentro de `startVosk()`, y tras el build se confirmo con `grep` sobre `.next/static/chunks` que el codigo de `KaldiRecognizer` vive en un chunk separado (`9cb48f3a....js`, 5.7MB) que solo se pide cuando se aprieta el boton — el tamano de "First Load JS" de `/admin` reportado por el build casi no cambio (+1KB). **No se pudo probar con microfono real en un dispositivo** (sin browser real disponible en este entorno) — falta la prueba end-to-end en un Android real, que es justamente el objetivo de este boton.
- Auditoria: Se releyo `VOICE_ORDER_FEATURE.md` (creado en el turno anterior) antes de tocar codigo, para mantener consistencia con lo ya documentado sobre la Web Speech API. `git status --short` antes de empezar solo mostraba los binarios sin trackear ya conocidos.
- Pendiente/Riesgos: Falta la prueba real en celular — no hay garantia de que el pipeline de audio (`ScriptProcessorNode`, sample rate de `audioContext.sampleRate` pasado al `KaldiRecognizer`) funcione sin ajustes en un dispositivo real; puede necesitar iteracion despues de la primera prueba del usuario. Esta version de Vosk no tiene pausa/reanudar (solo iniciar/detener). El modelo (~38MB) se vuelve a descargar/inicializar cada vez que se abre el modal (no persiste entre aperturas), lo cual puede sentirse lento la primera vez de cada apertura — si molesta, se puede mover el cache a un nivel superior al modal. Se sumo un archivo binario de ~38MB al repo (`public/models/...tar.gz`), bien por debajo del limite de GitHub (100MB) pero aumenta el tamano del repo/clone.

## 2026-07-07 20:26 -03:00 - Claude Sonnet 5

- Objetivo: El usuario corrigio un supuesto del turno anterior (20:19): el "1" pegado en "Argentina 1" (nombre de diseno cargado por voz) NO es un artefacto del reconocedor de voz — es el nombre real de ese diseno en el catalogo (este negocio numera variantes de disenos). Pidio crear documentacion de como funciona la carga de pedidos por voz, para que esto no se repita.
- Cambios: Nuevo archivo `VOICE_ORDER_FEATURE.md` en la raiz (mismo patron que `CHAT_FEATURE_PLAN.md`): explica la gramatica de comandos de voz (namespaces "pedido guardar"/"microfono cerrar", campos reconocidos, como se separan diseno+cantidad), el matching contra disenos reales (`fuzzyMatchDesigns`, threshold 0.68), las limitaciones conocidas de Android (beep cada ~5s, reinicio del reconocimiento) y el patron de refs para comandos de voz (`handleSaveRef`/`handleCloseRef`). Arranca explicitamente con la regla "un nombre de diseno raro dictado por voz puede ser real, verificar contra el catalogo antes de asumir bug de transcripcion" citando este mismo incidente como motivo. Se agrego un puntero a este archivo en `CONTEXT.md`, seccion nueva "Features implementadas con documentacion propia" (junto a la seccion ya existente de planes pendientes).
- Verificacion: Cambio de documentacion unicamente, sin tocar codigo. No aplica build/lint.
- Auditoria: Se releyeron las dos entradas anteriores de esta bitacora (19:15 y 20:19) para no repetir la misma suposicion incorrecta en la documentacion nueva.
- Pendiente/Riesgos: Ninguno. Nota para el futuro: si vuelve a aparecer un reporte de "nombre de diseno raro" cargado por voz, primero verificar contra la tabla `designs` real antes de asumir cualquier cosa sobre el reconocimiento de voz.

## 2026-07-07 20:19 -03:00 - Claude Sonnet 5

- Objetivo: El usuario probo en el celular el fix de "transcripcion repetida" del turno anterior y mando una captura mostrando que el bug seguia: el "Dictado" mostraba `Argentina Argentina 1 Argentina 1 por Argentina 1 por 10  siguiente abejita abejita 1 ...` y el diseno cargado quedaba con el nombre "Argentina 1" (con un "1" de mas) en vez de "Argentina".
- Cambios: El fix anterior (`lastFinalRef`) solo descartaba una repeticion EXACTA del ultimo texto final. La captura revelo el patron real: cada reinicio del mic en Android no manda solo la palabra nueva, sino que vuelve a mandar el resultado final COMPLETO de lo dictado hasta ese momento, una palabra mas largo cada vez ("Argentina" → "Argentina 1" → "Argentina 1 por" → "Argentina 1 por 10"), y el codigo lo estaba concatenando entero cada vez en vez de detectar que era una extension del anterior. En `components/CreateOrderModal.js`, `processVoiceFinal` ahora compara el nuevo resultado final contra el anterior (`lastFinalRef`): si es identico lo descarta igual que antes; si el nuevo EMPIEZA con el anterior (mas espacio), es una extension — se calcula la cola nueva con `raw.slice(last.text.length)` y solo esa cola se agrega a la transcripcion y se manda a `parseVoiceFull`, en vez del texto completo repetido. Se probo la logica aislada con la secuencia real de la captura y el resultado da limpio: `Argentina 1 por 10 siguiente abejita 1 por 10 siguiente` (sin duplicacion de "Argentina"/"abejita").
- Verificacion: `npx eslint components/CreateOrderModal.js` sin errores nuevos (mismo warning preexistente de siempre). `CI=true npm run build` OK. Se corrio la logica de `processVoiceFinal` aislada en `node -e` contra la secuencia exacta de chunks que se ve en la captura del usuario, confirmando que ya no duplica. No se pudo probar con microfono real en un Android en este entorno.
- Auditoria: Se releyo el turno anterior en esta misma bitacora (19:15) antes de tocar nada; el fix de ahi quedaba corto porque asumia repeticion exacta, no creciente. `git status --short` antes de empezar solo mostraba los binarios sin trackear ya conocidos.
- Pendiente/Riesgos: El "1" que aparece pegado a "Argentina" en el nombre del diseno (visible en la captura del usuario, ej. "Argentina 1" en vez de "Argentina") no es un artefacto de duplicacion — es contenido que el propio motor de reconocimiento de Android entrego como parte del resultado final, probablemente relacionado con el mismo reinicio del mic (ruido/corte de audio en el limite del reinicio mal interpretado como "1"). Este fix no lo puede corregir porque no hay forma de distinguir desde el codigo un "1" real dictado por el usuario de un artefacto del reconocedor; si vuelve a pasar, revisar si el nombre del diseno queda con numeros sueltos pegados y avisar. El beep del microfono cada ~5s en Android sigue siendo limitacion de plataforma (ver turno anterior, punto 5), no se toco de nuevo en este turno.

## 2026-07-07 19:15 -03:00 - Claude Sonnet 5

- Objetivo: El usuario pidio revisar en detalle la carga de pedidos por voz (Admin > Nuevo pedido) y corregir errores. Primero se hizo solo diagnostico (sin tocar codigo, a pedido explicito); el usuario aprobo corregir los 3 hallazgos y sumo dos reportes propios: la transcripcion mostraba una misma palabra repetida muchas veces (ej. "Argentina Argentina Argentina...") y en el celular el mic suena como si se abriera/cerrara cada ~5 segundos.
- Cambios: **1) Closure obsoleto en "pedido guardar"/"pedido cancelar"** (`components/CreateOrderModal.js`): `createRecognition()` arma los callbacks de `SpeechRecognition` una sola vez al iniciar/reanudar grabacion; mientras el mic sigue grabando sin pausas, esos callbacks quedan atados al closure de ESE render, por lo que `handleSave`/`handleClose` (que leen `rows`/`customerName`/fechas/etc. directo del closure, no de un ref) podian usar datos viejos al ejecutar el comando de voz — en el peor caso, dictar un pedido entero y cerrar con "pedido guardar" podia guardar un pedido vacio o incompleto aunque en pantalla se viera todo cargado. Se agregaron `handleSaveRef`/`handleCloseRef`, actualizados en un `useEffect` sin deps (se refrescan en cada render, mismo patron que `designsRef`/`knownCustomerNamesRef` ya existente en el archivo), y los dos call sites de voz ahora invocan `handleSaveRef.current()`/`handleCloseRef.current()` con un `setTimeout` de 50ms (para darle tiempo a React a aplicar el batch de `setState` previo del mismo comando de voz) en vez de llamar directo a `handleSave`/`handleClose`.
  **2) Fecha con año pegado sin conector** (`lib/voice-order-parser.js`, `parseSpanishDate`): el regex para "N de MES AÑO" solo capturaba el año si iba precedido de "del"/"de" ("2 de julio del 2026"); si se decia "2 de julio 2026" (sin conector, forma muy natural al hablar) el regex igual "matcheaba" pero sin capturar el grupo del año, y el codigo caia silenciosamente al año actual. Se agrego el `\s+` faltante en la segunda alternativa del regex para que tambien capture el año cuando va pegado solo con un espacio.
  **3) Cantidades dictadas con numeros compuestos** (`lib/voice-order-parser.js`, `parseQtyWord`): el diccionario `WORD_TO_NUM` solo tiene palabras sueltas (uno..veintinueve, treinta..noventa, cien..quinientos); cualquier cantidad compuesta de dos palabras como "treinta y dos" (32) o "cuarenta y cinco" (45) no matcheaba nada y la cantidad caia silenciosamente a 1, sin ningun aviso. Se reescribio `parseQtyWord` para partir el texto en palabras, ignorar "y" y sumar cada componente reconocido (treinta+dos=32, ciento+veinte=120, etc.); si alguna palabra no se reconoce devuelve `null` (mismo fallback a 1 que antes) en vez de arriesgar un numero inventado.
  **4) Transcripcion repetida** (`components/CreateOrderModal.js`, `processVoiceFinal`): en Android, aunque `continuous=true`, la sesion de reconocimiento se corta sola cada pocos segundos y el codigo ya la reinicia automaticamente (esto ya estaba asi de una mejora anterior); cada reinicio puede volver a finalizar la misma palabra que se seguia diciendo, duplicandola en el log ("Dictado") y en el pedido. Se agrego un `lastFinalRef` que descarta un resultado final si es identico (normalizado) al inmediato anterior y llego dentro de los 8 segundos siguientes.
  **5) Beep del microfono cada ~5s (no se corrigio, es una limitacion de plataforma)**: se investigo el mecanismo de reinicio (`rec.onend` en `createRecognition`) y esta al limite de lo que se puede hacer desde JS: Android/Chrome no respeta bien `continuous=true` y corta la sesion sola cada varios segundos por su propio VAD interno (no hay parametro del Web Speech API para configurar ese timeout); el codigo ya reinicia lo mas rapido posible (150ms) para minimizar perdida de audio, pero el sonido de "inicio de escucha" lo dispara el propio sistema operativo al invocar `.start()` en cada reinicio, y no hay forma de silenciarlo desde la pagina web. Se le explico esto al usuario en vez de simular un fix que no es real.
- Verificacion: Se probaron ambas funciones de `lib/voice-order-parser.js` de forma aislada con `node -e` (numeros compuestos: "treinta y dos"→32, "ciento veinte"→120, "doscientos treinta y cinco"→235, sigue funcionando "10"→10 y "cinco"→5, entrada invalida→null; fechas: "2 de julio 2026"→año 2026 correcto, ya no cae al año actual, "2 de julio del 2026" y "2 de julio" sin año siguen igual que antes). `npx eslint lib/voice-order-parser.js components/CreateOrderModal.js` sin errores nuevos (1 warning preexistente de deps de `useCallback` en `handleClose`, no tocado en este turno). `CI=true npm run build` completo OK, mismas rutas que antes. No se pudo probar en un dispositivo Android real con microfono en este entorno (sin browser real disponible); la verificacion del reconocimiento de voz en si fue por lectura cuidadosa del codigo + pruebas aisladas de las funciones puras de parseo.
- Auditoria: Se leyeron `AGENTS.md`, `CONTEXT.md` y las ultimas entradas de `AI_RUN_LOG.md` antes de tocar nada; la entrada anterior (ChatGPT Codex, tabla de produccion) ya estaba commiteada (`git log` mostraba un commit "deploy" encima) y no conflictuaba con este cambio. `git status --short` solo mostraba los binarios sin trackear ya conocidos (`Inkora.PrintBridge.zip`, `Messi 2.3mf`) antes de empezar.
- Pendiente/Riesgos: El punto 5 (beep repetido en Android) queda como limitacion conocida de la plataforma, no como bug corregible en este codigo. El fix de deduplicacion (punto 4) usa un umbral fijo de 8 segundos entre finales identicos; si algun dia el reinicio del mic tarda mas que eso en Android, un caso legitimo de repetir la misma palabra sola dos veces seguidas (poco comun) podria perderse una vez — se considero un trade-off aceptable frente al bug real reportado. No se probo de punta a punta con microfono real en celular ni en desktop.

## 2026-07-07 15:51 -03:00 - ChatGPT Codex

- Objetivo: Corregir en produccion la superposicion de los encabezados "A producir" e "Impreso" en la tabla de produccion.
- Cambios: En `components/ProductionTab.js` y `app/operarios/page.js` se agrego ancho minimo a la tabla, se amplio la columna "A producir" y se protegieron los encabezados con `overflow: hidden`/`textOverflow: ellipsis` para evitar que invadan columnas vecinas en paneles angostos.
- Verificacion: `node --check components\ProductionTab.js` OK; `node --check app\operarios\page.js` OK; `npx.cmd eslint components\ProductionTab.js app\operarios\page.js --quiet` OK; `git diff --check` OK con solo aviso CRLF; `npm.cmd run build` OK con warnings preexistentes de `<img>` y deps de hooks; `vercel.cmd deploy --prod --yes` OK, deploy `dpl_FwWiCVxgFK53KkYsEdEUDTygYYWv` READY y aliasado a `https://www.inkora.com.ar`.
- Auditoria: Se leyeron `AGENTS.md`, `CONTEXT.md`, `AI_RUN_LOG.md`, el skill Next.js y `git status --short`. La ultima entrada relevante era de emails y no conflictuaba; el arbol solo tenia sin trackear `Inkora.PrintBridge.zip` y `Messi 2.3mf` antes del cambio.
- Pendiente/Riesgos: No se probo visualmente con sesion admin real; el fix esta publicado en produccion y deberia mostrarse al refrescar la pagina.

## 2026-07-06 11:22 -03:00 - ChatGPT Codex

- Objetivo: Ajustar los botones del email interno de nuevo pedido: usar el logo de INKORA del recurso publico del sitio y mejorar el icono/texto de "Copiar pedido"; revisar si se puede copiar directamente desde el email.
- Cambios: En `app/api/send-email/route.js` se cambio el icono del boton "Ir al pedido" para usar `https://www.inkora.com.ar/icons/icon-192.png` en vez del logo del bucket. Se ajusto el boton "Copiar pedido" para mantener el texto exacto y mostrar un icono de dos cuadrados superpuestos mas reconocible. Se replico el mismo HTML visual en `components/EmailsTab.js` para que la preview coincida.
- Verificacion: `node --check` OK en `app/api/send-email/route.js` y `components/EmailsTab.js`. `npx.cmd eslint app/api/send-email/route.js components/EmailsTab.js --quiet` OK. `git diff --check` OK, solo aviso CRLF. `npm.cmd run build` OK; quedaron warnings preexistentes de `<img>` y deps de hooks en archivos no relacionados.
- Auditoria: Se leyeron `AGENTS.md`, `CONTEXT.md`, `AI_RUN_LOG.md`, el skill de email y `git status --short`. La ultima entrada propia era la mejora de estadisticas ya commiteada/deployada. El arbol solo tenia sin trackear `Inkora.PrintBridge.zip` y `Messi 2.3mf`.
- Pendiente/Riesgos: No se puede copiar al portapapeles directamente desde un email de forma confiable porque los clientes de correo bloquean JavaScript; el boton sigue abriendo `/copiar-pedido`, que intenta copiar automaticamente y deja respaldo manual.

## 2026-07-06 09:35 -03:00 - ChatGPT Codex

- Objetivo: Mejorar Admin > Seguimiento > Estadisticas con metricas realmente utiles, sin agregar filtro Activos/Archivados/Todos, y agregando filtro por cliente.
- Cambios: En `app/admin/page.js` se amplio `StatsTab`: filtro por cliente, facturacion valida excluyendo pedidos cancelados, pedidos validos/cancelados, porcentaje de clientes recurrentes, comparativas ultimos 7/30 dias contra el periodo anterior, pedidos por fuente, ranking por vendedor, embudo basico de actividad, resumen de carritos abandonados y productos mas abandonados. Los rankings de productos/disenos ahora muestran unidades y monto. `TrackingTab` ahora pasa `users` y `carts` a Estadisticas para poder cruzar cliente/carritos.
- Verificacion: `npx.cmd eslint app/admin/page.js --quiet` OK. `git diff --check` OK, solo aviso CRLF. `npm.cmd run build` OK; quedaron warnings preexistentes de `<img>` y dependencias de hooks en archivos no relacionados.
- Auditoria: Se leyeron `AGENTS.md`, `CONTEXT.md`, `AI_RUN_LOG.md` y `git status --short`. La ultima entrada relevante era de Claude sobre modales/carritos; se contrasto que esta tarea toca principalmente `StatsTab` en `app/admin/page.js`. El arbol solo tenia sin trackear `Inkora.PrintBridge.zip` y `Messi 2.3mf`.
- Pendiente/Riesgos: No se agrego SQL. Las estadisticas siguen usando solo `activeOrders`, como antes y como pidio el usuario al excluir el filtro de activos/archivados/todos. El valor estimado de carritos depende de que los items del carrito tengan precio unitario/subtotal guardado.

## 2026-07-06 -03:00 - Claude Sonnet 5 (v20)

- Objetivo: El usuario reporto (con captura) que el modal de confirmacion del nuevo boton "Enviar recordatorio" (Admin > Carritos) mostraba texto de "¿Confirmar eliminación?" con boton rojo "Eliminar" — bug real: se reutilizo el modal generico `askConfirm` de Admin sin notar que tenia el titulo y el label del boton hardcodeados para borrado. Pidio revisar ese bug y "todo lo relacionado". Ademas, en paralelo, pidio una mejora general: que TODAS las ventanas/modales de admin y de la web publica se puedan cerrar con click afuera o con Escape.
- Cambios: **Bug del modal generico**: `askConfirm(message, onConfirm, opts)` en `app/admin/page.js` ahora acepta `opts.title`, `opts.confirmLabel` y `opts.tone` ('danger' por defecto, para no romper ninguno de los ~5 call sites existentes que ya dependian del texto/color rojo de borrado); el render del modal usa esos valores en vez de tenerlos fijos. Se actualizo el call site nuevo (enviar recordatorio) para pedir `{ title: '¿Enviar recordatorio?', confirmLabel: 'Enviar', tone: 'primary' }` (boton azul, no rojo). De paso se encontro y corrigio el mismo tipo de bug ya existente en el boton "Sobrescribir diseños" (mostraba "¿Confirmar eliminación?"/"Eliminar" para una accion que no es un borrado), ahora con su propio titulo/label.
  **Click-afuera + Escape en todas las ventanas**: se audito cada overlay tipo modal en el proyecto (`grep` de `position:fixed + inset:0` en todos los `.js`, mas variantes). En `app/admin/page.js` (~13 modales): la mayoria ya cerraba con click afuera (patron backdrop-onClick + contenido-stopPropagation, o `e.target === e.currentTarget`); se corrigio el unico que faltaba (`confirmReset`, en el sub-panel de actividad). Para Escape, la mayoria NO lo tenia (salvo `productManageModal` y `userProductsModal`, que ya usaban el patron correcto de ref+focus+onKeyDown o su propio listener): se agrego un unico `useEffect` consolidado en el componente principal de Admin que escucha `keydown` global y cierra el modal mas "de encima" segun cual este abierto (`confirmModal` primero, por ser tipicamente una confirmacion secundaria sobre otro modal, y asi hacia abajo: `designPreviewImage`, `showImpersonatorPanel`, `inviteLinksModal`, `infoTagsModal`, `userScaleModal`, `orderDetail`, `versionSnapshotViewer`, `allScalesModalOpen`); para `confirmReset` (que vive en un componente distinto, `AdminActivityHistory`) se agrego un `useEffect` propio ahi mismo. En los componentes standalone: `components/CreateOrderModal.js`, `components/QuickPrintOverlay.js`, `components/PrintQueueOverlay.js` y `contexts/DesignsContext.js` YA tenian ambos comportamientos bien implementados (no se toco nada). Se agrego Escape (faltaba) a `components/AuthModal.js`, `components/chat/ChatReferencePicker.js` y `components/chat/ChatNewChannelModal.js`; en `components/chat/ChatPanel.js` ya existia una cadena de prioridad de Escape para varios estados del chat, se sumo `lightboxImage` (imagen ampliada) a esa misma cadena, con maxima prioridad. En `app/catalogo/page.js`, los 3 overlays (modal principal de producto/pedido, confirmacion de "limpiar carrito", panel de carrito mobile) ya cerraban con click afuera; se agrego un `useEffect` consolidado de Escape (`clearConfirmOpen` > `infoTagsPopup` > `modalOpen` > `cartPanelOpen`, de mas a menos prioritario).
- Verificacion: `npx eslint` sobre los ~9 archivos tocados sin errores (solo warnings preexistentes de `<img>` y de dependencias de hooks en efectos que deliberadamente no incluyen todas las deps, igual que ya hacia el propio codigo existente en casos similares). `npx next build` completo OK, mismas rutas que el turno anterior. Se corrio `next start` en local y se confirmo 200 en `/`, `/catalogo` y `/admin`.
- Auditoria: Se hizo un relevamiento exhaustivo con `grep` de TODOS los overlays con `position:fixed`+`inset:0` (y variantes) en `app/admin/page.js`, `app/catalogo/page.js` y los componentes standalone relevantes, verificando modal por modal el comportamiento real antes de tocar nada (varios ya estaban bien implementados y no se modificaron, para no arriesgar una regresion innecesaria). Se dejaron deliberadamente afuera dos casos que parecen "ventanas" pero no lo son: la pantalla de "Acceso denegado" al simular un usuario no-admin (es un estado bloqueante intencional con boton explicito de salida, no un modal descartable) y el panel fijo de la pestaña "Producción" (es contenido de pestaña a pantalla completa, no un overlay).
- Pendiente/Riesgos: No se pudo probar visualmente en un navegador real en este entorno (sin herramienta de automatizacion de browser); toda la verificacion fue por lectura cuidadosa de codigo mas build/lint. Sigue pendiente correr `sql/cart_reminder_email.sql` en Supabase (ver entrada anterior) para que la funcion de recordatorio de carrito quede operativa.

## 2026-07-05 -03:00 - Claude Sonnet 5 (v19)

- Objetivo: Nueva funcion pedida por el usuario, negociada en varias rondas de diseño antes de implementar: un boton admin en Carritos para mandarle a un cliente logueado con carrito abandonado un email recordatorio con resumen + boton "Retomar pedido"; una preferencia por cliente para recibir o no estos avisos (visible pero no editable por el admin en Clientes); un boton "No volver a recibir" dentro del email; y un toggle en el dashboard del cliente para poder reactivarlo. Decisiones clave del usuario durante el diseño: (1) "Retomar pedido" NO usa magic-link/auto-login por seguridad — es un link plano a `/catalogo`, si el navegador ya tiene sesion el carrito guardado aparece solo (no hace falta reconstruir nada); (2) el boton de desactivar del email no puede ejecutar JS dentro del email (limitacion real de los clientes de correo, explicada y aceptada) — en cambio manda al dashboard del cliente, y ahi recien se desactiva con ~1 segundo de demora para que se alcance a ver la animacion del switch.
- Cambios: `sql/cart_reminder_email.sql` (nuevo) agrega `profiles.cart_reminder_email_enabled boolean DEFAULT true` y actualiza `admin_get_profiles()` para exponerla (mismo patron que `send_confirmation_email`). `app/api/admin/send-cart-reminder/route.js` (nuevo, admin-only via `requireAdmin`) busca el perfil y el carrito del `userId` recibido, re-valida server-side que el cliente no haya desactivado la preferencia (evita condicion de carrera si lo desactivo justo antes de que el admin confirmara), arma una tabla HTML del carrito (con precio si `showPrice` y `pricePerUnit` estan presentes) y manda el email via Resend con dos links con estilo de boton: "Retomar pedido" (`https://www.inkora.com.ar/catalogo`, sin token) y "No volver a recibir estos avisos" (`https://www.inkora.com.ar/dashboard?tab=miperfil&cart_reminder_off=1`). En `app/dashboard/page.js` se agrego el toggle "Recordatorios de carrito" (misma UI de switch que ya se usaba en Admin) en una nueva tarjeta "Preferencias de email", mas la logica de auto-desactivado: se lee `cart_reminder_off=1` de la URL en el primer render (guardado en un ref para no perderlo), y una vez que `loadProfile` confirma que la preferencia sigue activa, se dispara `toggleCartReminder(false, userId)` despues de un `setTimeout` de 1000ms (para que se vea la animacion del switch) y se limpia el parametro de la URL con `replaceState`. En `app/admin/page.js`: `loadUsers()` normaliza el nuevo campo (`!== false`, default activado); en la fila de cada cliente de la pestaña Clientes se agrego una nueva columna de solo lectura "Email retomar pedido" (badge Activado/Desactivado, sin `onClick`, con `title` explicando que el admin no la puede modificar); en `CartsTab` se agrego un boton "Enviar recordatorio" por cada carrito de cliente logueado (no aparece para carritos anonimos, que no tienen email), con confirmacion via el `askConfirm` ya existente, estado local de enviando/enviado, y deshabilitado con tooltip cuando el cliente desactivo la preferencia; se agrego `sendCartReminder(userId)` en el componente principal que llama al nuevo endpoint y trackea la actividad admin.
- Verificacion: `npx eslint app/admin/page.js app/dashboard/page.js app/api/admin/send-cart-reminder/route.js` sin errores (solo warnings preexistentes de `<img>`). `npx next build` OK, con `/api/admin/send-cart-reminder` listada. Se probo en local (`next start`, puerto 3200) que `/`, `/dashboard` y `/admin` responden 200, y que el endpoint nuevo devuelve 401 "No autorizado" ante una llamada sin sesion admin (confirma que `requireAdmin` esta bien enganchado).
- Auditoria: Antes de escribir codigo se investigo a fondo (sin aplicar cambios, con el usuario confirmando el diseño en varias rondas) la tabla `carts` real (no era solo localStorage), el shape exacto de los items de `CartContext`, el patron de toggle ya usado para `send_confirmation_email` (columna Clientes) para no inventar un estilo nuevo, y el patron de auth de rutas admin (`requireAdmin` + cookies, sin necesidad de pasar el token a mano) para no reinventar nada ahi tampoco. `git status` antes de commitear solo mostraba los archivos de esta funcion.
- Pendiente/Riesgos: **Requiere ejecutar `sql/cart_reminder_email.sql` en el SQL Editor de Supabase antes de que la funcion ande de punta a punta** — hasta entonces, el boton "Enviar recordatorio" va a fallar (la columna no existe todavia para el `SELECT` del endpoint) y el toggle del dashboard puede fallar su guardado silenciosamente (el `UPDATE` a una columna inexistente); el resto de la UI se degrada de forma segura (todo se ve como "Activado" por defecto) pero no hay que asumir que la funcion esta operativa sin correr esa migracion primero. El link "Retomar pedido" no reconstruye el carrito si el cliente no tiene sesion iniciada en ese navegador — eso fue una decision explicita del usuario (prefirio simplicidad y seguridad a costa de eso). No se probo el envio real de un email de prueba en este turno (a diferencia de otras funciones anteriores) para no generarle a un cliente real un email de carrito que no le corresponde.

## 2026-07-05 -03:00 - Claude Sonnet 5 (v18)

- Objetivo: En la pantalla de "Pedido confirmado", el overlay de "¡Copiado!" del codigo de pedido usaba el mismo verde que el de WhatsApp; el usuario pidio diferenciarlos, dejando el codigo de pedido en azul y el de WhatsApp en verde (sin tocar).
- Cambios: En `app/catalogo/page.js`, el overlay `.copied-overlay` del codigo de pedido (linea ~2261) paso de `rgba(24,163,106,0.6)` (verde) a `rgba(45,107,228,0.6)` (el mismo azul `#2D6BE4` ya usado en el resto del sitio para acciones primarias). El overlay de "Copiado al portapapeles" del recuadro de WhatsApp (linea ~2306) se dejo sin cambios, sigue en verde.
- Verificacion: `npx eslint app/catalogo/page.js` sin errores (solo el warning preexistente de `cachedUrl`). `npx next build` OK.
- Auditoria: N/A, cambio de un solo color en un archivo.
- Pendiente/Riesgos: Ninguno esperado.

## 2026-07-05 -03:00 - Claude Sonnet 5 (v17)

- Objetivo: El usuario pidio que Codex hiciera los cambios de "Ir al pedido" + "Copiar pedido" (ver entrada de abajo), pero Codex no pudo commitear ni deployar por un limite de permisos de escritura en `.git` de su entorno. Se pidio revisar, verificar y completar el commit + deploy de ese trabajo (que estaba correcto pero sin subir).
- Cambios: Se reviso el diff completo de Codex (`app/api/send-email/route.js`, `app/catalogo/page.js`, `components/EmailsTab.js`, `lib/order-confirmation-message.js` nuevo, `app/copiar-pedido/page.js` nuevo) antes de commitear. Se encontro y corrigio un detalle: al delegar `buildWhatsAppConfirmationMessage` de `app/catalogo/page.js` hacia el nuevo helper compartido, Codex dejo un `return` temprano seguido de ~30 lineas de la implementacion vieja ahora inalcanzable (dead code); se elimino esa funcion local por completo y se importa/usa directamente `buildWhatsAppConfirmationMessage` desde `lib/order-confirmation-message.js` en el unico call site (linea del boton de WhatsApp del modal de confirmacion). Se confirmo que el total que usa "Copiar pedido" en el email admin (`copyMessageTotal = Number(total) || emailTotal`) es consistente con lo que ya manda el propio cliente en su boton de WhatsApp (`confirmedOrder.total`, sin condicionar a `showPrice`), asi que no se introdujo ninguna fuga de precio nueva: el email admin solo replica exactamente el mismo texto que el cliente ya podia generar el.
- Verificacion: `npx eslint` sobre los 5 archivos (incluyendo el catalogo ya limpio) sin errores, solo el warning preexistente de `cachedUrl` en un `useEffect` no relacionado. `npx next build` completo OK, con `/copiar-pedido` listada como ruta nueva generada. Se probo en aislado el output de `buildWhatsAppConfirmationMessage` con `node --input-type=module` contra un pedido de ejemplo (formato identico al anterior). Se corrio `next start` en local (puerto 3200) y se confirmo 200 en `/copiar-pedido?texto=...`, `/catalogo` y `/admin`.
- Auditoria: Se comparo el diff de Codex contra la entrada que el mismo dejo en la bitacora (mas abajo) para confirmar que coincidian; el unico hallazgo fue el dead code ya corregido. `git status` antes de commitear solo mostraba los archivos de este cambio (mas los binarios sin trackear ya conocidos).
- Pendiente/Riesgos: Igual que lo que ya anticipo Codex: el copiado automatico en `/copiar-pedido` puede fallar en navegadores que bloqueen `navigator.clipboard` fuera de un gesto directo del usuario al abrir el link desde el cliente de email — para eso ya queda visible el textarea + boton manual de "Copiar pedido" como respaldo. No se pudo probar visualmente el email real (renderizado en un cliente de correo) en este entorno, solo el build y el HTML generado.

## 2026-07-05 22:11 -03:00 - ChatGPT Codex

- Objetivo: Ajustar el email interno de pedido: cambiar "Ver pedido" por "Ir al pedido" con icono de INKORA y agregar "Copiar pedido" con icono de copiar, usando el mismo texto de confirmacion por WhatsApp.
- Cambios: En `app/api/send-email/route.js` se cambio el bloque de acciones del email admin: boton primario "Ir al pedido" con mini logo y boton secundario "Copiar pedido" con icono de doble cuadrado. Como los clientes de email no permiten copiar al portapapeles con JavaScript dentro del email, el boton nuevo abre `/copiar-pedido` con el texto ya armado; esa pagina intenta copiar automaticamente y tambien deja un boton manual. Se agrego `app/copiar-pedido/page.js` y `lib/order-confirmation-message.js` para centralizar el texto tipo WhatsApp; `app/catalogo/page.js` tambien delega en ese helper para que cliente y email usen el mismo formato. Se actualizo `components/EmailsTab.js` para que la preview muestre ambos botones.
- Verificacion: `node --check` OK en `app/api/send-email/route.js`, `app/copiar-pedido/page.js`, `components/EmailsTab.js` y `lib/order-confirmation-message.js`. `npx.cmd eslint app/api/send-email/route.js app/copiar-pedido/page.js components/EmailsTab.js lib/order-confirmation-message.js --quiet` OK. `npm.cmd run build` OK; la ruta `/copiar-pedido` aparece generada. Solo quedaron warnings preexistentes de `<img>` y deps de hooks en otros archivos.
- Auditoria: Se leyeron `AGENTS.md`, `CONTEXT.md`, `AI_RUN_LOG.md` y `git status --short`. La ultima entrada relevante era la limpieza del secret del webhook de chat, sin conflicto con emails. Permanecen sin trackear `Inkora.PrintBridge.zip` y `Messi 2.3mf`.
- Pendiente/Riesgos: El copiado automatico puede ser bloqueado por algunos navegadores al abrir desde email; por eso la pagina deja visible el textarea y boton "Copiar pedido". El texto via URL puede crecer si el pedido tiene muchisimos items; para pedidos normales deberia funcionar bien.

## 2026-07-05 20:53 -03:00 - ChatGPT Codex

- Objetivo: Revisar y corregir la alerta de GitGuardian por un `Generic High Entropy Secret` expuesto en GitHub.
- Cambios: Se confirmo que el candidato real era el `x-webhook-secret` hardcodeado en `sql/chat_push_notifications.sql` para el webhook de push del chat. Se elimino el secret real del SQL versionado. Ahora `notify_chat_message_webhook()` lee el valor desde `private.app_secrets` (schema privado, sin grants a `anon/authenticated`) y omite solo el webhook si falta configurar el secret, sin romper el insert del mensaje. Se agrego `sql/chat_webhook_secret_rotation.sql` como script de rotacion: crea/asegura la tabla privada, guarda el nuevo secret y recrea la funcion/trigger sin hardcodear el valor.
- Verificacion: `rg` confirmo que el valor filtrado ya no aparece en archivos actuales del repo. `git diff --check` OK, solo aviso CRLF. No se ejecuto build porque el cambio fue SQL/documentacion, sin JS runtime.
- Auditoria: Se leyeron `AGENTS.md`, `CONTEXT.md`, `AI_RUN_LOG.md` y `git status --short`. La ultima entrada relevante era de Claude sobre mantenimiento/402, sin conflicto. El arbol tenia solo los binarios sin trackear ya conocidos (`Inkora.PrintBridge.zip`, `Messi 2.3mf`).
- Pendiente/Riesgos: Falta rotar el valor real en produccion: generar un secret nuevo, guardarlo como `CHAT_WEBHOOK_SECRET` en Vercel Production y ejecutar `sql/chat_webhook_secret_rotation.sql` en Supabase SQL Editor con ese mismo valor. No se cambio Vercel desde esta corrida para no desincronizarlo de Supabase y romper temporalmente las push del chat. El secret viejo debe considerarse comprometido aunque se haya quitado del archivo actual, porque sigue existiendo en el historial de Git.

## 2026-07-05 -03:00 - Claude Sonnet 5 (v16)

- Objetivo: Corregir 5 detalles reportados por el usuario tras probar el sistema de mantenimiento/402 del turno anterior: (1) agregar el icono de WhatsApp al boton de la pantalla de mantenimiento/error; (2) el mensaje prellenado de WhatsApp no debe mencionar "problema tecnico" cuando es por mantenimiento; (3) al activarse para un usuario activo, la pagina se sentia como si "se reiniciara" en vez de solo aparecer el aviso; (4) confirmado un bug real: navegar de landing a catalogo (u otra seccion) disparaba el bloqueo instantaneo como si fuera una recarga, cuando no deberia; (5) pedido nuevo: si el usuario esta viendo la pantalla de mantenimiento y esta se desactiva, que se le recargue solo para que vea la pagina real actualizada.
- Cambios: En `components/ServiceUnavailable.js` se agrego el mismo SVG de WhatsApp ya usado en el boton de confirmacion de pedido de `app/catalogo/page.js` (para no inventar un icono nuevo) dentro del boton, y se separo el texto prellenado por variante: para `'maintenance'` dice "vi que la pagina esta en mantenimiento" (sin mencionar problema tecnico), para `'error'` sigue mencionando un problema tecnico (caso realista, es un 402). El fix de (3) y (4) requirio rediseñar `components/MaintenanceGate.js` de fondo: se descubrio que TODA la navegacion interna del sitio (`components/Header.js`, `app/dashboard/page.js`) usa `<a href>` planas en vez de `next/link`, es decir que ir de landing a catalogo YA es tecnicamente una recarga completa del documento — por eso antes no se podia distinguir "cambio de seccion" de "F5 real" con solo mirar si el componente seguia montado. La solucion fue usar la Navigation Timing API (`performance.getEntriesByType('navigation')[0].type === 'reload'`) para detectar un reload real, combinada con un flag en `sessionStorage` (`inkora_session_active`) que se lee ANTES de escribirse en cada carga: si ya estaba en `true` (la pestaña ya habia visto una pagina antes, sin importar si fue reload o navegacion) Y no es un reload real, se trata como "sesion ya activa" y NO se bloquea instantaneo aunque el mantenimiento ya este programado (se muestra el cartel con cuenta regresiva normal); si es un reload real, o es la primera carga de la pestaña con el mantenimiento ya activo, se bloquea directo. Para (3), se cambio el render de "reemplazar `children` por la pantalla" a "mantener `children` montado (sin destruir su estado) y superponer `ServiceUnavailable` encima con `position:fixed`" tanto para el caso de mantenimiento como para el 402. Para (5), se agregaron refs (`activatesAtMsRef`, `freshLoadBlockedRef`) para poder comparar en cada poll (cada 15s) si el estado "bloqueado" pasa de `true` a `false` (es decir, se desactivo el mantenimiento mientras el usuario lo estaba viendo) y en ese caso especifico se llama a `window.location.reload()` una sola vez.
- Verificacion: `npx eslint components/MaintenanceGate.js components/ServiceUnavailable.js` sin errores ni warnings. `npx next build` completo OK. Se corrio `next start` en local (puerto 3200) y se confirmo que `/`, `/catalogo` y `/admin` siguen respondiendo 200 con el nuevo `MaintenanceGate`.
- Auditoria: Se releyo `components/Header.js` y `app/dashboard/page.js` para confirmar el patron real de navegacion del sitio antes de asumir que el enfoque anterior (basado solo en si el componente seguia montado) era suficiente — no lo era, de ahi el cambio de diseño. `git status` antes de commitear solo mostraba los dos archivos tocados.
- Pendiente/Riesgos: Sigue sin poder probarse visualmente en un navegador real en este entorno (sin herramienta de automatizacion de browser); la logica de reload/sessionStorage/Navigation Timing API se razono con cuidado paso a paso pero conviene que el usuario la prueba a mano una vez en produccion (activar servicio tecnico, navegar entre secciones sin recargar, despues F5, despues desactivar) para confirmar el comportamiento exacto. La Navigation Timing API es estandar y esta soportada en todos los navegadores modernos (Chrome/Edge/Firefox/Safari actuales), pero si algun navegador viejo no la soporta, `isHardReload()` devuelve `false` de forma segura (no rompe, simplemente ese caso puntual dependeria solo de `sessionStorage`). Sigue pendiente la correccion de RLS y la decision sobre el dominio en el proyecto viejo de Vercel.

## 2026-07-05 -03:00 - Claude Sonnet 5 (v15)

- Objetivo: Ajustar dos cosas reportadas por el usuario tras el turno anterior (sistema de mantenimiento/402): (1) en Admin > Config, el estado de "servicio tecnico" no se notaba lo suficiente y los botones de activar seguian clickeables aunque ya hubiera uno programado/activo; (2) la regla de cuenta regresiva para usuarios activos estaba mal: debia activarse recien a los N minutos SOLO si la pestaña se mantiene abierta sin recargar ni volver a cargar el documento — un F5/recarga manual durante la ventana de mantenimiento debe bloquear al instante (como un usuario "recien llegado"), pero navegar entre secciones sin recargar (landing/catalogo, via router de Next) no debe disparar el bloqueo instantaneo. Ademas, de paso, se arreglo el menu superior de tabs de Admin que se cortaba a la derecha sin ninguna pista de que se podia scrollear.
- Cambios: En `app/admin/page.js`, la tarjeta de "Servicio tecnico (mantenimiento)" ahora muestra un banner de estado con color e icono bien visibles (verde/inactivo, amarillo/programado, rojo/activo) en vez de una linea de texto gris chica; los botones "Activar servicio tecnico" y "Activar al instante" se deshabilitan (`disabled`, gris, `cursor:not-allowed`, con `title` explicando por que) mientras ya haya algo programado o activo, y el boton de desactivar paso a ser prominente (verde, `Desactivar servicio tecnico`) y solo aparece en ese mismo caso. En `components/MaintenanceGate.js` se agrego la distincion entre "carga fresca" y "sesion ya abierta": con un `useRef` (`firstFetchDoneRef`) se marca si, en el PRIMER poll tras montar el componente (que solo ocurre en una carga de documento nueva: recarga manual, pestaña nueva, o navegar desde una ruta exenta como /admin), ya habia un `maintenance_activates_at` seteado — en ese caso se bloquea directo con la pantalla de mantenimiento sin esperar el resto de la cuenta regresiva (`freshLoadBlocked`). Si en cambio el valor aparece por primera vez en un poll POSTERIOR al primero (se programo mientras la pestaña ya estaba abierta, sin recarga de por medio), se sigue mostrando el cartel con cuenta regresiva como antes y recien se bloquea cuando esta llega a cero. Como el componente vive en el layout raiz y no se remonta al navegar entre rutas no exentas (landing/catalogo) via el router de Next, el estado persiste correctamente entre esas navegaciones. Tambien se agrego que si se desactiva el mantenimiento desde Admin mientras alguien ya esta bloqueado, se libera solo en el siguiente poll (sin recargar). Para el menu de tabs (`app/admin/page.js`), se agrego un ref (`tabsScrollRef`) al contenedor `.adm-tabs`, un estado `tabScrollState` que detecta si hay contenido oculto a izquierda/derecha (listener de `scroll` + `resize`), y dos botones flecha superpuestos (con gradiente para no tapar bruscamente) que aparecen solo cuando corresponde y hacen `scrollBy` suave de 220px.
- Verificacion: `npx eslint app/admin/page.js components/MaintenanceGate.js` sin errores (solo warnings preexistentes de `<img>`). `npx next build` completo OK, mismas rutas que el turno anterior.
- Auditoria: Se releyo el propio `MaintenanceGate.js` del turno anterior antes de tocarlo para entender bien por que la version previa bloqueaba apenas se cumplia el timestamp sin importar si la pestaña ya estaba abierta o no (le faltaba distinguir "primer poll" de "poll posterior"). `git status` antes de commitear solo mostraba estos dos archivos modificados, sin cambios de otros agentes de por medio.
- Pendiente/Riesgos: Igual que el turno anterior, no se pudo verificar visualmente en un navegador real (sin herramienta de browser automation en este entorno) que el cartel de cuenta regresiva se comporte exactamente asi en vivo; se valido por lectura cuidadosa de la logica de estado. El scroll del menu de tabs tampoco se probo visualmente, solo se confirmo que el build no rompe y que el codigo sigue el mismo patron ya usado en otros lados del archivo (`.adm-tabs` con overflow-x ya existente). Sigue pendiente la correccion de RLS y la decision sobre el dominio en el proyecto viejo de Vercel.

## 2026-07-05 -03:00 - Claude Sonnet 5 (v14)

- Objetivo: Implementar el sistema de resiliencia ante el 402 de Supabase (Fair Use Policy / cuota excedida) discutido en turnos anteriores: (1) detectar el 402 y mostrar una pantalla propia en vez del error crudo, (2) email de alerta a inkorashop@gmail.com ante ese error u otros graves, con throttling para no saturar, (3) modo "servicio tecnico" activable manualmente desde Admin, con cartel de cuenta regresiva para usuarios activos y bloqueo instantaneo para los que recien cargan la pagina. Se descarto a pedido explicito del usuario la idea de "simular" el error 402 (solo queda el toggle manual).
- Cambios: `lib/error-alert.js` (nuevo) expone `notifyOpsError({source, message, details})`: manda un email via Resend directo a `process.env.EMAIL`, con cooldown en memoria de 10 min por `source` para no saturar si un error se repite en loop; no depende de Supabase para nada, asi funciona aunque Supabase este caido. `app/api/notify-error/route.js` (nuevo) expone esto via POST para que el cliente (browser) tambien pueda dispararlo. `lib/supabase.js` ahora crea el cliente con un `fetch` custom (`global: { fetch }`) que detecta `status === 402`, dispara un `CustomEvent('inkora:supabase-402')` en `window` y llama a `/api/notify-error` (con su propio cooldown de 5 min en el browser, capa extra sobre el del servidor). `components/ServiceUnavailable.js` (nuevo) es la UNICA pantalla reutilizable (variant `'error'` o `'maintenance'`) pedida por el usuario; resuelve el numero de WhatsApp con la MISMA logica exacta que ya existia en `app/catalogo/page.js` (telefono del vendedor asignado del perfil, si no `3765211017` por defecto), haciendo su propio fetch de `auth.getUser()` + `profiles` con manejo de error silencioso (si Supabase esta caido, cae solo al numero por defecto). `components/MaintenanceGate.js` (nuevo) envuelve toda la app desde `app/layout.js`: no hace nada en `/admin`, `/operarios`, `/produccion` (quedan exentas); en el resto, escucha el evento de 402 (bloqueo inmediato con la pantalla) y hace polling cada 15s a `settings.maintenance_activates_at` — si ya paso, bloquea con la pantalla de mantenimiento (esto cubre tanto "activacion instantanea" como el momento en que se cumple la cuenta regresiva); si es una fecha futura, muestra un cartel superior fijo con los minutos restantes (recalculado en cada poll) sin bloquear la pagina. En `app/admin/page.js` se agrego una tarjeta nueva en Config con: input de minutos de aviso (`maintenance_minutes`, persistido igual que otros settings numericos ya existentes), boton "Activar servicio tecnico" (confirm simple, guarda `maintenance_activates_at = ahora + N minutos`), boton "Activar al instante" (con confirmacion doble via `window.confirm` encadenados, ya que es mas disruptivo), boton "Desactivar" (solo visible si hay algo activo/programado, limpia el setting) y un texto de estado actual. En `app/api/send-email/route.js` se engancho `notifyOpsError` en el fallo del email de aviso al admin (el caso mas grave: si eso falla, nadie se entera del pedido nuevo).
- Verificacion: `npx eslint` sobre todos los archivos nuevos/tocados, sin errores (solo warnings preexistentes de `<img>` en otras partes de `admin/page.js`). `npx next build` completo OK, `/api/notify-error` aparece listado como ruta dinamica. Se corrio `next start` en local (puerto 3200) y se probo en caliente: `POST /api/notify-error` con un `source` de prueba devolvio `{"ok":true}` (email real enviado, primera llamada); una segunda llamada inmediata con el mismo `source` devolvio `{"ok":false,"skipped":true,"reason":"throttled"}`, confirmando el cooldown. Se confirmo que `/`, `/catalogo` y `/admin` siguen respondiendo 200 con `MaintenanceGate` montado en el layout raiz.
- Auditoria: Se leyeron `AGENTS.md`/`CONTEXT.md`/bitacora antes de empezar; el `git status` mostraba el arbol limpio salvo binarios sin trackear ya conocidos (`Inkora.PrintBridge.zip`, `Messi 2.3mf`) — no hubo conflicto con la entrada de Codex de arriba (Produccion/mobile, archivos distintos). No se pudo probar visualmente en navegador real el cartel de cuenta regresiva en vivo (sin herramienta de browser automation disponible en este entorno); la logica se valido por lectura de codigo y por las pruebas de red/API descriptas arriba.
- Pendiente/Riesgos: Falta validar visualmente en un navegador real que el cartel superior decrezca sin refrescar y que la pantalla de mantenimiento efectivamente reemplace el contenido al llegar a 0 (recomendado probar activando "al instante" desde Admin en un entorno de staging/produccion con cuidado, ya que afecta a usuarios reales). El polling de 15s implica que el bloqueo tras "activar al instante" puede demorar hasta ~15s en aparecer en pestañas ya abiertas, no es literalmente instantaneo para esas pestañas (si es instantaneo para quien recien carga la pagina). El cooldown de `notifyOpsError` es en memoria del proceso serverless: sobrevive bien dentro de una misma instancia "tibia" pero se resetea en un cold start, por lo que en un caso raro podria mandarse un email un poco antes de los 10 minutos tras un reinicio de funcion — se acepto como limitacion razonable para no sumar infraestructura nueva (Redis/KV) solo para esto. Sigue pendiente, sin resolver, la decision sobre sacar `www.inkora.com.ar` del proyecto viejo `inkora` en Vercel, y sigue diferida (a pedido del usuario) la correccion de los 4 hallazgos CRITICAL de RLS deshabilitado en `orders`/`products`/`designs`/`admins`.

## 2026-07-05 19:42 -03:00 - ChatGPT Codex

- Objetivo: Mejorar el responsive mobile de Produccion, confirmar que la PWA respete el orden de tabs configurado en Admin y que las tabs principales tengan iconos con menos separacion en mobile.
- Cambios: En `components/ProductionTab.js` se ajusto Produccion para mobile: contenedor con altura/overflow flexible, sub-tabs con scroll horizontal compacto, tarjetas superiores de acceso/bridge en columna cuando corresponde, header del pedido y controles sin solaparse, resumen en grilla 2x2 y tabla de tareas con ancho minimo para evitar columnas pisadas. Se audito `app/admin/page.js` y ya estaba en `HEAD` con iconos SVG simples en tabs principales, menor espaciado mobile y sincronizacion del orden de tabs via `settings.admin_tab_order` + migracion desde `localStorage`.
- Verificacion: `npx.cmd eslint app/admin/page.js components/ProductionTab.js --quiet` OK. `npm.cmd run build` OK, con warnings preexistentes de `<img>` y dependencias de hooks en archivos no relacionados.
- Auditoria: Se leyeron `AGENTS.md`, `CONTEXT.md`, `AI_RUN_LOG.md` y `git status --short`; el arbol solo tenia sin trackear `Inkora.PrintBridge.zip` y `Messi 2.3mf`. Se reviso la entrada anterior de Claude sobre emails y no habia conflicto con Produccion/Admin tabs.
- Pendiente/Riesgos: Validar visualmente en un celular/PWA real que el scroll horizontal de la tabla de tareas sea comodo; no se tocaron las cantidades ni la logica de impresion.

## 2026-07-05 -03:00 - Claude Sonnet 5 (v13)

- Objetivo: Agregar un boton "Ver pedido" al email de notificacion interna (el que llega a inkorashop@gmail.com por cada pedido nuevo) que lleve directo al detalle de ese pedido en Admin. Si quien lo abre no tiene sesion de admin, el login normal de la app se encarga de pedirselo (no hizo falta nada especial para eso).
- Cambios: Se encontro que `app/admin/page.js` ya tenia un esquema de deep-link para pedidos (`?tab=pedidos&modal=pedido&pedido=<id_o_codigo>`, usado internamente por `buildAdminUrlFromState`/`applyAdminUrlFromLocation` para compartir/persistir el link a un pedido abierto) y que `findOrderFromAdminUrl` matchea tanto por `order.id` como por `order.order_code` — asi que se pudo usar directamente el `orderCode` (ya disponible en el payload de `/api/send-email`) sin tener que buscar el uuid interno del pedido. En `app/api/send-email/route.js` se agrego `orderAdminUrl` (`https://www.inkora.com.ar/admin?tab=pedidos&modal=pedido&pedido=${encodeURIComponent(orderCode)}`) y un boton HTML centrado ("Ver pedido") insertado en `defaultAdminHtml` entre los datos del pedido y la tabla de items. A proposito **no** se agrego al email del cliente (`defaultClientHtml`), solo al del admin, que es lo que se pidio.
- Verificacion: `npx eslint`/`npx next build` sin errores; se probo la construccion de la URL con `node -e` usando un codigo de pedido de ejemplo para confirmar el formato exacto.
- Auditoria: Se leyo con cuidado el mecanismo existente de URL del admin (`TAB_SLUGS`, `SLUG_TABS`, `applyAdminUrlFromLocation`, `findOrderFromAdminUrl`) antes de escribir la URL a mano, para no inventar un formato de parametros que no coincidiera con lo que el admin realmente sabe leer — se confirmo que el slug de tab correcto es `pedidos` (espaniol, `TAB_SLUGS.orders === 'pedidos'`), no `orders`, y que no hace falta pasar `vista` porque el codigo ya la infiere solo segun si el pedido esta archivado.
- Pendiente/Riesgos: Ninguno esperado — reutiliza un mecanismo de navegacion ya probado en el propio admin, no se invento logica nueva de routing.

- Objetivo: Se le paso el resumen del fix anterior (email de pedidos) a otra IA para revision independiente. Confirmo los 6 puntos preguntados, pero encontro 2 cosas incompletas: (1) el fix de locale (`es-AR`) solo se habia aplicado al total del mensaje de WhatsApp, quedaban 8 lugares mas en el catalogo mostrando dinero con `.toLocaleString()` sin locale; (2) `i.qty` no se escapaba en el HTML del email (bajo riesgo en el flujo normal porque siempre es un numero ya normalizado, pero si alguien llama la API a mano con un payload raro podria inyectar HTML via cantidad).
- Cambios: En `app/catalogo/page.js` se verifico con grep que habia exactamente 8 instancias de `.toLocaleString()` sin argumento (precio unitario, precio de item en carrito, total desktop/mobile) y se corrigieron todas a `.toLocaleString('es-AR')` con un solo reemplazo global, verificando antes que no hubiera ningun otro uso de `.toLocaleString()` en el archivo que no fuera dinero (los otros 3 usos ya tenian `'es-AR'` explicito de antes). En `app/api/send-email/route.js`, dentro de `buildTable()`, se agrego `const safeQty = escapeHtml(i.qty);` y se uso en los dos `<td>` que mostraban `i.qty` directo.
- Verificacion: `npx eslint`/`npx next build` sin errores; se re-listaron todos los `toLocaleString` del archivo despues del cambio para confirmar que quedaron los 10 (8 nuevos + 2 que ya estaban bien) todos con `'es-AR'` y ninguno se duplico ni se rompio por el reemplazo global.
- Auditoria: La segunda IA confirmo los 6 puntos de verificacion pedidos (el throw queda dentro del try correcto, el catch interno del email aisla bien, el contrato `clientEmailSent` coincide entre frontend/backend, el escape de HTML esta completo salvo qty, y confirmo independientemente que `applyTemplate` no esta definida). Sobre `applyTemplate`: sigue sin tocarse a proposito, fuera de alcance (ver entrada anterior).
- Pendiente/Riesgos: Ninguno esperado. Sigue pendiente, sin resolver, la decision sobre sacar `www.inkora.com.ar` del proyecto viejo `inkora` en Vercel.

## 2026-07-05 -03:00 - Claude Sonnet 5 (v11)

- Objetivo: Corregir con cuidado los hallazgos confirmados de una auditoria de emails (pedido/admin/whatsapp), excluyendo a proposito el bug de agregacion de cantidades por variante (ya conocido, deprioritizado por el usuario).
- Cambios: En `app/catalogo/page.js` (`submitOrder`): el insert de `orders` ahora revisa `{ error }` y lo tira si falla, para que el catch general lo atrape y NO se mande el email ni se muestre "confirmado" si el pedido no se guardo de verdad (antes seguia de largo en silencio). El fetch a `/api/send-email` quedo en su propio try/catch interno: si el pedido ya se guardo pero el email falla (red o respuesta no-ok), se registra un evento de analytics (`order_email_failed` / `order_client_email_failed`) en vez de bloquear la confirmacion al cliente (el pedido es el dato real, el email es un aviso). Se corrigio tambien `total.toLocaleString()` sin locale en el mensaje de WhatsApp, que dependia del locale del navegador del cliente — ahora usa `'es-AR'` explicito como en el resto del sitio. En `app/api/send-email/route.js`: se agrego `escapeHtml()` y se aplico a todo dato que viene del formulario del cliente (nombre, telefono, email, notas, nombre de vendedor, nombre de producto/diseno) antes de insertarlo en el HTML de los emails (admin y cliente), sin tocar el CSV adjunto (que ya tiene su propio escape correcto) ni los fragmentos de HTML ya generados por el propio codigo (tabla, seccion de total). El envio del email de confirmacion al cliente ahora revisa la respuesta de Resend (antes no la revisaba, podia fallar en silencio); si falla, no se devuelve error 500 (el email al admin, mas critico, ya se mando bien), sino que se informa `clientEmailSent:false` en la respuesta para que el front lo pueda registrar.
- Verificacion: `npx eslint`/`npx next build` sin errores; se probo `escapeHtml` de forma aislada con `node -e` contra casos con `< > & " '`, null y undefined.
- Auditoria: Se hizo una revisión independiente completa antes de corregir (sin mirar primero un diagnostico de otra IA que el usuario habia compartido, para no sesgarse) y se confirmaron los 5 hallazgos de esa revisión previa como correctos, mas 1 hallazgo propio no detectado antes (el locale de WhatsApp). Se detecto en paralelo que "ChatGPT Codex" habia commiteado justo antes (`3682685`, ver entrada de abajo) un cambio a este mismo archivo (sacar la carga de plantillas custom); se verifico con `git diff HEAD` que mis cambios quedaron limpiamente encima de ese commit, sin pisar nada.
- Pendiente/Riesgos: **No se toco** el bug de agregacion de cantidades por variante en `getUnitPrice` (por pedido explicito del usuario). El commit de Codex de este mismo dia dejo llamadas a una funcion `applyTemplate(...)` que ya no esta definida en el archivo (la removieron junto con `loadEmailTemplates`) — hoy es codigo muerto inofensivo porque `customTemplates` esta hardcodeado a `{}` y esas ramas nunca se ejecutan, pero si en el futuro se repuebla `customTemplates` sin restaurar `applyTemplate`, cualquier pedido tiraria un 500. No lo toque por estar fuera del alcance de lo pedido en este turno; queda como nota para quien retome ese tema. Sigue pendiente, sin resolver, la decision sobre sacar `www.inkora.com.ar` del proyecto viejo `inkora` en Vercel.

---

## 2026-07-05 18:51 -03:00 - ChatGPT Codex

- Objetivo: Quitar la edicion de plantillas de email desde Admin y dejar una pestaña solo de vista previa de los formatos de emails.
- Cambios: Se reemplazo `components/EmailsTab.js` por un visor de solo lectura con previews de pedido interno, confirmacion de pedido al cliente, confirmacion de cuenta y reset de contraseña. En `app/api/send-email/route.js` se dejo de cargar plantillas custom desde `settings`, para que los emails de pedido usen el formato fijo del codigo aunque existan configuraciones viejas guardadas.
- Verificacion: `node --check components/EmailsTab.js` OK; `node --check app/api/send-email/route.js` OK; `git diff --check` OK con aviso CRLF; `npm.cmd run build` OK con warnings preexistentes.
- Auditoria: Se leyeron `AGENTS.md`, `CONTEXT.md`, `AI_RUN_LOG.md` y `git status --short`. La ultima entrada relevante era de Claude sobre ajustes visuales en Catalogo, sin conflicto con Emails. Se detecto que `components/EmailsTab.js` existia localmente pero no estaba trackeado, por eso se incluye como archivo nuevo de este turno.
- Pendiente/Riesgos: Probar manualmente Admin > Emails en desktop/mobile y confirmar que la vista comunica correctamente que los emails de Supabase Auth se administran fuera del admin.

Formato obligatorio:

## 2026-07-05 -03:00 - Claude Sonnet 5 (v10)

- Objetivo: El usuario no notaba el efecto de transparencia del overlay de "copiado" (85% opacidad + blur 5px resultaba demasiado sutil en la practica, casi indistinguible de solido).
- Cambios: En `app/catalogo/page.js` se bajo la opacidad de 0.85 a 0.6 y el desenfoque de 5px a 2px en ambos overlays (codigo del pedido y recuadro de WhatsApp), para que el texto de atras se note bastante mas. Se agrego `textShadow` sutil al texto blanco de confirmacion para mantenerlo legible a pesar de la menor opacidad de fondo.
- Verificacion: `npx eslint`/`npx next build` sin errores; se confirmo con `curl` sobre el bundle desplegado que el cambio anterior (0.85/5px) SI estaba en produccion antes de este ajuste — no era un problema de deploy, era que el efecto resultaba demasiado sutil a simple vista.
- Auditoria: N/A.
- Pendiente/Riesgos: Ninguno esperado. Sigue pendiente, sin resolver, la decision sobre sacar `www.inkora.com.ar` del proyecto viejo `inkora` en Vercel (ver entrada anterior) — se sigue reforzando cada deploy con `vercel --prod` manual ademas del push a git por las dudas.

- Objetivo: Ajustar el overlay de "copiado" para que sea semi-transparente (no solido) y se siga viendo el texto de atras, en vez de taparlo del todo.
- Cambios: En `app/catalogo/page.js`, el fondo del overlay (`.copied-overlay`, tanto en el codigo del pedido como en el recuadro de WhatsApp) paso de `background:'#18a36a'` solido a `background:'rgba(24,163,106,0.85)'` + `backdropFilter:'blur(5px)'` (con prefijo `WebkitBackdropFilter` para Safari). El texto de atras queda visible pero desenfocado, sin competir en nitidez con el texto blanco de confirmacion que va encima.
- Verificacion: `npx eslint`/`npx next build` sin errores.
- Auditoria: N/A (ajuste de diseno discutido y acordado con el usuario antes de implementar, incluyendo la alternativa de solo-opacidad que se descarto por legibilidad).
- Pendiente/Riesgos: Se detecto en el turno anterior que `www.inkora.com.ar` esta asignado a 2 proyectos de Vercel a la vez (`inkora-next` y un proyecto viejo `inkora` sin actualizar hace 40 dias), lo que puede hacer que el auto-deploy de GitHub no se refleje de forma confiable. Sigue sin resolverse (el usuario no confirmo sacar el dominio del proyecto viejo todavia) — por las dudas, se va a reforzar cada deploy de este turno con `vercel --prod` manual ademas del push a git.

- Objetivo: Rehacer el feedback de "copiado" (codigo del pedido y texto de WhatsApp) que el usuario no aprobaba: no le gustaba el difuminado del contenido ni que el aviso flotara arriba del elemento en vez de taparlo. Se propuso una alternativa (overlay que cubre el elemento) antes de tocar codigo, y el usuario la aprobo.
- Cambios: En `app/catalogo/page.js` se reemplazo por completo el patron anterior (blur + toast flotando afuera con `bottom:100%`) por un overlay `position:absolute; inset:0` que cubre el elemento entero al copiar, con fondo verde solido (#18a36a), icono de check + texto centrado, animacion de fade+scale suave (`copied-overlay-pop`, reemplaza a la vieja `copied-toast-pop`). El icono de copiar en la esquina/al lado deja de alternar a check (queda fijo) ya que el overlay es ahora la unica señal de "copiado". Para el codigo del pedido (elemento chico) el texto del overlay es "¡Copiado!" corto para que entre sin achicarse; para el recuadro de WhatsApp (mas ancho) se mantiene "Copiado al portapapeles" completo.
- Verificacion: `npx eslint`/`npx next build` sin errores.
- Auditoria: N/A (rediseño acordado explicitamente tras propuesta discutida con el usuario antes de implementar).
- Pendiente/Riesgos: Ninguno esperado.

- Objetivo: El texto "Confirmar por WhatsApp" se cortaba en dos renglones dentro de su boton. El usuario pidio que ambos botones ("Listo" y "Confirmar por WhatsApp") sean del mismo tamano y se agranden lo necesario para que el texto entre en un renglon.
- Cambios: En `app/catalogo/page.js` ambos botones pasan de `maxWidth:260`/`maxWidth:220` a `maxWidth:300` (mismo valor en los dos) y se agrega `whiteSpace:'nowrap'` para evitar el corte de linea de forma robusta independientemente del ancho final.
- Verificacion: `npx eslint`/`npx next build` sin errores.
- Auditoria: N/A.
- Pendiente/Riesgos: El usuario tambien pidio repensar (sin implementar todavia) la animacion de "Copiado al portapapeles": no le gusta el difuminado actual y quiere que el aviso aparezca tapando el elemento copiado (overlay), no flotando arriba de el. Queda pendiente proponer un diseno nuevo en el proximo turno, sin tocar codigo hasta que lo apruebe.

- Objetivo: 3 ajustes finos sobre la pantalla de pedido confirmado: boton de WhatsApp un poco menos ancho que "Listo"; sacar el titulo/subtitulo "Confirmar por WhatsApp / Opcional, para avisarnos directo."; y que el feedback de copiado (toast + difuminado) dure un poco menos y difumine menos fuerte.
- Cambios: En `app/catalogo/page.js`: se elimino el bloque de titulo+subtitulo sobre el boton de WhatsApp. El boton paso de `width:100%` a `maxWidth:220` centrado con `margin:'0 auto'` (mas angosto que el "Listo" que tiene `maxWidth:260`). El timeout de "copiado" (codigo y texto de WhatsApp) bajo de 2000ms a 1300ms, sincronizado con la animacion CSS `.copied-toast`; el difuminado bajo de `blur(4px)`/opacidad 0.4 a `blur(2px)`/opacidad 0.55 en ambos lugares.
- Verificacion: `npx eslint`/`npx next build` sin errores.
- Auditoria: N/A (ajustes de diseno acordados explicitamente).
- Pendiente/Riesgos: Ninguno esperado.

- Objetivo: 4 ajustes sobre la pantalla de pedido confirmado: (1) icono de copiar mas visible y cambiado al iconico "dos cuadraditos superpuestos"; (2) los textos "Codigo de tu pedido:" y "Te enviamos la confirmacion a tu email." menos protagonicos (gris); (3) boton de "Confirmar por WhatsApp" mas grande, centrado en su seccion, con el icono de WhatsApp; (4) el texto de email de confirmacion solo debe aparecer si el pedido realmente va a mandar ese email.
- Cambios: En `app/catalogo/page.js`: se reemplazo el icono tipo "clipboard con broche" por el icono estandar de copiar (dos rectangulos superpuestos), subiendo tamano (15→19px) y opacidad en reposo (0.4→0.6), en los dos lugares (codigo de pedido y recuadro de WhatsApp). Los dos textos secundarios pasan a `color:'#9aa3bc'`. El boton de WhatsApp se reescribio: `width:100%`, centrado, `padding` mas grande, `fontSize:15`, y el path SVG real de WhatsApp (el mismo que ya se usaba en el boton flotante) adentro. El texto "Te enviamos la confirmacion..." ahora esta condicionado a `profile?.send_confirmation_email !== false` — el mismo flag que ya se usa para decidir si `/api/send-email` manda el mail al cliente (`sendConfirmation: profile?.send_confirmation_email !== false` en la funcion de envio), asi el texto nunca miente sobre si se mando o no el email.
- Verificacion: `npx eslint`/`npx next build` sin errores.
- Auditoria: Para el punto (4), se busco en todo el repo el flag real que controla el envio del email de confirmacion de pedido (no es un setting global de Admin sino un campo por-usuario `profiles.send_confirmation_email`, el mismo que ya usaba `submitOrder` para decidir `sendConfirmation` al llamar `/api/send-email`), para que el texto y el comportamiento real queden sincronizados sin inventar una fuente de verdad nueva.
- Pendiente/Riesgos: Ninguno esperado.

- Objetivo: Sumarle al feedback de copiar (codigo del pedido y texto de WhatsApp) un toast "Copiado al portapapeles" con animacion suave, difuminado del contenido copiado mientras se muestra, y un icono chico de portapapeles (que pasa a ✓ por 2s) al lado/esquina de cada elemento.
- Cambios: En `app/catalogo/page.js` se agrego el keyframe `copied-toast-pop` (fade+scale in, pausa, fade+scale out, 2s totales via clase `.copied-toast`) al bloque `<style>` existente. El codigo del pedido ahora es `display:inline-block` con un icono de portapapeles/check inline a la derecha del texto (semi-transparente en reposo, opaco+verde al copiar) y el toast aparece flotando arriba centrado; el texto en si se difumina (`filter: blur(4px)` + opacidad reducida) mientras esta copiado. Mismo patron en el recuadro de WhatsApp, con el icono en la esquina superior derecha (estilo bloque de codigo de documentacion) en vez de inline, ya que es una caja de varias lineas.
- Verificacion: `npx eslint`/`npx next build` sin errores.
- Auditoria: N/A (iteracion de diseno acordada explicitamente con el usuario sobre el turno anterior).
- Pendiente/Riesgos: Ninguno esperado.

- Objetivo: Simplificar mas la pantalla de "pedido confirmado": sacar el boton/label de "Copiar" (tanto del texto de confirmacion como del codigo del pedido) sin perder que ambos se puedan copiar, y agrandar el titulo "Pedido confirmado!" para que se entienda de un vistazo que ya quedo confirmado.
- Cambios: En `app/catalogo/page.js`: `successTitle` paso de 22px/700 a 30px/800. El codigo del pedido (`{orderCode}`) y el recuadro con el texto de WhatsApp ahora son clickeables directamente (todo el elemento, no un boton aparte): `cursor:pointer`, `title="Click para copiar"` como tooltip nativo, y feedback puramente visual (fondo/borde verde ~2s) via los estados `codeCopied` (nuevo) y `waCopied` (ya existia). Se elimino la fila con el label "TEXTO DE CONFIRMACION" y el boton "Copiar"/"Copiado".
- Verificacion: `npx eslint`/`npx next build` sin errores.
- Auditoria: N/A (cambio de UI acordado explicitamente con el usuario, opcion "2" de las alternativas propuestas).
- Pendiente/Riesgos: El tooltip nativo (`title`) no se ve en mobile (sin hover), pero el area clickeable es todo el elemento (no un icono chico), asi que sigue siendo facil de tocar aunque no se explique por adelantado — coincide con lo que el usuario eligio conscientemente.

- Objetivo: (1) Terminar y desplegar un cambio de Codex (reorganizar pantalla de "pedido confirmado") que habia quedado sin commitear porque esa sesion corto por limite de uso; (2) evitar que las cards de diseno en Catalogo cambien de alto cuando aparece/desaparece el precio unitario segun la cantidad.
- Cambios: (1) Se reviso el diff sin commitear que dejo Codex en `app/catalogo/page.js` y `AI_RUN_LOG.md`, se verifico que compilaba y lintaba limpio, y se completo el paso que faltaba (commit + push). (2) En `app/catalogo/page.js`, el bloque que muestra `$precio/u` debajo de la categoria de cada diseno devolvia `null` cuando el precio todavia no aplicaba (cantidad insuficiente para ese tramo), haciendo que esa card (y por extension toda la fila del grid) creciera verticalmente recien cuando el precio aparecia. Se cambio para que el `div` siempre se renderice con el mismo alto, alternando `visibility: hidden/visible` en vez de `null`, asi el espacio queda reservado desde el principio.
- Verificacion: `npx eslint`/`npx next build` sin errores en ambos cambios.
- Auditoria: Para (1), se comparo el diff de Codex contra los requisitos que el usuario ya habia pedido en esa sesion (titulo "Pedido confirmado", boton "Listo" unico, WhatsApp en tarjeta aparte, "Copiar" chico integrado arriba del texto) y coincidia exactamente, no hizo falta corregir nada, solo desplegar.
- Pendiente/Riesgos: Ninguno esperado.

---

## 2026-07-05 16:49 -03:00 - ChatGPT Codex

- Objetivo: Reorganizar la pantalla posterior a confirmar pedido en Catalogo: titulo "Pedido confirmado", boton principal "Listo", seccion separada de WhatsApp y texto de confirmacion con accion de copiar integrada.
- Cambios: Se actualizo `app/catalogo/page.js` para cambiar el titulo de exito, separar el cierre del modal como accion principal, mover WhatsApp a una tarjeta secundaria y reemplazar el boton grande de copiar por un boton pequeno "Copiar" en el encabezado del texto.
- Verificacion: `node --check app\catalogo\page.js` OK; `git diff --check` OK con aviso CRLF; `npm.cmd run build` OK con warnings preexistentes/esperables.
- Auditoria: Se leyeron `AGENTS.md`, `CONTEXT.md`, `AI_RUN_LOG.md` y `git status --short`. Se audito el turno anterior sobre formato de dinero del checkout y el arbol tracked estaba limpio antes de editar; quedaron fuera de scope `Inkora.PrintBridge.zip` y `Messi 2.3mf`.
- Pendiente/Riesgos: Probar manualmente la pantalla de exito en mobile y desktop para ajustar espaciados si hiciera falta.

---

## 2026-07-05 16:40 -03:00 - ChatGPT Codex

- Objetivo: Corregir formato de miles en el resumen de confirmacion del catalogo y cambiar el boton de envio a "Confirmar pedido" / "Confirmando...".
- Cambios: Se actualizo `app/catalogo/page.js` para usar `formatOrderMoney` en los importes del modal de confirmacion, evitando que valores numericos o strings se rendericen como `$6000` sin separador, y se ajusto el texto del boton principal.
- Verificacion: `node --check app\catalogo\page.js` OK; `git diff --check` OK con aviso CRLF; `npm.cmd run build` OK con warnings preexistentes/esperables.
- Auditoria: Se leyeron `AGENTS.md`, `CONTEXT.md`, `AI_RUN_LOG.md` y `git status --short`. Se audito el turno anterior sobre Categorias y el arbol tracked estaba limpio antes de editar; quedaron fuera de scope `Inkora.PrintBridge.zip` y `Messi 2.3mf`.
- Pendiente/Riesgos: Probar manualmente un pedido con importes de 4 digitos para confirmar que se vea `$6.000` y que el estado loading muestre "Confirmando...".

---

## 2026-07-05 16:36 -03:00 - ChatGPT Codex

- Objetivo: Mejorar la pestana de Categorias del modal de productos: tags alineadas con mismo tamano, opcion de color "Por defecto" y reordenamiento visual mientras se arrastra.
- Cambios: Se actualizo `app/admin/page.js` para que las categorias se rendericen como chips de ancho/alto fijo, se agrego un menu de color propio con boton "Por defecto" que elimina el color guardado del JSON `category_colors`, y se reemplazo el drag flotante por un preview local que reordena las categorias visualmente durante el arrastre y persiste al soltar.
- Verificacion: `node --check app\admin\page.js` OK; `git diff --check` OK con aviso CRLF; `npm.cmd run build` OK con warnings preexistentes/esperables.
- Auditoria: Se leyeron `AGENTS.md`, `CONTEXT.md`, `AI_RUN_LOG.md` y `git status --short`. Se audito la entrada anterior sobre seleccion en Admin > Usuarios y el arbol tracked estaba limpio antes de editar; quedaron fuera de scope `Inkora.PrintBridge.zip` y `Messi 2.3mf`.
- Pendiente/Riesgos: Probar manualmente en produccion el modal de Categorias: abrir/cerrar menu de color, volver a Por defecto, cambiar color personalizado y arrastrar categorias entre varias posiciones.

---

## 2026-07-05 14:47 -03:00 - ChatGPT Codex

- Objetivo: Corregir la seleccion de clientes en Admin > Usuarios para que funcione parecido a Disenos: click en la fila/card selecciona o deselecciona, Ctrl/Cmd y Shift permiten seleccion multiple, Escape y click afuera sueltan la seleccion.
- Cambios: Se actualizo `app/admin/page.js` agregando limpieza de seleccion de usuarios al handler general del admin, soporte de Escape para usuarios/disenos, marcado `data-user-card` en filas de clientes y filtrado de clicks sobre controles interactivos para que botones, inputs, selects y el switch de email no cambien la seleccion accidentalmente.
- Verificacion: `node --check app\admin\page.js` OK; `git diff --check` OK con aviso CRLF; `npm.cmd run build` OK con warnings preexistentes/esperables.
- Auditoria: Se leyeron `AGENTS.md`, `CONTEXT.md`, `AI_RUN_LOG.md` y `git status --short`. Se audito la entrada anterior sobre el desplegable de vendedores y se confirmo que el arbol tracked solo tenia cambios de este turno; quedaron fuera de scope `Inkora.PrintBridge.zip` y `Messi 2.3mf`.
- Pendiente/Riesgos: Probar manualmente en produccion Admin > Usuarios/Clientes: click en margen/lista vacia, Escape, click en una fila seleccionada, Ctrl/Cmd, Shift y uso de controles dentro de la fila.

---

## 2026-07-05 14:36 -03:00 - ChatGPT Codex

- Objetivo: En Admin > Usuarios/Clientes cambiar la asignacion de vendedor por fila de varios botones a un desplegable; confirmar que clientes nuevos nacen con email de confirmacion de pedido desactivado; auditar registro/auth sin modificarlo.
- Cambios: Se actualizo `app/admin/page.js` para reemplazar los botones inline de vendedor por un `<select>` por cliente. El desplegable mantiene la opcion "Sin vendedor", muestra vendedores activos y conserva el vendedor asignado aunque este inactivo o ya no exista para no falsear visualmente el estado.
- Verificacion: `node --check app\admin\page.js` OK; `git diff --check` OK con aviso CRLF; `npm.cmd run build` OK con warnings preexistentes/esperables. Se revisaron `app/api/register/route.js`, `app/api/invite-user/route.js`, `components/AuthModal.js`, rutas Google/auth y SQL de perfiles.
- Auditoria: Se leyeron `AGENTS.md`, `CONTEXT.md`, `AI_RUN_LOG.md` y `git status --short`. El arbol tracked estaba limpio antes de editar; quedaron fuera de scope `Inkora.PrintBridge.zip` y `Messi 2.3mf`. Se confirmo que `send_confirmation_email` ya se crea en `false` desde API/SQL y que `/api/send-email` respeta el flag via `sendConfirmation`.
- Pendiente/Riesgos: Probar manualmente en Admin > Usuarios que el desplegable cambie vendedor y respete seleccion multiple deshabilitada. Revisar en un turno aparte posibles mejoras de auth: manejo de error del popup Google con `prompt=none`, rate-limit/captcha para registro publico y unificar rutas de registro.

---

## 2026-07-05 14:03 -03:00 - ChatGPT Codex

- Objetivo: Implementar la correccion para que el click en el margen exterior/gutter gris de Admin > Disenos suelte la seleccion.
- Cambios: Se actualizo `app/admin/page.js` para conectar `clearDesignSelectionOutsideCards` tambien al wrapper general `s.wrap`, que cubre el fondo gris fuera de `.adm-content`. Se conserva el handler existente en `.adm-content` y la logica de click dentro de una card seleccionada sigue igual.
- Verificacion: `node --check app\admin\page.js` OK; `git diff --check` OK con aviso CRLF; `npm.cmd run build` OK con warnings preexistentes/esperables.
- Auditoria: Se leyeron `AGENTS.md`, `CONTEXT.md`, `AI_RUN_LOG.md` y `git status --short`. Se confirmo que el ultimo commit local era de Claude (`7ba7deb`) sobre el popup de Google y que el arbol tracked estaba limpio antes de editar; solo quedaron fuera de scope `Inkora.PrintBridge.zip` y `Messi 2.3mf`. Se contrasto el diagnostico anterior: `.adm-content` no cubria el gutter gris porque `s.content` mide 90% y queda centrado dentro de `s.wrap`.
- Pendiente/Riesgos: Probar manualmente en produccion click en el gutter gris izquierdo/derecho de Admin > Disenos con una seleccion activa.

---

## 2026-07-05 -03:00 - Claude Sonnet 5

- Objetivo: El popup de "Iniciar sesion con Google" a veces se quedaba trabado en "Iniciando sesion..." para siempre, aunque la sesion ya se hubiera iniciado (confirmado: cerrando el popup a mano se veia todo ya logueado).
- Cambios: Diagnostico primero (sin tocar codigo) y confirmacion del usuario antes de corregir. Causa: `app/auth/popup-callback/page.js` solo actuaba (mandar `postMessage` al opener + `window.close()`) ante el evento `SIGNED_IN` de `supabase.auth.onAuthStateChange`. Pero el login real ya se completa del lado del servidor en `app/api/auth/google/callback/route.js` (que llama a `signInWithIdToken` y deja la sesion en cookies) *antes* de que esta pagina cargue — asi que casi siempre lo que el cliente ve al iniciar es `INITIAL_SESSION` (sesion ya existente), no `SIGNED_IN` (transicion en vivo), y ese evento se ignoraba silenciosamente. Era una carrera dependiente de timing (por eso "a veces"). Fix: se acepta tambien `INITIAL_SESSION` (con sesion valida) ademas de `SIGNED_IN`, con una bandera `handled` para evitar doble ejecucion si llegaran a dispararse ambos eventos.
- Verificacion: `npx eslint`/`npx next build` sin errores.
- Auditoria: Se confirmo que esta misma pagina de callback es compartida por el login de Google del catalogo publico, Admin y Operarios (los 3 usan `redirectTo: .../auth/popup-callback`), asi que la correccion cubre los 3 flujos con un solo cambio.
- Pendiente/Riesgos: Ninguno esperado — el cambio es aditivo (agrega un caso mas al `if`, no saca ninguno) y la bandera `handled` previene cualquier efecto doble. Falta confirmacion del usuario probando en produccion que ya no se cuelga.

---

## 2026-07-05 12:34 -03:00 - ChatGPT Codex

- Objetivo: Explicar como se llama el espacio gris lateral de Admin > Disenos y hacer que al clickear ese margen/gutter se suelte la seleccion de disenos.
- Cambios: Se actualizo `app/admin/page.js` agregando `clearDesignSelectionOutsideCards` y conectandolo al contenedor `.adm-content`, limitado a la pestana Disenos. Ahora cualquier click del area principal que no este dentro de una fila `data-design-card`, incluido el margen exterior gris/padding lateral, limpia `selectedIds`.
- Verificacion: `node --check app\admin\page.js` OK; `git diff --check` OK con aviso CRLF; `npm.cmd run build` OK con warnings preexistentes/esperables.
- Auditoria: Se leyeron `AGENTS.md`, `CONTEXT.md`, `AI_RUN_LOG.md` y `git status --short`. El arbol tracked estaba limpio antes de editar; solo habia untracked `Inkora.PrintBridge.zip` y `Messi 2.3mf`, que quedaron fuera de scope. Se audito el turno anterior y se confirmo que el fix previo limpiaba dentro de las cards, pero no cubria el gutter lateral gris fuera de ellas.
- Pendiente/Riesgos: Probar manualmente en produccion click sobre el margen exterior izquierdo/derecho de la pestana Disenos para confirmar que deselecciona, y que click dentro de una fila de diseno siga seleccionando normalmente.

---

## 2026-07-04 21:55 -03:00 - ChatGPT Codex

- Objetivo: Corregir dos detalles puntuales: en Admin > Disenos soltar seleccion al hacer click fuera de cualquier card de diseno, y en Catalogo ocultar "Sin variantes" cuando el producto no tiene variantes.
- Cambios: Se actualizo `app/admin/page.js` para que los paneles de la pestana Disenos limpien `selectedIds` cuando el click no ocurre dentro de una fila marcada como `data-design-card`; las filas de diseno quedan marcadas como cards para no disparar la limpieza. Se actualizo `app/catalogo/page.js` para ocultar la fila de variantes cuando `activeVariants.length <= 1`.
- Verificacion: `node --check app\admin\page.js` OK; `node --check app\catalogo\page.js` OK; `git diff --check` OK con avisos CRLF; `npm.cmd run build` OK con warnings preexistentes/esperables.
- Auditoria: Se reviso `git status --short` y los ultimos commits de Claude (`6dcbee5`, `fcad1ac`, `f7e9634`, `5b5b8eb`, `87ce92a`). El arbol tracked estaba limpio antes de editar; solo quedaban untracked `Inkora.PrintBridge.zip` y `Messi 2.3mf`, que se mantuvieron fuera de scope.
- Pendiente/Riesgos: Probar manualmente en produccion que clickear filtros/panel superior o espacios fuera de una fila de diseno deseleccione, y que el Catalogo no muestre nada entre productos y categorias si no hay variantes.

---

## 2026-07-04 -03:00 - Claude Sonnet 5 (v8)

- Objetivo: El indicador de version agregado en el turno anterior no se veia en mobile/PWA.
- Cambios: Causa: la version quedo anidada dentro de `.adm-header-title`, y hay un media query (`@media max-width:480px`) que oculta esa clase entera para hacer lugar a los botones de icono en pantallas chicas. Se saco el span de version de adentro del titulo y se lo puso como elemento independiente (sin esa clase) entre el logo y el titulo, con `flexShrink:0` para que no se lo coma el layout.
- Verificacion: `npx eslint`/`npx next build` sin errores.
- Auditoria: N/A.
- Pendiente/Riesgos: Ninguno.

- Objetivo: Mostrar la version actual (del deploy) en la barra superior de Admin, muy sutil, tanto en web como en PWA.
- Cambios: `next.config.mjs` calcula el hash corto de git (`git rev-parse --short HEAD`) en build time y lo expone como `NEXT_PUBLIC_APP_VERSION` (con fallback `'dev'` si no hay git disponible). `app/admin/page.js` lo muestra como `v{hash}` junto al titulo "Panel de Administración", en texto chico y opacidad 0.35 — no requiere distincion entre web/PWA porque ambas cargan la misma pagina.
- Verificacion: `npx eslint` sin errores; `npx next build` completo sin errores.
- Auditoria: N/A (cambio chico y autocontenido).
- Pendiente/Riesgos: Ninguno — se recalcula solo en cada build/deploy, siempre refleja el commit real desplegado.

- Objetivo: (1) Notificaciones push para mensajes nuevos del chat interno; (2) silenciar notificaciones por canal en 2 niveles (sin sonido / no mostrar nada); (3) en mobile, el canal por defecto al entrar a la pestana Chat siempre debe ser "General", no el ultimo canal visitado.
- Cambios: No existia ninguna infraestructura de push previa (se investigo con un agente antes de tocar nada). Se agrego: `sql/chat_push_notifications.sql` con las tablas `push_subscriptions` (endpoint/keys por dispositivo) y `chat_channel_member_settings` (mute_level 'none'|'mute_sound'|'mute_all' por canal y usuario, + last_read_at server-side a futuro), mas un trigger `on_chat_message_created_notify` via `pg_net` (misma extension que usa el feature nativo "Database Webhooks" de Supabase) que llama a `/api/webhooks/chat-message-created` en cada INSERT de `chat_messages`. Esa ruta valida un secreto (`CHAT_WEBHOOK_SECRET`) y manda los push reales con `web-push` (nueva dependencia) usando un par de claves VAPID generadas para este proyecto, respetando el mute_level de cada destinatario (excluye al que escribio, salta a los `mute_all`, manda silencioso a los `mute_sound`) y limpiando suscripciones vencidas (404/410). `public/sw.js` gano handlers `push` y `notificationclick`. Nuevo componente `components/ChatPushToggle.js` (boton campana en la barra superior de Admin) maneja pedir permiso + suscribirse/desuscribirse via `/api/push/subscribe` y `/api/push/unsubscribe`. En `components/chat/ChatPanel.js`: nuevo menu por canal (🔔/🔈/🔕) para elegir el mute_level, persistido en `chat_channel_member_settings`; y se corrigio el efecto de auto-seleccion de canal para que en mobile (`isMobile`) ignore el ultimo canal guardado en localStorage y vaya siempre al canal `type==='main'` ("General"), salvo que venga un `?canal=` explicito en la URL (deep link).
- Verificacion: `npx eslint` sobre todos los archivos nuevos/tocados sin errores; `npx next build` completo sin errores, rutas nuevas (`/api/push/subscribe`, `/api/push/unsubscribe`, `/api/webhooks/chat-message-created`) presentes en el output.
- Auditoria: Se investigo con un agente toda la arquitectura de chat/PWA antes de implementar (no habia service worker con push, ni tabla de preferencias por usuario, ni convencion de "info por canal" en la base) para no reinventar mal algo que ya existiera. Se siguio el mismo criterio de RLS liviana (`USING(true)`) que ya usan el resto de las tablas de chat, por consistencia.
- Pendiente/Riesgos: **Critico**: hay que agregar las env vars nuevas (`VAPID_PUBLIC_KEY`, `NEXT_PUBLIC_VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY`, `VAPID_SUBJECT`, `CHAT_WEBHOOK_SECRET`) en el dashboard de Vercel — hoy solo estan en `.env.local` (nunca se commitea), asi que en produccion el envio de push va a fallar hasta que se agreguen ahi y se re-deploye. Ejecutar `sql/chat_push_notifications.sql` en Supabase (requiere que `pg_net` este disponible en el proyecto, deberia estarlo por defecto). El secreto del webhook quedo hardcodeado en el SQL (necesario porque el trigger no puede leer variables de entorno de Vercel) — si se regenera el secreto hay que actualizar tanto `.env.local`/Vercel como volver a correr ese UPDATE del trigger. No se implemento un boton de "silenciar" accesible fuera del menu por canal (ej. no hay un mute global); tampoco se persiguio mover `last_read_at` de localStorage a la tabla nueva (la columna existe pero no se usa todavia, queda para mas adelante si hace falta sincronizar leido/no-leido entre dispositivos).

- Objetivo: "Iniciar sesion con Google" dentro de la app nativa mostraba "Acceso bloqueado... Error 403: disallowed_useragent".
- Cambios: Es un bloqueo deliberado de Google (politica "Usa navegadores seguros"): detecta el token "; wv" que Android agrega al user-agent por defecto de cualquier WebView embebido y rechaza el login OAuth desde ahi, para prevenir robo de credenciales via WebViews maliciosos. Fix estandar aplicado en `MainActivity.kt` y `FloatingBubbleService.kt`: `webView.settings.userAgentString = webView.settings.userAgentString.replace("; wv", "")` antes de cargar cualquier URL, para que el WebView se identifique como un navegador normal. Bump a versionCode/versionName 3/1.0.2, recompilado (mismo certificado, verificado) y publicado con `publish-release.js`.
- Verificacion: `./gradlew assembleRelease` compila y firma con el mismo certificado que v1/v2 (`apksigner verify`); publicado y confirmado con `curl -I` (200 OK).
- Auditoria: N/A (fix puntual, causa conocida y documentada de Google).
- Pendiente/Riesgos: Esto es un workaround de deteccion basada en el user-agent, no una excepcion oficial otorgada por Google — si Google en el futuro refuerza la deteccion con otras señales (fingerprinting de comportamiento de WebView, no solo el string), podria volver a bloquearse. Para una solucion mas robusta a largo plazo habria que migrar el login de Google especificamente a Chrome Custom Tabs, pero es mas trabajo y no hizo falta por ahora. Falta confirmacion del usuario de que el login ya funciona en el celular real.

- Objetivo: El usuario probo la app en su celular y reporto 3 problemas: (1) confirmar que la actualizacion debe poder bajarse e instalarse desde dentro de la app, lo mas automatico posible; (2) la app debia ser exclusivamente para el panel de Admin, pero cargaba la web/catalogo general; (3) al tocar la burbuja flotante para expandirla, no se abria y al segundo intento Android mostraba "la app sigue fallando".
- Cambios: **Bug critico (3) resuelto** en `FloatingBubbleService.kt`: `addExpandedView()` inflaba un layout con el atributo de tema `?attr/selectableItemBackgroundBorderless` (de AppCompat) usando el contexto pelado del Service — los Services, a diferencia de las Activities, no reciben el tema de la app (`Theme.InkoraApp`), asi que ese atributo no resolvia y el inflado tiraba una excepcion cada vez que se tocaba la burbuja, crasheando la app. Fix: envolver el contexto con `ContextThemeWrapper(this, R.style.Theme_InkoraApp)` antes de inflar. Se agrego ademas manejo defensivo (`runCatching` + `Toast`) en `toggleExpanded()` y en el arranque de la burbuja para que un fallo futuro no vuelva a crashear la app, solo muestre un aviso. **Punto (2)**: `MainActivity.START_URL` paso de `https://www.inkora.com.ar/` a `https://www.inkora.com.ar/admin`; se agrego el boton "Activar flotante" tambien a la barra superior propia de `app/admin/page.js` (antes solo estaba en `components/Header.js`, que Admin no usa). **Punto (1)**: se confirmo que el mecanismo de auto-update (`UpdateManager.kt`) no tenia bugs nuevos — se bumpeo `versionCode`/`versionName` a 2/1.0.1 en `android-app/app/build.gradle.kts`, se recompilo (firmado con el mismo keystore, mismo digest SHA-256 que la v1, requisito para que Android acepte la actualizacion sobre la instalacion existente) y se publico con `node android-app/publish-release.js 2 1.0.1`.
- Verificacion: `./gradlew assembleRelease` compila y firma correctamente (mismo certificado que v1, verificado con `apksigner verify`); `npx eslint`/`npx next build` sin errores (dos intentos de build tuvieron un crash nativo de Windows no relacionado al codigo — 0xC0000005 — que se resolvio solo al reintentar). Se confirmo con `curl -I` que la URL publicada de la v2 responde 200 OK.
- Auditoria: El diagnostico del crash se hizo por lectura cuidadosa del codigo (no habia logs del celular disponibles): se noto que el WebView de `MainActivity` (contexto de Activity, con tema) funcionaba bien, pero el de `FloatingBubbleService` (contexto de Service, sin tema) fallaba — la diferencia entre ambos contextos fue la pista clave para encontrar el atributo de AppCompat sin resolver.
- Pendiente/Riesgos: La v1 instalada en el celular del usuario probablemente no puede auto-actualizarse a si misma si el crash tambien afectaba ese flujo indirectamente (no confirmado); como salvaguarda, el usuario deberia instalar la v1.0.1 manualmente una vez mas (bajandola con el icono de descarga en Admin), y de ahi en mas el auto-update ya deberia andar solo para v3 en adelante. Falta que el usuario confirme en su celular real que la burbuja ahora expande sin crashear.

- Objetivo: El usuario no queria el flujo de publicacion de versiones desde una UI de Admin (file picker + inputs de version) que arme en el turno anterior — lo considero poco practico. Pidio en cambio: (1) un boton simple (icono de descargar) en la barra superior para que cualquiera baje el APK actual instalado, sin nada mas que elegir; (2) que la publicacion de versiones nuevas la haga la IA por terminal, no un admin desde el navegador.
- Cambios: Se elimino por completo la tarjeta "App Android" de Admin > Config (file input, campos de version, boton "Subir y publicar") junto con su estado y handler `handleUploadAppApk`, y se borro la ruta `app/api/admin/app-apk-upload-url/route.js` que ya no hacia falta. En su lugar: un icono chico de descarga en la barra superior de `app/admin/page.js` (junto al boton de tema), que solo aparece si `settings.android_app_apk_url` tiene valor, y linkea directo a esa URL con atributo `download`. Se creo `android-app/publish-release.js`: script de Node que sube el APK compilado a Supabase Storage y hace upsert de las 3 keys de `settings`, para correr por terminal (`node publish-release.js <versionCode> <versionName>`) cada vez que se compile una version nueva. Se publico la v1.0.0 actual con este script como primera prueba end-to-end.
- Verificacion: `npx eslint` sobre `app/admin/page.js` sin errores; `npx next build` completo sin errores (un intento anterior crasheo con codigo de salida nativo de Windows 0xC0000005, no relacionado al codigo — el reintento inmediato compilo limpio). Se verifico con `curl -I` que la URL publicada del APK responde 200 OK con el content-type correcto.
- Auditoria: El primer diseño (upload UI en Admin) resolvia el problema equivocado — el usuario aclaro que solo existe una version "actual" y que public quiere descargar, no publicar, desde la web. Se corrigio rapido tras una sola pregunta de aclaracion en vez de asumir.
- Pendiente/Riesgos: El SQL `sql/android_app_version.sql` quedo como documentacion nomas (las filas ya se crean solas via el script); no hace falta correrlo salvo que la tabla `settings` se recree desde cero. Cada vez que se compile un cambio nuevo en `android-app/`, correr `node android-app/publish-release.js <versionCode> <versionName>` para publicarlo — sin este paso el boton de descarga sigue apuntando a la version anterior y el auto-update de la app instalada nunca encuentra nada nuevo.

---

## YYYY-MM-DD HH:mm -03:00 - IA

- Objetivo:
- Cambios:
- Verificacion:
- Auditoria:
- Pendiente/Riesgos:

---

## 2026-07-04 -03:00 - Claude Sonnet 5 (v2)

- Objetivo: Crear una app nativa Android (aparte de la PWA) que agregue una "burbuja flotante" tipo chat-head sobre otras apps (imposible en PWA por sandboxing) y se auto-actualice via wifi sin Play Store.
- Cambios: Nueva carpeta `android-app/` (proyecto Gradle/Kotlin separado, minSdk 26, compileSdk 34): `MainActivity.kt` con un WebView que carga `https://www.inkora.com.ar/` (asi la web sigue siendo la unica fuente de UI/logica, sin duplicar nada) + puente JS `AndroidBridge.activateFloating()`; `FloatingBubbleService.kt` implementa el icono flotante arrastrable (`TYPE_APPLICATION_OVERLAY`) que expande/colapsa una vista compacta con otro WebView; `UpdateManager.kt` chequea `/api/app-version`, descarga el APK con `DownloadManager` y dispara el instalador del sistema (un toque de "Instalar" es inevitable sin MDM) + `PackageReplacedReceiver.kt` reabre la app sola despues de actualizarse. Se genero un keystore de firma propio (`android-app/keystore/`, fuera de git) y se instalaron JDK 17, Android Studio y el SDK de Android (cmdline-tools, platform-tools, platform 34, build-tools 34) en esta PC via winget/sdkmanager. Del lado de Next.js: nuevo endpoint publico `app/api/app-version/route.js` (lee de la tabla `settings`) y un boton "Activar flotante" en `components/Header.js` (solo visible cuando `window.AndroidBridge` existe, o sea corriendo dentro de esta app nativa, no en el navegador).
- Verificacion: `./gradlew assembleRelease` compila y firma un APK release valido (`android-app/app/build/outputs/apk/release/app-release.apk`, verificado con `apksigner verify`); `npx next build` del sitio sigue sin errores.
- Auditoria: Se investigaron limitaciones reales de plataforma antes de proponer nada (una PWA no puede dibujar overlays sobre otras apps ni auto-instalarse sin toque del usuario, son restricciones de seguridad de Android/iOS, no arbitrarias). El usuario eligio explicitamente: solo Android, sin Google Play, actualizador propio con un toque de "Instalar" inevitable.
- Pendiente/Riesgos: Ejecutar en Supabase `sql/android_app_version.sql` (agrega las 3 keys de version a `settings`). Falta decidir donde alojar los APKs de futuras versiones (ej. bucket de Supabase Storage) y cargar esa URL en `android_app_apk_url` cada vez que se compile una version nueva del cascaron nativo. El keystore (`android-app/keystore/inkora-app.jks` + `keystore.properties`) vive solo en este disco, fuera de git: si se pierde no se puede volver a firmar/actualizar esta instalacion especifica, hay que guardarlo a resguardo (ej. backup cifrado aparte). Falta la instalacion inicial real en un celular de prueba (no pude verificar visualmente la burbuja en un dispositivo real).

---

## 2026-07-04 -03:00 - Claude Sonnet 5

- Objetivo: (1) Agrupar por producto + resumen de unidades en el mensaje de confirmacion de WhatsApp; (2) eliminar el beep repetido del microfono al cargar pedidos por voz en celular; (3) evitar que se pierda audio al hablar justo despues de decir "siguiente"; (4) diagnosticar (sin tocar) el flujo de confirmacion de pedido -> email en busca de errores de cantidades/precios; (5) diagnosticar (sin tocar) toda la app en busca de errores/parches, y luego, con la aprobacion del usuario sobre ese diagnostico, corregir los items de prioridad alta y media que el usuario aprobo explicitamente.
- Cambios: `app/catalogo/page.js` ahora arma el mensaje de WhatsApp con `buildWhatsAppConfirmationMessage` (agrupado por producto en el orden elegido + resumen de unidades si hay mas de un producto) y agrega una suscripcion realtime a `price_tiers` (`loadPriceTiersForUser`). `components/CreateOrderModal.js` cambia el reconocimiento de voz a `continuous=true` con `interimResults`, evitando el reinicio (que causaba el beep) y la perdida de audio entre comandos. Alta prioridad aprobada: `lib/admin-api-auth.js` gano `requireAdminOrOperator` (Bearer+cookie, admins+operarios activos) y se aplico en `/api/bridge-config` y `/api/upload-image` (antes sin auth real); se creo `lib/supabase-admin.js` con `getAdminClient()` unico y se migraron ~11 rutas API que lo reimplementaban; `sql/app_config.sql` elimino la policy `authenticated_read` que exponia `bridge_token`; `components/Header.js` corrige el badge del carrito (mostraba "1" fijo); `app/dashboard/page.js` usa el estado de pedido compartido (le faltaban `in_production`/`ready`). Media prioridad aprobada: `lib/fuzzy-match.js` corrigio el regex de acentos roto; se elimino codigo muerto y las flags `useProductManagementModals`/`inviteOpen` hardcodeadas en `true` en `app/admin/page.js` (incluido un bloque de ~250 lineas de UI de categorias/escalas ya reemplazado por los modales); se creo `lib/order-status.js` como fuente unica de label/color de estado de pedido (usado por `Header.js`, `dashboard/page.js` y `lib/chat-helpers.js` via re-export) y `lib/slug.js` como `toSlug` unico (usado por `app/page.js`, `app/catalogo/page.js`, `app/admin/page.js`). Se agrego `sql/price_tiers_realtime_enable.sql` para habilitar Realtime sobre `price_tiers`.
- Verificacion: `npx eslint` sobre todos los archivos tocados sin errores; `npx next build` completo sin errores (solo warnings preexistentes de `<img>`/`exhaustive-deps` no relacionados).
- Auditoria: Se audito todo el repo antes de tocar nada (puntos 4 y 5 fueron solo diagnostico en un primer turno); recien en un turno posterior el usuario aprobo explicitamente que hallazgos corregir. Antes de tocar `/api/bridge-config` y `/api/upload-image` se greppearon todos los callers reales (incluyendo `app/operarios/page.js` y `components/chat/ChatPanel.js`) para no romper el acceso de operarios ni del chat. Se verifico compatibilidad de `requireAdmin` con sus ~11 call sites antes de cambiar su forma de retorno. No se toco `app/api/send-email/route.js` (su `getAdminClient()` tiene un contrato de error distinto, se dejo afuera a proposito).
- Pendiente/Riesgos: Ejecutar manualmente en Supabase SQL Editor `sql/app_config.sql` (saca la policy vieja) y `sql/price_tiers_realtime_enable.sql` (sin esto la suscripcion realtime de precios no recibe eventos aunque el codigo este bien) — un deploy de codigo no corre SQL. Diagnostico 4 quedo con 2 puntos sin tocar por decision explicita del usuario: la agregacion de cantidades por variante en `getUnitPrice`/`getProductMinQty` (bug real pero de impacto bajo, no es dinero de pasarela) y la discrepancia CONTEXT.md vs codigo sobre `user_product_localities` (el usuario confirmo que el comportamiento correcto es el del codigo; CONTEXT.md quedo desactualizado y no se corrigio su texto). Diagnostico 5 aun tiene pendientes de prioridad baja no pedidos en este turno.

---

## 2026-07-04 09:41 -03:00 - ChatGPT Codex

- Objetivo: Ajustar UX de optimizacion en Disenos: persistir el objetivo de KB, quitar el boton grande de optimizar y mostrar Original/Optimizada sin objetivo.
- Cambios: Se actualizo `app/admin/page.js` para guardar `Miniatura` en `localStorage`, quitar el boton bulk del encabezado y dejar solo los botones inline, enviar `sourceSizeKb` al optimizar y mostrar `Original X KB / Optimizada Y KB`. Se extendio `app/api/admin/design-optimized-image/route.js` para guardar el peso original cuando la columna exista y reintentar sin bloquear si falta. Se extendio `app/api/admin/design-image-summary/route.js` para devolver tamanos por diseno desde Storage. Se agrego `optimized_image_source_size_kb` a `sql/design_optimized_images.sql`.
- Verificacion: `node --check app\admin\page.js` OK; `node --check app\api\admin\design-optimized-image\route.js` OK; `node --check app\api\admin\design-image-summary\route.js` OK; `git diff --check` OK con avisos CRLF; `npm.cmd run build` OK con warnings preexistentes/esperables.
- Auditoria: Se reviso la entrada anterior y el estado real del repo. El ultimo deploy estaba en `77d1f93`; se mantuvieron fuera de scope `.claude/settings*.json`, `Inkora.PrintBridge.zip` raiz y `Messi 2.3mf`.
- Pendiente/Riesgos: Ejecutar nuevamente `sql/design_optimized_images.sql` en Supabase para agregar `optimized_image_source_size_kb`; si no se ejecuta, la optimizacion sigue funcionando por fallback pero el peso original no queda persistido en DB para futuras recargas.

---

## 2026-07-04 09:33 -03:00 - ChatGPT Codex

- Objetivo: Corregir en Admin > Disenos que al hacer click en espacio vacio se suelte la seleccion.
- Cambios: Se ajusto `app/admin/page.js` para distinguir entre zona seleccionable de la fila, controles de la derecha y espacios vacios. Ahora el hueco visual de la fila y el fondo de la lista limpian `selectedIds`, mientras que la info del diseno sigue seleccionando normalmente.
- Verificacion: `node --check app\admin\page.js` OK; `git diff --check` OK con avisos CRLF; `npm.cmd run build` OK con warnings preexistentes/esperables.
- Auditoria: Se reviso la entrada anterior y se confirmo que el cambio previo estaba desplegado en `8a96de1`, pero no cubria el espacio vacio dentro del ancho de una fila. Se mantuvieron fuera de scope `.claude/settings*.json`, `Inkora.PrintBridge.zip` raiz y `Messi 2.3mf`.
- Pendiente/Riesgos: Probar manualmente en produccion haciendo click en el hueco entre la info del diseno y los botones, y debajo de la lista.

---

## 2026-07-04 09:15 -03:00 - ChatGPT Codex

- Objetivo: Corregir el error de suma de pesos en Disenos, permitir soltar seleccion con click en espacio vacio y mejorar el visor de miniaturas con imagen mas grande y zoom con rueda.
- Cambios: Se actualizo `app/api/admin/design-image-summary/route.js` para calcular tamanos con Supabase Storage API en vez de consultar el schema SQL `storage`, evitando `Invalid schema: storage`. Se ajusto `app/admin/page.js` para que la lista de Disenos tenga area vacia clickeable que limpia seleccion, y para que el modal abra al 130%, permita zoom in/out con rueda y mantenga los controles fijos.
- Verificacion: `node --check app\admin\page.js` OK; `node --check app\api\admin\design-image-summary\route.js` OK; `git diff --check` OK con avisos CRLF; `npm.cmd run build` OK con warnings preexistentes/esperables.
- Auditoria: Se reviso la entrada anterior y el estado real del repo. El deploy anterior estaba aplicado en `9481d57`; se mantuvieron fuera de scope `.claude/settings*.json`, `Inkora.PrintBridge.zip` raiz y `Messi 2.3mf`.
- Pendiente/Riesgos: Ejecutar `sql/design_optimized_images.sql` en Supabase si aun no se aplico. El resumen depende de que Storage API devuelva metadata de tamanos para los objetos del bucket `assets`.

---

## 2026-07-04 08:49 -03:00 - ChatGPT Codex

- Objetivo: Ajustar la experiencia de miniaturas optimizadas en Diseños: cursor de mano, preview original/optimizada sin nueva pestaña y barra inferior de resumen estilo Excel.
- Cambios: Se actualizo `app/admin/page.js` para que la miniatura tenga cursor pointer, el modal abra primero la imagen original y permita alternar ahi mismo entre Original y Optimizada. Se agrego una barra inferior sticky en Diseños con tres valores siempre visibles: recuento, peso total de originales y peso total de optimizadas, aplicada a total/filtro/seleccion. Se agrego `app/api/admin/design-image-summary/route.js`, que calcula pesos desde metadata de Supabase Storage sin descargar las imagenes completas.
- Verificacion: `node --check app\admin\page.js` OK; `node --check app\api\admin\design-image-summary\route.js` OK; `git diff --check` OK con avisos CRLF; `npm.cmd run build` OK con warnings preexistentes/esperables por `<img>` en previews.
- Auditoria: Se reviso la entrada anterior de optimizacion y el estado real del repo. El commit anterior `3683cf2` ya estaba aplicado; quedaron fuera de scope `.claude/settings*.json`, `Inkora.PrintBridge.zip` raiz y `Messi 2.3mf`.
- Pendiente/Riesgos: Ejecutar `sql/design_optimized_images.sql` en Supabase antes de usar Optimizar. La barra de resumen depende de que la metadata de Storage tenga tamanos; si algun objeto no reporta size, no suma ese archivo.

---

## 2026-07-04 08:31 -03:00 - ChatGPT Codex

- Objetivo: Agregar optimizacion masiva y por fila de miniaturas desde Admin > Diseños, conservando originales y usando optimizadas en la web.
- Cambios: Se agrego `sql/design_optimized_images.sql` con columnas `optimized_image_*`. Se creo `app/api/admin/design-optimized-image/route.js` para guardar miniaturas optimizadas con validacion admin, borrar la optimizada anterior del mismo diseño y mantener `image_url`/`model_url` originales. Se agrego `lib/design-image-url.js` y se actualizo `app/admin/page.js` para comprimir en navegador a un objetivo editable en KB, procesar seleccion multiple o fila individual, mostrar estado por diseño, usar preview optimizada en la fila y abrirla en grande. Se actualizo `app/catalogo/page.js`, `components/DesignThumb.js` y la miniatura local de `app/operarios/page.js` para preferir `optimized_image_url` cuando exista. Siguen presentes los cambios pendientes previos de realtime en `components/ProductionTab.js`, `app/operarios/page.js` y `sql/production_realtime_enable.sql`.
- Verificacion: `node --check app\admin\page.js` OK; `node --check app\catalogo\page.js` OK; `node --check app\operarios\page.js` OK; `node --check components\DesignThumb.js` OK; `node --check app\api\admin\design-optimized-image\route.js` OK; `node --check lib\design-image-url.js` OK; `npm.cmd run build` OK con warnings preexistentes y nuevos warnings esperables por `<img>` en previews admin.
- Auditoria: Se reviso la entrada anterior y el estado real del repo. Se confirmo que el turno anterior no habia podido commitear/deployar por permisos/red; sus cambios siguen en el arbol. Se mantuvieron fuera de scope `.claude/settings*.json`, `Inkora.PrintBridge.zip` raiz y `Messi 2.3mf`.
- Pendiente/Riesgos: Ejecutar en Supabase SQL Editor `sql/design_optimized_images.sql` antes de usar el boton Optimizar; si no, la API devuelve aviso de SQL faltante. Hacer commit/deploy si el entorno lo permite.

---

## 2026-07-03 22:59 -03:00 - ChatGPT Codex

- Objetivo: Corregir que las cantidades impresas/troqueladas/desperdicio no se actualicen entre PCs aunque los botones ya no se bloqueen.
- Cambios: Se agrego refresco silencioso cada 2.5s en `components/ProductionTab.js` y `app/operarios/page.js`, solo con la pestana visible y sin activar loaders ni bloquear inputs. Se conserva Realtime como camino principal y el polling queda como respaldo si Supabase Realtime no esta aplicado o se corta. En `/operarios` el claim de operario queda cacheado para no ejecutarse en cada refresco. Se agrego `sql/production_realtime_enable.sql` para verificar/habilitar `production_order_tasks` en la publicacion `supabase_realtime`.
- Verificacion: `node --check components\ProductionTab.js` OK; `node --check app\operarios\page.js` OK; `git diff --check` OK con avisos CRLF preexistentes; `npm.cmd run build` OK con warnings preexistentes.
- Auditoria: Se reviso la entrada anterior sobre cantidades en vivo y se confirmo que la RPC atomica seguia pendiente de aplicar en Supabase por falta de `SUPABASE_ACCESS_TOKEN`. Se mantuvieron fuera de scope `.claude/settings*.json`, `Inkora.PrintBridge.zip` raiz y `Messi 2.3mf`.
- Pendiente/Riesgos: Ejecutar en Supabase SQL Editor `sql/production_realtime_enable.sql` para solucionar la causa de fondo de Realtime. El deploy web incluye fallback liviano, por lo que las PCs deberian sincronizar aunque Realtime todavia no emita eventos.

---

## 2026-07-03 22:35 -03:00 - ChatGPT Codex

- Objetivo: Hacer que las cantidades impresas/troqueladas/desperdicio se actualicen rapido y en vivo entre PCs sin pisarse.
- Cambios: Se ajusto `components/ProductionTab.js` y `app/operarios/page.js` para que los controles de cantidad no bloqueen la UI mientras guardan, usen cola por tarea, manden parches reales con `NULL` en campos no tocados y apliquen realtime puntual por fila en vez de recargar toda la lista en cada update. Los botones `+/-` ahora intentan usar la nueva RPC atomica `increment_production_task_counter` y cachean fallback si la RPC aun no existe. Se agrego esa RPC a `sql/production_task_progress_partial_updates.sql` y `sql/production_orders_and_operators.sql`.
- Verificacion: `node --check components\ProductionTab.js` OK; `node --check app\operarios\page.js` OK; `git diff --check` OK con avisos CRLF; `npm.cmd run build` OK con warnings preexistentes. Se intento aplicar `npx.cmd supabase db query --linked --file sql\production_task_progress_partial_updates.sql`, pero Supabase CLI pidio `SUPABASE_ACCESS_TOKEN` y no habia variables `SUPABASE*` cargadas.
- Auditoria: Se reviso el turno anterior del Bridge `1.6.5` y el estado real del repo. Se mantuvieron fuera de scope `.claude/settings*.json`, `Inkora.PrintBridge.zip` raiz y `Messi 2.3mf`.
- Pendiente/Riesgos: Ejecutar en Supabase SQL Editor `sql/production_task_progress_partial_updates.sql` para activar la RPC atomica. Sin ese SQL, la UI queda mas rapida y compatible por fallback, pero dos PCs haciendo `+/-` exactamente al mismo tiempo no tienen garantia atomica completa.

---

## 2026-07-03 22:04 -03:00 - ChatGPT Codex

- Objetivo: Implementar una correccion definitiva para que la impresion de pedidos detecte SumatraPDF y respete copias multiples.
- Cambios: Se subio el Bridge a `1.6.5`. `PrintJobService` ahora busca `SumatraPDF.exe` junto al ejecutable real, en la carpeta estable `%LOCALAPPDATA%\Inkora\PrintBridge\app`, en `AppContext`, cwd, Program Files y PATH, y expone las rutas revisadas en `/health`. El auto-update y `install.ps1` ahora instalan siempre en esa carpeta estable, copian el paquete completo, verifican Sumatra, registran `inkora-bridge://`, configuran auto-inicio e inician el Bridge. `build-release.ps1` incluye instalador y README dentro del ZIP. La web apunta a `bridge-v1.6.5`.
- Verificacion: `node --check components\ProductionTab.js` OK; `node --check app\operarios\page.js` OK; `dotnet build bridge\Inkora.PrintBridge\Inkora.PrintBridge.csproj` OK; `powershell -NoProfile -ExecutionPolicy Bypass -File bridge\build-release.ps1 -Version 1.6.5` OK y genero ZIP con exe, Sumatra, `install.ps1`, `install.bat` y README; `install.ps1` parsea OK; `npm.cmd run build` OK con warnings preexistentes; `git diff --check` OK con avisos CRLF. Se pusheo `bridge-v1.6.5`, el asset GitHub `Inkora.PrintBridge.zip` responde `200`, y Vercel quedo `READY` en produccion sin logs de error recientes.
- Auditoria: Se confirmo que el Bridge activo fallaba porque `/health` decia `sumatraPdf: false` aunque `SumatraPDF.exe` estaba junto al exe en Descargas. La causa probable era busqueda basada en `AppContext.BaseDirectory` en un publish single-file, no en la ruta real del proceso. Se mantuvieron fuera de scope `.claude/settings*.json`, `Inkora.PrintBridge.zip` raiz y `Messi 2.3mf`.
- Pendiente/Riesgos: Probar fisicamente actualizar el Bridge desde la web e imprimir un pedido con 7 copias; si la Epson sigue forzando el preset de 1 copia, revisar DEVMODE/preset de driver, pero el Bridge ya deberia usar Sumatra y enviar `Nx` como un unico trabajo.

---

## 2026-07-03 18:27 -03:00 - ChatGPT Codex

- Objetivo: Corregir que dos PCs se pisen al editar cantidades impresas/troqueladas/desperdicio en produccion.
- Cambios: Se ajusto `components/ProductionTab.js` y `app/operarios/page.js` para guardar parches parciales por tarea y, justo antes de llamar la RPC, leer la fila fresca de DB para completar los campos no tocados. Esto evita reenviar contadores viejos desde otra PC y es compatible con la funcion SQL vieja y nueva. Se actualizo `sql/production_orders_and_operators.sql` y se agrego `sql/production_task_progress_partial_updates.sql` como mejora idempotente para que la DB soporte `NULL` = "no tocar este campo". Tambien se alineo `/operarios` al Bridge `1.6.3`.
- Verificacion: `node --check components\ProductionTab.js` OK; `node --check app\operarios\page.js` OK; `CI=true npm.cmd run build` OK con warnings preexistentes; `git diff --check` OK con avisos CRLF. Se intento aplicar SQL con `npx supabase db query --linked --file sql\production_task_progress_partial_updates.sql`, pero Supabase CLI pidio `SUPABASE_ACCESS_TOKEN`; queda como mejora opcional, no bloquea deploy.
- Auditoria: Se reviso la entrada anterior sobre Bridge/release y el estado del repo. Se mantuvieron fuera de scope `.claude/settings*.json`, `Inkora.PrintBridge.zip` raiz y `Messi 2.3mf`.
- Pendiente/Riesgos: Desplegar al cerrar este turno y probar con dos PCs editando columnas distintas de la misma tarea. Si se quiere blindar tambien el caso de dos escrituras simultaneas al mismo milisegundo, ejecutar luego en Supabase SQL Editor `sql/production_task_progress_partial_updates.sql`.

---

## 2026-07-03 18:16 -03:00 - ChatGPT Codex

- Objetivo: Cerrar la publicacion del fix del Bridge y confirmar que web/release quedaron disponibles sin intervencion manual.
- Cambios: Se pusheo `main` con commit `51fbf61` (`Fix bridge printing and release flow`) y se creo/pusheo el tag `bridge-v1.6.3`. GitHub Actions creo el release del Bridge y dejo disponible `Inkora.PrintBridge.zip`.
- Verificacion: Release GitHub `bridge-v1.6.3` responde `200`; asset `Inkora.PrintBridge.zip` responde `200` con tamano aproximado 74.5 MB. Vercel muestra produccion `Ready` en `https://inkora-next-1ae23h7gm-inkorashop-7809s-projects.vercel.app`, con aliases `https://inkora.com.ar`, `https://www.inkora.com.ar` y `https://inkora-next.vercel.app`. `vercel logs --since 1h --level error` no encontro logs.
- Auditoria: Se confirmo que los cambios publicados corresponden al commit de Bridge/web/workflow y que los archivos ajenos `.claude/settings*.json`, `Inkora.PrintBridge.zip` raiz y `Messi 2.3mf` siguen fuera de scope.
- Pendiente/Riesgos: Hacer una prueba real desde una PC con Epson usando 7 copias para confirmar que Sumatra recibe una sola orden con `7x`; si una instalacion antigua no trae Sumatra, actualizar desde la web o reinstalar con el ZIP nuevo.

---

## 2026-07-03 18:13 -03:00 - ChatGPT Codex

- Objetivo: Subir lo necesario y hacer el maximo posible para publicar Bridge/web sin intervencion manual.
- Cambios: Se agrego `.github/workflows/release-bridge.yml` para que GitHub Actions cree automaticamente el release `bridge-v*` con el ZIP del Bridge al empujar un tag. Se preparo la publicacion de los cambios documentales, Bridge `1.6.3`, web apuntando a `bridge-v1.6.3` y workflow de release.
- Verificacion: `CI=true npm.cmd run build` OK con warnings preexistentes; `C:\Program Files\dotnet\dotnet.exe build bridge\Inkora.PrintBridge\Inkora.PrintBridge.csproj` OK tras permitir restore NuGet; `git diff --check` OK. `gh` existe pero no esta autenticado, por eso se uso workflow con `GITHUB_TOKEN` de Actions.
- Auditoria: Se reviso la entrada anterior y se confirmo que el ZIP local ya contenia `Inkora.PrintBridge.exe` y `SumatraPDF.exe`. Se mantuvieron fuera de scope `.claude/settings*.json`, `Inkora.PrintBridge.zip` raiz y `Messi 2.3mf`.
- Pendiente/Riesgos: Luego del push/tag hay que verificar que GitHub Actions haya creado el release y que Vercel haya desplegado `main`; si el workflow falla, revisar permisos de Actions o descarga de SumatraPDF.

---

## 2026-07-03 18:06 -03:00 - ChatGPT Codex

- Objetivo: Corregir el flujo de impresion para respetar copias desde la web, mejorar el Bridge/instalador y preparar una nueva version actualizable.
- Cambios: Se subio el Bridge a `1.6.3`; `PrintJobService` ahora usa Sumatra con `-print-settings Nx` y DEVMODE para dar prioridad a las copias pedidas, y ya no envia multiples trabajos `shell-printto` cuando faltan copias multiples. `build-release.ps1` ahora incluye `SumatraPDF.exe` en el ZIP. El auto-update del Bridge extrae/copia el paquete completo, muestra estados en el panel local y reinicia con `--updated`. Se mejoro el estilo WinForms del Bridge y se rehizo `install.ps1` con pasos claros. La web apunta a `bridge-v1.6.3` y advierte si falta Sumatra.
- Verificacion: `C:\Program Files\dotnet\dotnet.exe build bridge\Inkora.PrintBridge\Inkora.PrintBridge.csproj` OK; `powershell -NoProfile -ExecutionPolicy Bypass -File bridge\build-release.ps1` OK y genero `bridge\Inkora.PrintBridge\bin\Inkora.PrintBridge.zip` con `Inkora.PrintBridge.exe` y `SumatraPDF.exe`; `CI=true npm.cmd run build` OK con warnings preexistentes; `git diff --check` OK.
- Auditoria: Se siguio el protocolo de `AGENTS.md`, se reviso la ultima entrada y se confirmo que los cambios previos de documentacion seguian sin commitear. No se tocaron `.claude/settings*.json`, `Inkora.PrintBridge.zip` raiz ni `Messi 2.3mf`.
- Pendiente/Riesgos: Para que el boton web "Actualizar" funcione en produccion hay que publicar manualmente el release GitHub `bridge-v1.6.3` con `bridge\Inkora.PrintBridge\bin\Inkora.PrintBridge.zip` y luego desplegar la web. No se pudo crear el release porque `gh` no esta instalado y las herramientas GitHub disponibles no exponen releases.

---

## 2026-07-03 17:22 -03:00 - ChatGPT Codex

- Objetivo: Cambiar la practica del proyecto para que `AGENTS.md` sea el archivo de entrada unico para IAs.
- Cambios: Se actualizo `AGENTS.md` como puerta de entrada unica y protocolo principal. Se ajustaron `CONTEXT.md`, `CLAUDE.md` y `AI_RUN_LOG.md` para apuntar a `AGENTS.md` en lugar de `CONTEXT.md`.
- Verificacion: Se leyeron `AGENTS.md`, `CONTEXT.md`, `AI_RUN_LOG.md`, `CLAUDE.md` y se reviso `git status --short`. No se corrio build porque solo se modifico documentacion.
- Auditoria: Se reviso la entrada anterior de `AI_RUN_LOG.md`; ahora queda reemplazada la recomendacion anterior de leer `CONTEXT.md` por la nueva recomendacion de leer `AGENTS.md`.
- Pendiente/Riesgos: El prompt corto recomendado desde ahora es: `Lee AGENTS.md y segui el protocolo del proyecto. Tarea: ...`

---

## 2026-07-03 17:20 -03:00 - ChatGPT Codex

- Objetivo: Simplificar el arranque para que el usuario solo tenga que pedirle a la IA leer un archivo.
- Cambios: Se actualizo `CONTEXT.md` como archivo de entrada unico. Se ajustaron `AGENTS.md`, `CLAUDE.md` y `AI_RUN_LOG.md` para redirigir al mismo flujo: leer `CONTEXT.md`, leer `AGENTS.md`, revisar `AI_RUN_LOG.md`, auditar `git status --short` y actualizar la bitacora al cerrar.
- Verificacion: Se leyeron los archivos de protocolo antes de editar y se reviso `git status --short`. No se corrio build porque solo se modifico documentacion.
- Auditoria: Se contrasto la entrada anterior de `AI_RUN_LOG.md` con el estado actual; los archivos de protocolo siguen sin commitear y `CONTEXT.md` continua modificado.
- Pendiente/Riesgos: En nuevos turnos, el prompt corto recomendado es: `Lee CONTEXT.md y segui el protocolo del proyecto. Tarea: ...`

---

## 2026-07-03 17:11 -03:00 - ChatGPT Codex

- Objetivo: Mostrar con un ejemplo real como se actualiza la bitacora para que la proxima IA pueda retomar.
- Cambios: Se agrego esta entrada en `AI_RUN_LOG.md` siguiendo el formato definido en `AGENTS.md`.
- Verificacion: Se leyeron `AGENTS.md`, `CONTEXT.md` y `AI_RUN_LOG.md`; tambien se reviso `git status --short`. No se corrio build porque solo se edito documentacion.
- Auditoria: Se reviso la entrada anterior de ChatGPT Codex y se contrasto con el estado del repo; los archivos `AGENTS.md`, `CLAUDE.md` y `AI_RUN_LOG.md` siguen sin commitear, y `CONTEXT.md` continua modificado.
- Pendiente/Riesgos: Mantener esta practica al cierre de cada turno de trabajo para que Codex y Claude puedan auditarse entre si.

---

## 2026-07-03 17:07 -03:00 - ChatGPT Codex

- Objetivo: Crear un flujo ordenado para que ChatGPT Codex y Claude Code puedan alternar trabajo en el proyecto.
- Cambios: Se agregaron `AGENTS.md`, `CLAUDE.md` y `AI_RUN_LOG.md`. Se preparo una referencia desde `CONTEXT.md` hacia el nuevo protocolo.
- Verificacion: Se reviso `git status --short` y el diff existente de `CONTEXT.md` antes de editar. No se corrio build porque solo se modifico documentacion.
- Auditoria: Se confirmo que no existian `AGENTS.md` ni `CLAUDE.md`, y que `CONTEXT.md` ya tenia cambios previos sobre deploy manual que se conservaron.
- Pendiente/Riesgos: Cada IA debe actualizar esta bitacora al cerrar su turno de trabajo para que el flujo funcione.
